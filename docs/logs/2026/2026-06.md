# 2026-06

## 2026-06-01 M4 Workflow Task Center State Closure

- 继续 `codex/m4-m20-full-implementation` active goal，本轮只推进 M4.2 本地 workflow task center，不提高 Overall。
- 后端：新增 scheduler runtime state 持久化，SQLite `scheduler_tasks` 增加 `status`、`last_run_at`、`last_error`、`retry_count`；任务运行开始、完成、失败和 retry delay 回 idle 时都会保存状态。
- 迁移：新增 schema version `13`，并补 drift repair；即使历史库 `schema_migrations` 已前进但缺 scheduler runtime 列，也会在 `Migrate()` 后修复。
- 前端：Automation 任务页点击“立即执行”后刷新任务列表，并展示步骤数、上次运行、错误和重试次数，便于 operator 检查 create/run/status 闭环。
- 验证：`go test ./backend/internal/scheduler ./backend/internal/database -count=1` passed；`pnpm typecheck` passed。
- 边界：本轮不代表真实 provider 凭证 smoke、远程代理/TLS、跨机器 SessionBundle、AdsPower refresh、完整 headed realism 或完整 `450` observed/replay coverage 已完成。

## 2026-06-01 M4 Automation Primitive v1

- 继续 M4 本地实用闭环，本轮推进 M4.3 CDP task runner primitive，不提高 Overall。
- 后端：`CDPTaskRunner` 新增 typed actions：`select`、`dialog`、`download`、`upload`、`iframe`、`tab`，常用 operator action 不再必须写 raw `cdp` method。
- 验收：新增 mock CDP test 覆盖这些 action 的命令与参数形状，保留 raw `cdp` 作为 escape hatch；M4 acceptance gate v2 新增 `automation_primitives_contract` 本地源/测试覆盖检查。
- 验证：`go test ./backend/internal/scheduler -count=1` passed；`go test ./backend/... -count=1` passed；`pnpm typecheck` passed；`scripts/m4_acceptance_gate.ps1` 为 `expected_blocked`，summary `passed=2`、`expectedBlocked=4`、`failed=0`。
- 边界：这是本地 runner primitive contract，不代表目标站生产流程、真实 provider smoke、远程代理/TLS、跨机器 SessionBundle、AdsPower refresh 或完整 `450` live behavior replay 已完成。

## 2026-06-01 M4 Provider Dry-run Closure

- 继续 M4 本地 operator 闭环，本轮推进 M4.4 provider dry-run/failure taxonomy，不提高 Overall。
- 脚本：`scripts/provider_acceptance_preflight.ps1` 升级为 v3，输出 CAPTCHA/SMS/Email `dryRunStatus`、`dryRunContract`、`dryRunNextAction`、`failureTaxonomyStatus`、`failureTaxonomy`，并继续把真实 provider smoke 保持为 blocker。
- 桌面/API/UI：`DesktopProviderReadinessItem` 和 TS 类型新增 dry-run/failure taxonomy 字段；Settings 页新增 Provider dry-run 验收表，展示 credentials、dry-run、taxonomy、real smoke、latest report、next action 和 failure reason。
- 验收：`scripts/m4_acceptance_gate.ps1` 升级为 v3，会刷新 provider preflight 并新增 `provider_dry_run_contract` 本地报告检查；最新 M4 report 为 `expected_blocked`，summary `passed=3`、`expectedBlocked=4`、`failed=0`。
- 验证：`cargo test provider_production_readiness_reports_contract_blocks_without_claiming_closure` passed；`cargo fmt --check` passed；`pnpm typecheck` passed；`provider_acceptance_preflight.ps1` 预期 `blocked_missing_credentials`；`m4_acceptance_gate.ps1` 预期 `expected_blocked`。
- 边界：这是本地 dry-run/operator readiness 和报告 schema 证据，不代表真实 provider 凭证 smoke、远程代理/TLS、跨机器 SessionBundle、AdsPower refresh 或完整 `450` live behavior replay 已完成。

## 2026-06-01 M4/M6 Desktop/Profile Comparison Entry

- 继续 M4 证据闭环并为 M6 observed comparison 铺路，本轮补 desktop WebView 采集入口和同窗 comparison gate，不提高 Overall。
- 前端：Dashboard 验收证据卡新增 `采集 WebView` action，在真实桌面 WebView 中采样 fingerprint、canvas、audio、WebRTC、storage/leak signals，并调用 `collect_validation_report` 写入 validation report。
- 脚本：`scripts/profile_browser_comparison_gate.ps1` 升级为 v3，新增 `sameRunStatus`、`maxPairAgeMinutes`、`desktopProfileDeltaMinutes`；只有 desktop WebView 与 profile-browser 双边 report 在同窗内且五类 category 都 comparable 时才 `passed`。
- 修复：Tauri `collect_validation_report` 的 browser signal 归一化现在保留 `fingerprint` category，并在 report categories 中声明该类，防止 Dashboard 采集到的 WebView fingerprint evidence 被降级成 `detector`。
- 验收：最新 profile-browser comparison report 仍为 `blocked_missing_desktop_webview_report`，因为本轮没有启动 GUI 并点击 WebView 采集；最新 M4 gate 仍为 `expected_blocked`，summary `passed=3`、`expectedBlocked=4`、`failed=0`。
- 验证：`pnpm typecheck` passed；`git diff --check` passed；`profile_browser_comparison_gate.ps1` 预期 blocked；`m4_acceptance_gate.ps1` 预期 expected_blocked。
- 边界：WebView 采集只证明桌面壳 WebView scoped signals；profile-browser proof、完整 `450` observed coverage、远程代理/TLS、provider、跨机器 SessionBundle 和 AdsPower refresh 仍需各自真实报告。

## 2026-06-01 M4 SessionBundle Operator Loop

- 继续 M4 本机 operator 闭环，本轮推进 M4.6 SessionBundle export/preflight/dry-run/confirmed local restore，不提高 Overall。
- 前端：Settings 页新增 `SessionBundle 本机验收` 卡，输入 profile/bundle/target profile 后可触发 export、preflight、dry-run 和确认本机恢复，并展示 restore plan、missing refs、conflicts、writePerformed、restored binding count 与本机/跨机器边界。
- 脚本：`scripts/m4_acceptance_gate.ps1` 升级为 v4，新增 `session_bundle_operator_contract` 本地 source gate，检查 Settings UI、desktop service wrapper、TS 类型和 Rust SessionBundle 实现/测试 marker。
- 验收：最新 M4 report 为 `expected_blocked`，summary `passed=4`、`expectedBlocked=4`、`failed=0`；expected blocked 仍来自 runtime adapter B1-B5、provider credentials/real smoke、跨机器 SessionBundle 和 desktop/profile comparison。
- 验证：`pnpm typecheck` passed；`scripts/m4_acceptance_gate.ps1` 预期 `expected_blocked` 且 `failed=0`。
- 边界：这是本机 UI/API operator loop，不代表跨机器 portability、真实 provider、远程代理/TLS、AdsPower refresh 或完整 `450` observed/replay coverage 已完成。

## 2026-06-01 M4 Typed Facade Shrink

- 继续 M4.8 typed facade shrink，本轮只收窄 synchronizer/workbench high-traffic bridge DTO，不移除过渡 Wails/core bridge。
- 类型：`src/types/desktop.ts` 新增 `DesktopCoreSyncGroup`、`DesktopCoreSyncOperation`、`DesktopCoreSyncWindowPlacement`、`DesktopCoreWorkbenchTask` 等共享 DTO。
- 服务/API：`src/services/desktop.ts` 的 `SynchronizerListGroups`、`SynchronizerArrangeProfiles`、`SynchronizerGetOperationLog`、`SynchronizerListTasks`、`SynchronizerSaveTasks` 不再暴露 `unknown[]`；`src/modules/synchronizer/api.ts` 移除对应 `as Promise<...>` cast。
- 脚本：M4 gate v4 新增 `typed_facade_shrink_contract` source check；最新 M4 report 为 `expected_blocked`，summary `passed=5`、`expectedBlocked=4`、`failed=0`。
- 验证：`pnpm typecheck` passed；`git diff --check` passed；`scripts/m4_acceptance_gate.ps1` 预期 `expected_blocked` 且 `failed=0`。
- 边界：这只是第一批 facade 收窄；browser 模块和剩余 dynamic RPC 仍需后续切片处理。

## 2026-06-01 M4 Runtime Adapter Operator Surface

- 继续 M4.7 operator visibility，本轮只把 runtime adapter evidence 从 contract/report 拉到 Dashboard，不提高 Overall。
- 前端：Dashboard 新增 Runtime Adapter 卡，通过 `fetchReleaseSmokeContract()` 读取 release smoke contract，并按 evidence strength 对 adapter 排序，展示 adapter id、runner kind、status、profile runtime evidence、fingerprint depth 和前两个 blocker。
- 脚本：M4 gate v5 新增 `runtime_adapter_operator_contract` source check，覆盖 Dashboard UI、Dashboard API、desktop service wrapper 和共享 TS 类型。
- 验收：最新 M4 report 为 `expected_blocked`，summary `passed=6`、`expectedBlocked=4`、`failed=0`。
- 验证：`pnpm typecheck` passed；`git diff --check` passed；`scripts/m4_acceptance_gate.ps1` 预期 `expected_blocked` 且 `failed=0`。
- 边界：这是本地 operator 可见性，不代表完整 headed realism、B1-B5、远程代理/TLS、真实 provider、跨机器 SessionBundle、AdsPower refresh 或完整 `450` observed/replay coverage 已完成。
