# 2026-06

## 2026-06-01 M4 Workflow Task Center State Closure

- 继续 `codex/m4-m20-full-implementation` active goal，本轮只推进 M4.2 本地 workflow task center，不提高 Overall。
- 后端：新增 scheduler runtime state 持久化，SQLite `scheduler_tasks` 增加 `status`、`last_run_at`、`last_error`、`retry_count`；任务运行开始、完成、失败和 retry delay 回 idle 时都会保存状态。
- 迁移：新增 schema version `13`，并补 drift repair；即使历史库 `schema_migrations` 已前进但缺 scheduler runtime 列，也会在 `Migrate()` 后修复。
- 前端：Automation 任务页点击“立即执行”后刷新任务列表，并展示步骤数、上次运行、错误和重试次数，便于 operator 检查 create/run/status 闭环。
- 验证：`go test ./backend/internal/scheduler ./backend/internal/database -count=1` passed；`pnpm typecheck` passed。
- 边界：本轮不代表真实 provider 凭证 smoke、远程代理/TLS、跨机器 SessionBundle、AdsPower refresh、完整 headed realism 或完整 `450` observed/replay coverage 已完成。

## 2026-06-01 M4 Automation Primitive v1

- 继续 M4 本地实用闭环，本轮推进 M4.3 CDP task runner primitive，不提高 Overall。
- 后端：`CDPTaskRunner` 新增 typed actions：`select`、`dialog`、`download`、`upload`、`iframe`、`tab`，常用 operator action 不再必须写 raw `cdp` method。
- 验收：新增 mock CDP test 覆盖这些 action 的命令与参数形状，保留 raw `cdp` 作为 escape hatch；M4 acceptance gate v2 新增 `automation_primitives_contract` 本地源/测试覆盖检查。
- 验证：`go test ./backend/internal/scheduler -count=1` passed；`go test ./backend/... -count=1` passed；`pnpm typecheck` passed；`scripts/m4_acceptance_gate.ps1` 为 `expected_blocked`，summary `passed=2`、`expectedBlocked=4`、`failed=0`。
- 边界：这是本地 runner primitive contract，不代表目标站生产流程、真实 provider smoke、远程代理/TLS、跨机器 SessionBundle、AdsPower refresh 或完整 `450` live behavior replay 已完成。

## 2026-06-01 M4 Provider Dry-run Closure

- 继续 M4 本地 operator 闭环，本轮推进 M4.4 provider dry-run/failure taxonomy，不提高 Overall。
- 脚本：`scripts/provider_acceptance_preflight.ps1` 升级为 v3，输出 CAPTCHA/SMS/Email `dryRunStatus`、`dryRunContract`、`dryRunNextAction`、`failureTaxonomyStatus`、`failureTaxonomy`，并继续把真实 provider smoke 保持为 blocker。
- 桌面/API/UI：`DesktopProviderReadinessItem` 和 TS 类型新增 dry-run/failure taxonomy 字段；Settings 页新增 Provider dry-run 验收表，展示 credentials、dry-run、taxonomy、real smoke、latest report、next action 和 failure reason。
- 验收：`scripts/m4_acceptance_gate.ps1` 升级为 v3，会刷新 provider preflight 并新增 `provider_dry_run_contract` 本地报告检查；最新 M4 report 为 `expected_blocked`，summary `passed=3`、`expectedBlocked=4`、`failed=0`。
- 验证：`cargo test provider_production_readiness_reports_contract_blocks_without_claiming_closure` passed；`cargo fmt --check` passed；`pnpm typecheck` passed；`provider_acceptance_preflight.ps1` 预期 `blocked_missing_credentials`；`m4_acceptance_gate.ps1` 预期 `expected_blocked`。
- 边界：这是本地 dry-run/operator readiness 和报告 schema 证据，不代表真实 provider 凭证 smoke、远程代理/TLS、跨机器 SessionBundle、AdsPower refresh 或完整 `450` live behavior replay 已完成。

## 2026-06-01 M4/M6 Desktop/Profile Comparison Entry

- 继续 M4 证据闭环并为 M6 observed comparison 铺路，本轮补 desktop WebView 采集入口和同窗 comparison gate，不提高 Overall。
- 前端：Dashboard 验收证据卡新增 `采集 WebView` action，在真实桌面 WebView 中采样 fingerprint、canvas、audio、WebRTC、storage/leak signals，并调用 `collect_validation_report` 写入 validation report。
- 脚本：`scripts/profile_browser_comparison_gate.ps1` 升级为 v3，新增 `sameRunStatus`、`maxPairAgeMinutes`、`desktopProfileDeltaMinutes`；只有 desktop WebView 与 profile-browser 双边 report 在同窗内且五类 category 都 comparable 时才 `passed`。
- 修复：Tauri `collect_validation_report` 的 browser signal 归一化现在保留 `fingerprint` category，并在 report categories 中声明该类，防止 Dashboard 采集到的 WebView fingerprint evidence 被降级成 `detector`。
- 验收：最新 profile-browser comparison report 仍为 `blocked_missing_desktop_webview_report`，因为本轮没有启动 GUI 并点击 WebView 采集；最新 M4 gate 仍为 `expected_blocked`，summary `passed=3`、`expectedBlocked=4`、`failed=0`。
- 验证：`pnpm typecheck` passed；`git diff --check` passed；`profile_browser_comparison_gate.ps1` 预期 blocked；`m4_acceptance_gate.ps1` 预期 expected_blocked。
- 边界：WebView 采集只证明桌面壳 WebView scoped signals；profile-browser proof、完整 `450` observed coverage、远程代理/TLS、provider、跨机器 SessionBundle 和 AdsPower refresh 仍需各自真实报告。

## 2026-06-01 M4 SessionBundle Operator Loop

- 继续 M4 本机 operator 闭环，本轮推进 M4.6 SessionBundle export/preflight/dry-run/confirmed local restore，不提高 Overall。
- 前端：Settings 页新增 `SessionBundle 本机验收` 卡，输入 profile/bundle/target profile 后可触发 export、preflight、dry-run 和确认本机恢复，并展示 restore plan、missing refs、conflicts、writePerformed、restored binding count 与本机/跨机器边界。
- 脚本：`scripts/m4_acceptance_gate.ps1` 升级为 v4，新增 `session_bundle_operator_contract` 本地 source gate，检查 Settings UI、desktop service wrapper、TS 类型和 Rust SessionBundle 实现/测试 marker。
- 验收：最新 M4 report 为 `expected_blocked`，summary `passed=4`、`expectedBlocked=4`、`failed=0`；expected blocked 仍来自 runtime adapter B1-B5、provider credentials/real smoke、跨机器 SessionBundle 和 desktop/profile comparison。
- 验证：`pnpm typecheck` passed；`scripts/m4_acceptance_gate.ps1` 预期 `expected_blocked` 且 `failed=0`。
- 边界：这是本机 UI/API operator loop，不代表跨机器 portability、真实 provider、远程代理/TLS、AdsPower refresh 或完整 `450` observed/replay coverage 已完成。

## 2026-06-01 M4 Typed Facade Shrink

- 继续 M4.8 typed facade shrink，本轮只收窄 synchronizer/workbench high-traffic bridge DTO，不移除过渡 Wails/core bridge。
- 类型：`src/types/desktop.ts` 新增 `DesktopCoreSyncGroup`、`DesktopCoreSyncOperation`、`DesktopCoreSyncWindowPlacement`、`DesktopCoreWorkbenchTask` 等共享 DTO。
- 服务/API：`src/services/desktop.ts` 的 `SynchronizerListGroups`、`SynchronizerArrangeProfiles`、`SynchronizerGetOperationLog`、`SynchronizerListTasks`、`SynchronizerSaveTasks` 不再暴露 `unknown[]`；`src/modules/synchronizer/api.ts` 移除对应 `as Promise<...>` cast。
- 脚本：M4 gate v4 新增 `typed_facade_shrink_contract` source check；最新 M4 report 为 `expected_blocked`，summary `passed=5`、`expectedBlocked=4`、`failed=0`。
- 验证：`pnpm typecheck` passed；`git diff --check` passed；`scripts/m4_acceptance_gate.ps1` 预期 `expected_blocked` 且 `failed=0`。
- 边界：这只是第一批 facade 收窄；browser 模块和剩余 dynamic RPC 仍需后续切片处理。

## 2026-06-01 M4 Runtime Adapter Operator Surface

- 继续 M4.7 operator visibility，本轮只把 runtime adapter evidence 从 contract/report 拉到 Dashboard，不提高 Overall。
- 前端：Dashboard 新增 Runtime Adapter 卡，通过 `fetchReleaseSmokeContract()` 读取 release smoke contract，并按 evidence strength 对 adapter 排序，展示 adapter id、runner kind、status、profile runtime evidence、fingerprint depth 和前两个 blocker。
- 脚本：M4 gate v5 新增 `runtime_adapter_operator_contract` source check，覆盖 Dashboard UI、Dashboard API、desktop service wrapper 和共享 TS 类型。
- 验收：最新 M4 report 为 `expected_blocked`，summary `passed=6`、`expectedBlocked=4`、`failed=0`。
- 验证：`pnpm typecheck` passed；`git diff --check` passed；`scripts/m4_acceptance_gate.ps1` 预期 `expected_blocked` 且 `failed=0`。
- 边界：这是本地 operator 可见性，不代表完整 headed realism、B1-B5、远程代理/TLS、真实 provider、跨机器 SessionBundle、AdsPower refresh 或完整 `450` observed/replay coverage 已完成。

## 2026-06-01 M4 Browser Wails Binding Type Contract

- 继续 M4.8 typed facade shrink，本轮收窄 browser 模块 Wails binding 动态入口，不移除过渡 Wails/core bridge。
- 前端/API：`src/modules/browser/api.ts` 新增 `BrowserNativeBindings`、`BrowserNativeWindow` 和 `getWindowGoApp()`；高频 profile/instance/core/proxy/cookie/snapshot/bookmark/group/recording/LLM bindings 不再通过 `const bindings: any = await getBindings()` 或 `(window as any).go?.main?.App` 访问。
- 脚本：M4 gate v6 扩展 `typed_facade_shrink_contract` source check，要求 synchronizer DTO 和 browser Wails binding marker 同时存在，并阻止上述 browser dynamic marker 回归。
- 验收：最新 M4 report 为 `expected_blocked`，summary `passed=6`、`expectedBlocked=4`、`failed=0`。
- 验证：`pnpm typecheck` passed；`git diff --check` passed；`scripts/m4_acceptance_gate.ps1` 预期 `expected_blocked` 且 `failed=0`。
- 边界：这是类型和 source contract 收窄，不代表 browser payload schema 已全部规范化、`tauriWailsBridge` 已移除、真实 provider/远程代理/TLS/跨机器/AdsPower/完整 `450` coverage 已完成。

## 2026-06-01 M4 Safety Logging Contract

- 继续 M4.9 safety/logging，本轮只补本地 logger 脱敏合同，不提高 Overall。
- 后端：`backend/internal/logger` 新增默认敏感字段、free-form 文本脱敏、结构化字段递归脱敏和 `RedactLogEntry`；logger 写入路径和 Text/JSON formatter 都会遮蔽 password/token/api_key/authorization/credential/secret/cookie、Bearer/Basic 值和 URL userinfo 密码。
- 配置：默认 interceptor sensitive fields 从 password/token/secret 扩展到 API key、authorization、credential、client_secret、private_key、cookie 等常见凭证字段。
- 脚本：`scripts/m4_acceptance_gate.ps1` 升级为 v7，新增 `safety_logging_contract` source/test gate；最新 M4 report `data/reports/m4-acceptance/m4-acceptance-gate-1780317928923.json` 为 `expected_blocked`，summary `passed=7`、`expectedBlocked=4`、`failed=0`。
- 验证：`go test ./backend/internal/logger ./backend/internal/config ./backend/internal/scheduler ./backend/internal/launchcode -count=1` passed；`pnpm typecheck` passed；M4 gate v7 预期 `expected_blocked` 且 `failed=0`。
- 边界：这是本地日志脱敏 source/test 证据，不代表真实 provider 凭证 smoke、外部分发日志审计、历史报告清洗、远程代理/TLS、跨机器 SessionBundle、AdsPower refresh 或完整 `450` coverage 已完成。

## 2026-06-01 M4 Total Gate

- 继续 M4.10 total gate，本轮只改善 M4 总闸可读性和 Dashboard 分类，不新增外部证明。
- 脚本：`scripts/m4_acceptance_gate.ps1` 升级为 v8；当本地 gate 全部通过且只剩预期外部阻塞时，顶层 `status` 和 `operatorStatus` 输出 `passed_with_expected_external_blockers`，同时保留 `gateClassificationStatus=expected_blocked`。
- 报告：新增 `m4TotalGate`，包含 localContractStatus、externalBlockerCount、failedGateIds、expectedBlockedGateIds、passedGateIds 和 nextAction。
- 桌面：evidence history 读取 M4 report 时识别 `passed_with_expected_external_blockers` 为 `expected_external_blockers`，Dashboard 状态色也按预期外部阻塞处理。
- 验收：最新 M4 report `data/reports/m4-acceptance/m4-acceptance-gate-1780319108325.json` 为 `passed_with_expected_external_blockers`，gate classification 为 `expected_blocked`，summary `passed=7`、`expectedBlocked=4`、`failed=0`。
- 验证：`cargo fmt --check` passed；`cargo test evidence_report_history_reads_m4_acceptance_reports` passed；`pnpm typecheck` passed；M4 gate v8 passed with expected external blockers。
- 边界：provider credentials、remote proxy/TLS、cross-machine SessionBundle、same-run profile-browser comparison、AdsPower refresh 和完整 `450` coverage 仍必须等各自真实报告。

## 2026-06-01 M5 Release Health Budget

- 继续 M5 release performance/health，本轮只补 release health report contract 和 Dashboard 可见性，不提高 Overall。
- 脚本：`scripts/release_performance_smoke.ps1` 升级为 v2，新增 `budgetStatus`、三项 `budgetResults`、10% drift target、`driftReason` 边界、`healthSummary` 和 `mitigationHints`；`processBreakdown` 转为对象输出，避免 RSS 汇总失败。
- Gate：新增 `scripts/m5_release_health_gate.ps1`，检查最新 v2 report、budget results、health summary、mitigation hints 和 drift reason enforcement；最新 report 为 `passed_with_budget_overrun`。
- 桌面：release smoke contract 改为检查当前唯一入口 `personal-pilot-tauri.exe`，Dashboard evidence history 新增 M5 Health，并展示 release health status、budget results 和 mitigation hint。
- 验收：最新 clean release smoke `data/reports/release-smoke/release-performance-smoke-1780323308586.json` 为 `warning` / `budgetStatus=over_budget`，实测 cold start `941ms` passed、idle RSS `467MB` over budget、process count `9` over budget；M5 gate `data/reports/m5-release-health/m5-release-health-gate-1780323678612.json` failed checks `0`。
- 边界：`passed_with_budget_overrun` 只证明 M5 health report contract 可用；release performance 仍不能写 green，provider、远程代理/TLS、跨机器 SessionBundle、AdsPower refresh 和完整 `450` coverage 不变。

## 2026-06-02 M13 Evidence Trend v1

- 继续 M13 evidence operations，本轮只补本地 report history 趋势提示，不提高 Overall。
- 桌面：`list_desktop_evidence_reports` 在按 `generatedAt` 倒序读取本地 evidence reports 后，为每个 report 附加同 kind 上一份 report 的 `previousStatus`、`previousFailureReason`、`previousGeneratedAt`、`statusTrend`、`failureReasonTrend` 和 `trendSummary`；同时间戳 report 用 `reportPath` 做稳定 tie-breaker。
- 前端：Dashboard evidence rows 在已有 status、failureReason、nextAction 之外显示趋势提示，辅助 operator 判断失败原因是否变化。
- 语义边界：趋势字段只用于排障可见性，不改变 report status、evidenceLevel、gate classification、expected blocker 或 blocked/warning/passed 口径。
- 验收目标：`cargo test evidence_report_history`、`pnpm typecheck`、`git diff --check` 通过后提交；provider、远程代理/TLS、跨机器 SessionBundle、AdsPower refresh、完整 `450` observed/replay coverage 仍等待真实外部证据。

## 2026-06-02 M13 Evidence Risk Trend v2

- 继续 M13 evidence operations，本轮把本地 trend 从 status/failureReason 扩展到 risk trend，不提高 Overall。
- 桌面：`DesktopEvidenceReportSummary` 新增 `riskLevel`、`riskScore`、`previousRiskLevel`、`previousRiskScore` 和 `riskTrend`；风险等级只从现有 `status` / `evidenceLevel` 派生，不参与 gate 判定。
- 前端：Dashboard evidence rows 的趋势提示追加 `riskTrend`，辅助 operator 判断同 kind report 是风险改善、退化还是不变。
- 验收目标：`cargo test evidence_report_history`、`pnpm typecheck`、`git diff --check`、`scripts/live_truth_guard.ps1` 和 `scripts/m4_acceptance_gate.ps1` 通过后提交。
- 边界：risk score 是本地排障提示，不代表 provider、远程代理/TLS、跨机器 SessionBundle、AdsPower refresh 或完整 `450` coverage 已完成。

## 2026-06-02 M6 Profile/Browser Comparison Readability

- 继续 M6 observed comparison，本轮只改善 profile/browser comparison report 在 Dashboard evidence history 中的可读性，不提高 Overall。
- 桌面：`profile_browser_comparison` summary 现在展示 `sameRunStatus`、desktop/profile signal count、comparable category count、pair delta/max window 和缺失 category status。
- 桌面：`nextAction` 会按 `blocked_missing_desktop_webview_report`、`blocked_missing_profile_browser_report`、`blocked_stale_comparison_pair`、`partial_comparison_only` 给出下一步，而不是只重复 failureReason。
- 验收目标：`cargo test evidence_report_history`、`pnpm typecheck`、`git diff --check`、`scripts/live_truth_guard.ps1` 和 `scripts/m4_acceptance_gate.ps1` 通过后提交。
- 边界：本轮没有实际点击 Dashboard `采集 WebView`，没有生成新的 same-run passed comparison；真实 desktop/profile 同窗证据仍待采集。

## 2026-06-02 M13 Failure Reason Taxonomy v1

- 继续 M13 evidence operations，本轮新增机器可读 `failureReasonCategory`，不提高 Overall。
- 桌面：release budget、provider credentials、remote proxy、desktop/profile comparison、cross-machine SessionBundle、runtime adapter、external distribution 和 expected external blocker 会映射到稳定 category。
- 前端：Dashboard evidence rows 只展示 failure category，不改变 status 色彩、failureReason、riskTrend 或 gate 分类。
- 验收目标：`cargo test evidence_report_history`、`pnpm typecheck`、`git diff --check`、`scripts/live_truth_guard.ps1` 和 `scripts/m4_acceptance_gate.ps1` 通过后提交。
- 边界：category 是本地排障标签，不代表 provider、远程代理/TLS、跨机器 SessionBundle、AdsPower refresh 或完整 `450` coverage 已完成。

## 2026-06-02 M13 Richer Report Diff

- 继续 M13 evidence operations，本轮把本地 evidence history 从趋势/风险/分类扩展到结构化 diff，不提高 Overall。
- 桌面：`DesktopEvidenceReportSummary` 新增 `previousFailureReasonCategory`、`failureReasonCategoryTrend`、`reportDiffSummary` 和 `reportDiffItems`，同 kind report 会生成 status、failureReason、failureReasonCategory、risk 的 previous/current/trend。
- 前端：Dashboard evidence rows 追加 compact diff 展示，并把完整 diff summary 放在 hover title；原始 `status`、`evidenceLevel`、`failureReason`、gate classification 和 expected blocker 语义不变。
- 回归边界：新增测试锁住 `failureReasonCategory` 仍只由 kind/status/failureReason 派生，即使 release report 中 `budgetStatus=over_budget`，也不能靠 full JSON value 把非 target-exceeded reason 分类成 budget overrun。
- 验收目标：`cargo test evidence_report_history --lib -- --test-threads=1`、`pnpm typecheck`、`git diff --check`、`scripts/live_truth_guard.ps1` 和 `scripts/m4_acceptance_gate.ps1` 通过后提交。
- 边界：diff 是本地排障视图，不代表 provider、远程代理/TLS、跨机器 SessionBundle、AdsPower refresh、M6 same-run comparison 或完整 `450` observed/replay coverage 已完成。

## 2026-06-02 M10 Headed Repeatability Gate

- 继续 M10 runtime realism，本轮只推进 headed_external validation_probe repeatability，不提高 Overall。
- 真实证据：`scripts/headed_external_smoke.ps1 -Action validation_probe -TimeoutSeconds 45 -RepeatValidationProbeCount 2 -RunRustTests` 生成 `data/reports/headed-external-smoke/headed-external-smoke-1780375482756.json`，状态 `passed_real_binary_repeatability`；repeatability 为 `passed_repeatability_partial_coherence`，2/2 attempts passed，9 signals，categories `audio/canvas/detector/fingerprint/leak/timezone/webgl/webrtc`。
- Gate：新增 `scripts/m10_headed_repeatability_gate.ps1`，读取最新 ranked headed_external report 并生成 `data/reports/m10-headed-repeatability/m10-headed-repeatability-gate-1780377051356.json`，状态 `passed_repeatability_partial_coherence`。
- 桌面：`list_desktop_evidence_reports` 现在识别 `m10_headed_repeatability`，Dashboard 验收证据卡新增 `M10 Repeatability` 行，展示 attempts/signal/category/next action。
- 上层 gate：`scripts/runtime_adapter_evidence_gate.ps1` 生成 `data/reports/runtime-adapter/runtime-adapter-evidence-gate-1780375565693.json`，仍为 `blocked_evidence_required`，但已记录 `partial_real_binary_repeatability_recorded` / `partial_headed_profile_browser_repeatability_observed`。
- 边界：本轮只证明 multi-run profile-browser validation_probe signal/category/status shape stability；不代表长任务稳定性、远程代理/TLS、provider、跨机器 SessionBundle、AdsPower refresh、完整 headed runtime 或完整 `450` observed/replay coverage 已完成。

## 2026-06-02 M15 Browser Pool Lifecycle Harness

- 继续 M15 browser pool，本轮只推进本地 in-memory lifecycle harness，不提高 Overall。
- 后端：`backend/internal/pool` 新增 slot usage/resource budget、cleanup proof、cleanup report、release cleanup、expired lease cleanup、stale idle reclaim、failed slot reclaim 和 prewarm budget step。
- Gate：新增 `scripts/m15_browser_pool_gate.ps1`，检查 Go pool tests、cleanup proof/source contract、resource budget、usage accounting、prewarm budget step 和 cleanup tests，生成 `data/reports/m15-browser-pool/*`。
- 桌面：evidence history 新增 `m15_browser_pool` kind、summary、failure category、evidence level 和 next action；Dashboard 验收证据卡新增 `M15 Pool` row。
- 验收目标：`go test ./backend/internal/pool -count=1`、`scripts/m15_browser_pool_gate.ps1`、`cargo test evidence_report_history --lib -- --test-threads=1`、`pnpm typecheck`、`live_truth_guard` 和 M4 gate 通过后提交。
- 边界：`passed_pool_lifecycle_harness` 只证明本地 acquire/release/budget/cleanup 合同；真实 browser process prewarm、CDP ready、RSS/process cleanup、proxy/TLS、provider、cross-machine SessionBundle、AdsPower refresh 和完整 `450` coverage 仍待独立证据。

## 2026-06-02 M8 SessionBundle Handoff Gate

- 继续 M8 cross-machine SessionBundle 铺路，本轮只推进 handoff package readiness，不提高 Overall。
- Gate：新增 `scripts/m8_session_handoff_gate.ps1`，会刷新本地 `session_bundle_portability_smoke.ps1`、校验刷新 exit code，并检查跨机 runbook、portability smoke v2、最新本地 portability report、Rust desktop export/preflight/restore 命令与测试、Settings operator surface、typed TS contract 和 M4 boundary guard。
- 桌面：evidence history 新增 `m8_session_handoff` kind、summary、failure category、evidence level 和 next action；Dashboard 验收证据卡新增 `M8 Handoff` row，并把 `passed_handoff_package_ready` 显示为 partial/pending 语义。
- 验收：最新 M8 report `data/reports/m8-session-handoff/m8-session-handoff-gate-1780402941404.json` 为 `passed_handoff_package_ready`；该状态只代表第二机交接材料、UI/API 合同和本地 portability report 就绪。
- 边界：这不是 `cross_machine_passed`；没有第二 Win11 target 的 `crossMachineComplete=true` report 前，不得宣称跨机器 SessionBundle portability 已通过。
- 验证目标：`scripts/m8_session_handoff_gate.ps1`、`cargo test evidence_report_history --lib -- --test-threads=1`、`pnpm typecheck`、`live_truth_guard` 和 M4 gate 通过后提交。

## 2026-06-02 M4 Browser Payload Schema Gate

- 继续 M4.8 typed facade shrink，本轮只推进 browser runtime event payload schema normalize，不提高 Overall。
- 类型/API：`src/modules/browser/types.ts` 新增 `BrowserRuntimeEventPayload`；`src/modules/browser/api.ts` 新增 `normalizeBrowserRuntimeEventPayload(payload: unknown)`，兼容 string、camelCase/snake_case 和常见错误字段，并把 `normalizeLaunchServerInfo` / `normalizeRecordingDetail` 输入从 `any` 收窄到 `unknown`。
- 前端：Browser List/Detail 的 `browser:instance:*` runtime event handlers 不再使用 `payload: any`，改用共享 normalizer；Dashboard 验收证据卡新增 `M4 Payload` row。
- Gate：新增 `scripts/m4_browser_payload_schema_gate.ps1`，desktop evidence history 新增 `m4_browser_payload_schema` kind、summary、failure category、evidence level 和 next action；M4 acceptance gate 升级 v9 并纳入 `browser_payload_schema_contract`。
- 验收：最新 payload report `data/reports/m4-browser-payload-schema/m4-browser-payload-schema-gate-1780404372030.json` 为 `passed_browser_payload_schema_contract`；最新 M4 report `data/reports/m4-acceptance/m4-acceptance-gate-1780404394910.json` 为 `passed_with_expected_external_blockers`，summary `passed=8`、`expectedBlocked=4`、`failed=0`。
- 验证目标：`scripts/m4_browser_payload_schema_gate.ps1`、`cargo test evidence_report_history --lib -- --test-threads=1`、`pnpm typecheck`、`git diff --check`、`live_truth_guard`、M4 gate 和独立 reviewer 通过后提交。
- 边界：这是 browser 模块 source-level schema boundary，不代表 `tauriWailsBridge` 已移除、低频 workbench/core bridge 已统一、真实 headed runtime、provider、远程代理/TLS、跨机器 SessionBundle、AdsPower refresh 或完整 `450` coverage 已完成。

## 2026-06-02 M4 Dashboard Facade Gate

- 继续 M4.8 typed facade shrink，本轮只推进 Dashboard API source-level typed facade，不提高 Overall。
- 前端/API：`src/modules/dashboard/api.ts` 不再动态 import `../../wailsjs/go/main/App`，也不再使用 `const bindings: any`；stats/license/config/CD key 调用改走 `readDashboardStats`、`readLicenseStatus`、`reloadDesktopConfig`、`generateDesktopCdKeys` typed desktop service wrappers。
- Gate：新增 `scripts/m4_dashboard_facade_gate.ps1`，desktop evidence history 新增 `m4_dashboard_facade` kind、summary、failure category、evidence level 和 next action；Dashboard 验收证据卡新增 `M4 Dashboard` row；M4 acceptance gate 升级 v10 并纳入 `dashboard_facade_contract`。
- 验收：最新 Dashboard facade report `data/reports/m4-dashboard-facade/m4-dashboard-facade-gate-1780405656641.json` 为 `passed_dashboard_facade_contract`；最新 M4 report `data/reports/m4-acceptance/m4-acceptance-gate-1780405703275.json` 为 `passed_with_expected_external_blockers`，summary `passed=9`、`expectedBlocked=4`、`failed=0`。
- 验证目标：`scripts/m4_dashboard_facade_gate.ps1`、`cargo test evidence_report_history --lib -- --test-threads=1`、`pnpm typecheck`、`git diff --check`、`live_truth_guard` 和 M4 gate 通过后提交。
- 边界：这是 Dashboard API source-level typed facade boundary，不代表 `tauriWailsBridge` 已移除、profile/settings/logs 动态 binding 已统一、真实 headed runtime、provider、远程代理/TLS、跨机器 SessionBundle、AdsPower refresh 或完整 `450` coverage 已完成。

## 2026-06-02 M4 Settings/Logs Facade Gate

- 继续 M4.8 typed facade shrink，本轮只推进 Settings backup API 和 Browser logs source-level typed facade，不提高 Overall。
- 前端/API：`src/modules/settings/api.ts` 不再动态 import `../../wailsjs/go/main/App`，也不再使用 `const bindings: any`；backup initialize/export/import 改走 `initializeSystemData`、`exportSystemConfig`、`importSystemConfig` typed desktop service wrappers，并保留 destructive preflight、native dialog 和确认流程。`BrowserLogsPage` 的 app logs read/clear 改走 `getAppLogs` / `clearAppLogs` typed wrappers。
- Gate：新增 `scripts/m4_settings_logs_facade_gate.ps1`，desktop evidence history 新增 `m4_settings_logs_facade` kind、summary、failure category、evidence level 和 next action；Dashboard 验收证据卡新增 `M4 Settings/Logs` row；M4 acceptance gate 升级 v11 并纳入 `settings_logs_facade_contract`。
- 验收：最新 Settings/Logs facade report `data/reports/m4-settings-logs-facade/m4-settings-logs-facade-gate-1780407201965.json` 为 `passed_settings_logs_facade_contract`；最新 M4 report `data/reports/m4-acceptance/m4-acceptance-gate-1780407239835.json` 为 `passed_with_expected_external_blockers`，summary `passed=10`、`expectedBlocked=4`、`failed=0`。
- 验证目标：`scripts/m4_settings_logs_facade_gate.ps1`、`cargo test evidence_report_history --lib -- --test-threads=1`、`pnpm typecheck`、`cargo fmt`、`git diff --check`、`live_truth_guard` 和 M4 gate 通过后提交。
- 边界：这是 Settings backup API 和 Browser logs source-level typed facade boundary，不代表 `tauriWailsBridge` 已移除、profile/workbench/core bridge API 已统一、真实 headed runtime、provider、远程代理/TLS、跨机器 SessionBundle、AdsPower refresh 或完整 `450` coverage 已完成。

## 2026-06-02 M4 Profile Facade Gate

- 继续 M4.8 typed facade shrink，本轮只推进 Profile remote author source-level typed facade，不提高 Overall。
- 前端/API：`src/modules/profile/api.ts` 不再动态 import `../../wailsjs/go/main/App`，也不再使用 `const bindings: any` 或 `(window as any).go?.main?.App`；远端作者资料读取改为先走 `fetchRemoteAuthorProfileFromDesktop` typed wrapper，并保留非桌面预览的 browser `fetch` fallback。
- 类型边界：profile payload 归一化从 `Record<string, any>` 收窄到 `Record<string, unknown>`，`normalizeChannel(value: any)` 改为 `normalizeChannel(value: unknown)`，错误信息读取走 `errorMessage(error)` guard。
- Gate：新增 `scripts/m4_profile_facade_gate.ps1`，desktop evidence history 新增 `m4_profile_facade` kind、summary、failure category、evidence level 和 next action；Dashboard 验收证据卡新增 `M4 Profile` row；M4 acceptance gate 升级 v12 并纳入 `profile_facade_contract`。
- 验收：profile facade report `data/reports/m4-profile-facade/m4-profile-facade-gate-1780408998290.json` 为 `passed_profile_facade_contract`；M4 report `data/reports/m4-acceptance/m4-acceptance-gate-1780409033459.json` 为 `passed_with_expected_external_blockers`，summary `passed=11`、`expectedBlocked=4`、`failed=0`。
- 验证目标：`scripts/m4_profile_facade_gate.ps1`、`cargo test evidence_report_history --lib -- --test-threads=1`、`pnpm typecheck`、`cargo fmt`、`git diff --check`、`live_truth_guard` 和 M4 gate 通过后提交。
- 边界：这是 Profile remote author source-level typed facade boundary，不代表 `tauriWailsBridge` 已移除、workbench/core bridge API 已统一、真实 headed runtime、provider、远程代理/TLS、跨机器 SessionBundle、AdsPower refresh 或完整 `450` coverage 已完成。

## 2026-06-02 M4 Behavior Preset Facade Gate

- 继续 M4.8 typed facade shrink，本轮只推进 FingerprintPanel behavior preset source-level facade，不提高 Overall。
- 前端/API：`src/modules/browser/components/FingerprintPanel.tsx` 不再直接 import Wails `BehaviorPresetList`；行为预设加载改走 `src/modules/browser/api.ts` 的 `fetchBehaviorPresets` module facade。
- Gate：新增 `scripts/m4_behavior_preset_facade_gate.ps1`，desktop evidence history 新增 `m4_behavior_preset_facade` kind、summary、failure category、evidence level 和 next action；Dashboard 验收证据卡新增 `M4 Behavior` row；M4 acceptance gate 升级 v13 并纳入 `behavior_preset_facade_contract`。
- 验收：behavior preset facade report `data/reports/m4-behavior-preset-facade/m4-behavior-preset-facade-gate-1780409948440.json` 为 `passed_behavior_preset_facade_contract`；M4 report `data/reports/m4-acceptance/m4-acceptance-gate-1780410001954.json` 为 `passed_with_expected_external_blockers`，summary `passed=12`、`expectedBlocked=4`、`failed=0`。
- 验证：`pnpm typecheck`、`scripts/m4_behavior_preset_facade_gate.ps1`、`cargo test evidence_report_history --lib -- --test-threads=1`、`cargo fmt`、`live_truth_guard` 和 M4 gate 均已通过；收尾继续执行 `git diff --check`。
- 边界：这是 FingerprintPanel behavior preset source-level typed facade boundary，不代表 `tauriWailsBridge` 已移除、browser/workbench/core bridge API 已统一、真实 headed runtime、provider、远程代理/TLS、跨机器 SessionBundle、AdsPower refresh 或完整 `450` coverage 已完成。

## 2026-06-02 M4 Automation Facade Gate

- 继续 M4.8 typed facade shrink，本轮只推进 AutomationPage scheduler/rule source-level facade，不提高 Overall。
- 前端/API：`src/modules/browser/pages/AutomationPage.tsx` 不再直接 import Wails `Scheduler*` / `AutomationRule*` App bindings 或 `backend` model constructors；任务列表/创建/删除/立即运行与响应规则列表/创建/删除/开关/测试触发改走 `src/modules/browser/api.ts` 的 module facade，并使用 plain typed DTO。
- Gate：新增 `scripts/m4_automation_facade_gate.ps1`，desktop evidence history 新增 `m4_automation_facade` kind、summary、failure category、evidence level 和 next action；Dashboard 验收证据卡新增 `M4 Automation` row；M4 acceptance gate 升级 v14 并纳入 `automation_facade_contract`。
- 验收：automation facade report `data/reports/m4-automation-facade/m4-automation-facade-gate-1780411382396.json` 为 `passed_automation_facade_contract`；M4 report `data/reports/m4-acceptance/m4-acceptance-gate-1780411429879.json` 为 `passed_with_expected_external_blockers`，summary `passed=13`、`expectedBlocked=4`、`failed=0`。
- 验证：`pnpm typecheck`、`scripts/m4_automation_facade_gate.ps1`、`cargo test evidence_report_history --lib -- --test-threads=1`、`cargo fmt`、`live_truth_guard` 和 M4 gate 已通过；收尾继续执行 `git diff --check`。
- 边界：这是 AutomationPage scheduler/rule source-level typed facade boundary，不代表 `tauriWailsBridge` 已移除、完整 browser/app shell/monitor/workbench/core bridge API 已统一、真实 headed runtime、provider、远程代理/TLS、跨机器 SessionBundle、AdsPower refresh 或完整 `450` coverage 已完成。

## 2026-06-11 M4 App Shell Facade Gate

- 继续 M4.8 typed facade shrink，本轮只推进 App shell source-level facade，不提高 Overall。
- 前端/API：`src/App.tsx` 的关闭确认、通知订阅、环境读取、托盘隐藏/最小化和退出动作改走 `src/services/desktop.ts` 的 `desktopRuntimeListen`、`desktopEnvironment`、`desktopQuitAppOnly`、`desktopQuitFull`、`desktopQuit`、`desktopWindowHide`、`desktopWindowMinimize`；App shell 不再直接 import Wails App/runtime。
- Gate：新增 `scripts/m4_app_shell_facade_gate.ps1`，desktop evidence history 新增 `m4_app_shell_facade` kind、summary、failure category、evidence level 和 next action；Dashboard 验收证据卡新增 `M4 App Shell` row；M4 acceptance gate 升级 v15 并纳入 `app_shell_facade_contract`。
- 验证目标：`pnpm typecheck`、`scripts/m4_app_shell_facade_gate.ps1`、`cargo test evidence_report_history --lib -- --test-threads=1`、`cargo fmt --check`、`live_truth_guard`、M4 gate 和 `git diff --check` 通过后提交。
- 边界：这是 App shell source-level typed facade boundary，不代表 `tauriWailsBridge` 已移除、完整 monitor/browser/workbench/core bridge API 已统一、真实 headed runtime、provider、远程代理/TLS、跨机器 SessionBundle、AdsPower refresh 或完整 `450` coverage 已完成。

## 2026-06-11 M4 Monitor Facade Gate

- 继续 M4.8 typed facade shrink，本轮只推进 EventMonitor source-level facade，不提高 Overall。
- 前端/API：`src/modules/monitor/EventMonitorPage.tsx` 不再直接 import Wails `EventLogQuery` / `EventLogCount` / `EventLogPrune` / `EventLogExport` 或 `wailsjs/go/models`；实时事件订阅改走 `desktopRuntimeListen`，历史 query/count/export/prune 改走 `queryEventLog`、`countEventLog`、`exportEventLog`、`pruneEventLog`；导出/清理错误读取改为 `unknown` guard。
- 行为修复：实时监控暂停状态改用 `pausedRef`，避免一次性订阅回调长期读取初始 `paused=false`。
- Gate：新增 `scripts/m4_monitor_facade_gate.ps1`，desktop evidence history 新增 `m4_monitor_facade` kind、summary、failure category、evidence level 和 next action；Dashboard 验收证据卡新增 `M4 Monitor` row；M4 acceptance gate 升级 v16 并纳入 `monitor_facade_contract`。
- 验收：monitor facade report `data/reports/m4-monitor-facade/m4-monitor-facade-gate-1781196311382.json` 为 `passed_monitor_facade_contract`；M4 report `data/reports/m4-acceptance/m4-acceptance-gate-1781196340402.json` 为 `passed_with_expected_external_blockers`，summary `passed=15`、`expectedBlocked=4`、`failed=0`。
- 验证目标：`pnpm typecheck`、`scripts/m4_monitor_facade_gate.ps1`、`cargo test evidence_report_history --lib -- --test-threads=1`、`cargo fmt --check`、`live_truth_guard`、M4 gate 和 `git diff --check` 通过后提交。
- 边界：这是 EventMonitor source-level typed facade boundary，不代表 `tauriWailsBridge` 已移除、browser/workbench/core bridge API 已统一、真实 headed runtime、provider、远程代理/TLS、跨机器 SessionBundle、AdsPower refresh 或完整 `450` coverage 已完成。

## 2026-06-12 M4 Browser Runtime Facade Gate

- 继续 M4.8 typed facade shrink，本轮只推进 Browser List/Detail runtime subscription source-level facade，不提高 Overall。
- 类型/API：`src/modules/browser/types.ts` 新增 `BrowserInstanceRuntimeEventName` / `BrowserInstanceRuntimeEvent`；`src/modules/browser/api.ts` 改用 `desktopRuntimeListen` 并新增 `onBrowserInstanceRuntimeEvents`，对 `browser:instance:started/updated/stopped/crashed` 统一归一化 payload。
- 前端：`BrowserListPage` / `BrowserDetailPage` 不再直接 import/call Wails `EventsOn`，改从 browser module API facade 接收归一化 lifecycle event；旧 browser payload schema gate 已兼容“normalizer 在 API facade 内调用、页面用 typed facade”的结构。
- Gate：新增 `scripts/m4_browser_runtime_facade_gate.ps1`，desktop evidence history 新增 `m4_browser_runtime_facade` kind、summary、failure category、evidence level 和 next action；Dashboard 验收证据卡新增 `M4 Browser Runtime` row；M4 acceptance gate 升级 v17 并纳入 `browser_runtime_facade_contract`。
- 验收：browser runtime facade report 为 `passed_browser_runtime_facade_contract`；M4 gate v17 复跑为 `passed_with_expected_external_blockers`，summary `passed=16`、`expectedBlocked=4`、`failed=0`。
- 验证目标：`pnpm typecheck`、`scripts/m4_browser_runtime_facade_gate.ps1`、`cargo test evidence_report_history --lib -- --test-threads=1`、`cargo fmt --check`、`live_truth_guard`、M4 gate 和 `git diff --check` 通过后提交。
- 边界：这是 Browser List/Detail runtime subscription source-level typed facade boundary，不代表 `tauriWailsBridge` 已移除、settings/core/proxy/workbench bridge API 已统一、真实 headed runtime、provider、远程代理/TLS、跨机器 SessionBundle、AdsPower refresh 或完整 `450` coverage 已完成。

## 2026-06-12 M4 Workbench DTO Facade Gate

- 继续 M4.8 typed facade shrink，本轮只推进 Workbench DTO source-level boundary，不提高 Overall。
- 类型/API：`src/types/desktop.ts` 新增 `DesktopCoreWorkbenchDetectionResult`、`DesktopCoreWorkbenchUiState`、`DesktopCoreWorkbenchDetectorSite`；`src/services/desktop.ts` 的 Workbench detection results / detector sites / UI state / detector run API 不再返回 `unknown[]` 或裸 `unknown`。
- 前端：`src/modules/synchronizer/api.ts` 移除对应 `Array.isArray(results)` fallback，仍保留 normalizer 处理字段兼容。
- Gate：`scripts/m4_acceptance_gate.ps1` 升级 v18，在 `typed_facade_shrink_contract` 中检查 Workbench DTO markers 和 `unknown[]` 回归 marker。
- 验收：M4 gate v18 复跑为 `passed_with_expected_external_blockers`，summary `passed=16`、`expectedBlocked=4`、`failed=0`。
- 验证目标：`pnpm typecheck`、`live_truth_guard`、M4 gate、`git diff --check` 和独立 reviewer 通过后提交。
- 边界：这是 Workbench source-level DTO boundary，不代表 `tauriWailsBridge` 已移除、settings/core/proxy bridge API 已统一、真实 headed runtime、provider、远程代理/TLS、跨机器 SessionBundle、AdsPower refresh 或完整 `450` coverage 已完成。

## 2026-06-12 M4 Runtime Facade Gate

- 继续 M4.8 typed facade shrink，本轮只推进页面级 runtime event / external URL source-level facade，不提高 Overall。
- 前端：`SettingsPage` backup export/import progress、`CoreManagementPage` download progress 与 Releases 外链、`ProxyPickerModal` / `ProxyPoolPage` proxy speed/IP health runtime result 事件、`LaunchApiDocsPage` / `UsageTutorialPage` 外链打开已改走 `src/services/desktop.ts` 的 `desktopRuntimeListen` / `desktopOpenExternalUrl`，这些页面不再直接 import `wailsjs/runtime/runtime`。
- Gate：新增 `scripts/m4_runtime_facade_gate.ps1`，desktop evidence history 新增 `m4_runtime_facade` kind、summary、failure category、evidence level 和 next action；Dashboard 验收证据卡新增 `M4 Runtime` row；M4 acceptance gate 升级 v19 并纳入 `runtime_facade_contract`。
- 验收：runtime facade report `data/reports/m4-runtime-facade/m4-runtime-facade-gate-1781202272359.json` 为 `passed_runtime_facade_contract`；M4 gate v19 `data/reports/m4-acceptance/m4-acceptance-gate-1781202633600.json` 为 `passed_with_expected_external_blockers`，summary `passed=17`、`expectedBlocked=4`、`failed=0`。
- 验证：`pnpm typecheck`、`scripts/m4_runtime_facade_gate.ps1`、`cargo test evidence_report_history --lib -- --test-threads=1`、`scripts/m4_acceptance_gate.ps1` 已通过；收尾继续执行 `cargo fmt --check`、`live_truth_guard`、`git diff --check` 和独立 reviewer。
- 边界：这是选定页面级 runtime facade boundary，不代表 `tauriWailsBridge` 已移除、所有 settings/core/proxy bridge API 已统一、真实 headed runtime、provider、远程代理/TLS、跨机器 SessionBundle、AdsPower refresh 或完整 `450` coverage 已完成。

## 2026-06-12 M4 Workbench Report DTO Facade Gate

- 继续 M4.8 typed facade shrink，本轮只推进 Workbench report/status source-level DTO boundary，不提高 Overall。
- 类型/API：`src/types/desktop.ts` 新增 `DesktopBrowserProfile`、`DesktopCoreWorkbenchFingerprintSnapshot`、`DesktopCoreWorkbenchFingerprintHealthProfile`、`DesktopCoreWorkbenchIdentityStrengthReport`；`src/services/desktop.ts` 的 `BrowserInstanceStatus`、`WorkbenchFingerprintHealthProfile`、`WorkbenchFingerprintProfile`、`IdentityReportProfile` 不再返回裸 `unknown`。
- 前端：`src/modules/synchronizer/api.ts` 移除 `BrowserInstanceStatus` 和 `WorkbenchFingerprintProfile` 调用侧 cast，仍保留 normalizer 处理历史 payload 兼容。
- Gate：`scripts/m4_acceptance_gate.ps1` 升级 v20，在 `typed_facade_shrink_contract` 中检查 Workbench/report DTO markers，并阻止这些服务 wrapper 回退到 `Promise<unknown>`。
- 验收：M4 gate v20 完整复跑为 `passed_with_expected_external_blockers`，summary `passed=17`、`expectedBlocked=4`、`failed=0`；expected blockers 仍是 runtime adapter B1-B5、provider credentials/real smoke、跨机器 SessionBundle 和 profile/browser comparison。
- 验证目标：`pnpm typecheck`、M4 gate、`cargo test evidence_report_history --lib -- --test-threads=1`、`cargo fmt --check`、`live_truth_guard`、`git diff --check` 和独立 reviewer 通过后提交。
- 边界：这是 source-level DTO boundary，不代表 `tauriWailsBridge` 已移除、settings/core/proxy bridge API 已统一、真实 headed runtime、provider、远程代理/TLS、跨机器 SessionBundle、AdsPower refresh 或完整 `450` coverage 已完成。

## 2026-06-12 M4 UI Error Boundary Gate

- 继续 M4.8 source boundary shrink，本轮只推进选定 UI surface 的错误处理边界，不提高 Overall。
- 共享 helper：新增 `src/shared/errors.ts`，`messageFromUnknownError(error: unknown, fallback)` 只从 `Error`、非空 string 或 string `message` 读取文案；普通 object/function 无有效 message 时回到 fallback，避免 `[object Object]` 进入 toast。
- 前端：Dashboard WebView evidence probes、Dashboard CD key API、Settings provider/session/import/export 操作、Automation task/rule 操作、NaturalLanguageTask、TagManagement、RecordingDetailModal 均从 `catch (...: any)` 收窄到 `catch (error: unknown)`；Dashboard WebView audio fallback 改成 `WebkitAudioWindow` typed guard；Launch API docs markdown code block 改用 `isValidElement<MarkdownCodeElementProps>`，不再 `children as any`。
- Gate：`scripts/m4_acceptance_gate.ps1` 升级为 v21，新增 `ui_error_boundary_contract`，用 regex 检查选定文件里的 weak catch/cast 回归；最新 M4 gate 为 `passed_with_expected_external_blockers`，summary `passed=18`、`expectedBlocked=4`、`failed=0`。
- 验证：`pnpm typecheck` passed；`scripts/m4_acceptance_gate.ps1` passed with expected external blockers；独立 verifier PASS；code reviewer 初次指出 object fallback 与 gate 脆弱性，已修复后复跑通过。
- 边界：这是选定 UI surface source-level error boundary，不代表全仓 legacy `any` catch 已清零、`tauriWailsBridge` 已移除、真实 provider/远程代理/TLS/跨机器 SessionBundle/AdsPower refresh 或完整 `450` coverage 已完成。

## 2026-06-12 M4 Browser Instance UI Error Boundary Gate

- 继续 M4.8 source boundary shrink，本轮只推进浏览器实例操作面的错误处理边界，不提高 Overall。
- 前端：`BrowserListPage`、`BrowserDetailPage`、`BrowserEditPage`、`BrowserSettingsModal`、`QuickLaunchModal` 的实例启动/停止/重启、录制、复制、配置保存、内核保存、打开用户数据目录和按 Code 启动错误路径从 `catch (...: any)` 收窄到 `catch (error: unknown)`。
- 语义保持：启动/重启/QuickLaunch 继续走 `resolveActionFeedback`，停止继续走 `resolveActionErrorMessage`，普通 toast/setError 走 `messageFromUnknownError`；不改变背景连接 warning 和 fallback 文案策略。
- Gate：`scripts/m4_acceptance_gate.ps1` 升级为 v22，把这些 Browser instance surfaces 纳入 `ui_error_boundary_contract`，继续用 regex 检查 weak catch/cast 回归；action-specific / mojibake marker 在脚本内用 ASCII-safe `[char]0x...` 构造，避免 Windows PowerShell 解析 UTF-8 mojibake 字面量失败；最新 M4 gate 为 `passed_with_expected_external_blockers`，summary `passed=18`、`expectedBlocked=4`、`failed=0`。
- 验证：`pnpm typecheck`、`cargo test evidence_report_history --lib -- --test-threads=1`、`cargo fmt --check`、`scripts/live_truth_guard.ps1`、`scripts/m4_acceptance_gate.ps1` 和 `git diff --check` passed；局部 `rg` 未发现本批文件残留 `catch (...: any)` / `error?.message` / `as any`；M4 gate 脚本内非 ASCII source literal 检查无匹配。
- 边界：这是 Browser instance selected UI source-level error boundary，不代表 ProxyPoolPage、CoreManagementPage 或全仓 legacy `any` catch 已清零，也不代表外部 provider、远程代理/TLS、跨机器 SessionBundle、AdsPower refresh 或完整 `450` coverage 已完成。

## 2026-06-12 M4 Core Management UI Error Boundary Gate

- 继续 M4.8 source boundary shrink，本轮只推进 CoreManagementPage 的错误处理边界，不提高 Overall。
- 前端：`CoreManagementPage` 的打开内核路径、扫描内核、保存/删除内核、设为默认、启动下载和全局设置保存错误路径从 `catch (...: any)` 收窄到 `catch (error: unknown)`，toast 文案走 `messageFromUnknownError`。
- Gate：`scripts/m4_acceptance_gate.ps1` 升级为 v23，把 Core management 纳入 `ui_error_boundary_contract`；最新 M4 gate 为 `passed_with_expected_external_blockers`，summary `passed=18`、`expectedBlocked=4`、`failed=0`。
- 验证：`pnpm typecheck`、`cargo test evidence_report_history --lib -- --test-threads=1`、`cargo fmt --check`、`scripts/live_truth_guard.ps1`、`scripts/m4_acceptance_gate.ps1` 和 `git diff --check` passed；局部 `rg` 未发现 CoreManagementPage 残留 `catch (...: any)` / `error?.message` / `as any`；M4 gate 脚本内非 ASCII source literal 检查无匹配。
- 边界：这是 Core management selected UI source-level error boundary，不代表 ProxyPoolPage 或全仓 legacy `any` catch 已清零，也不代表外部 provider、远程代理/TLS、跨机器 SessionBundle、AdsPower refresh 或完整 `450` coverage 已完成。

## 2026-06-12 M4 Proxy Pool UI Error Boundary Gate

- 继续 M4.8 source boundary shrink，本轮只推进 ProxyPoolPage 的错误处理边界，不提高 Overall。
- 前端：`ProxyPoolPage` 的订阅刷新、批量/单项删除、代理保存、URL 获取 fallback、订阅导入、解析、导入确认和代理名称修复错误路径从 `catch (...: any)` 收窄到显式 `unknown` guard，toast 文案走 `messageFromUnknownError` 并保留原 fallback 优先级。
- 类型：本地 `ClashProxy` 的 `[key: string]: any` 收窄为 `[key: string]: unknown`。
- Gate：`scripts/m4_acceptance_gate.ps1` 升级为 v24，把 Proxy pool 纳入 `ui_error_boundary_contract`，并检查 selected files 的 `[key: string]: any` 回归；最新 M4 gate 为 `passed_with_expected_external_blockers`，summary `passed=18`、`expectedBlocked=4`、`failed=0`。
- 验证：`pnpm typecheck` passed；`scripts/m4_acceptance_gate.ps1` passed with expected external blockers；局部 `rg --pcre2` 未发现 ProxyPoolPage 残留 weak catch / optional message / any cast / `[key: string]: any`；M4 gate 脚本内非 ASCII source literal 检查无匹配。
- 边界：这是 Proxy pool selected UI/source error boundary，不代表全仓 generic `Record<string, any>` 或 shared Table 类型债已清零，也不代表外部 provider、远程代理/TLS、跨机器 SessionBundle、AdsPower refresh 或完整 `450` coverage 已完成。

## 2026-06-12 M4 Generic Any Residue Gate

- 继续 M4.8 type-boundary shrink，本轮只推进选定 generic/index `any` 残留，不提高 Overall。
- 前端共享：`Table` 的泛型约束从 `Record<string, any>` 收窄为 `object`，动态 row/cell 读取改为局部 `Record<string, unknown>` cast。
- 类型：`ProxyIPHealthResult.rawData` 改为 `Record<string, unknown>`；`ProxyPoolPage/types.ts` 的 ClashProxy index signature 改为 `unknown`。
- Gate：`scripts/m4_acceptance_gate.ps1` 升级为 v25，新增 `generic_any_residue_contract`；最新 M4 gate 为 `passed_with_expected_external_blockers`，summary `passed=19`、`expectedBlocked=4`、`failed=0`。
- 验证：`pnpm typecheck` passed；`scripts/m4_acceptance_gate.ps1` passed with expected external blockers；`src/modules src/services src/shared` 范围内 `rg` 未发现 `Record<string, any>` 或 `[key: string]: any` 残留。
- 边界：这是 selected generic any residue source contract，不代表仓库每一个显式 `any` 已移除，也不代表外部 provider、远程代理/TLS、跨机器 SessionBundle、AdsPower refresh 或完整 `450` coverage 已完成。

## 2026-06-12 M4 RecordingPanel UI Error Boundary Gate

- 继续 M4.8 source boundary shrink，本轮只推进 RecordingPanel 的错误处理边界，不提高 Overall。
- 前端：`RecordingPanel` 的录制、停止、养号、删除、导出、复制、导入、重命名、回放、暂停处理、接管浏览器和清理会话错误路径从未显式类型的 `catch (e)` 收窄到 `catch (error: unknown)`，错误文案复用 `messageFromUnknownError`。
- 文案：修正回放失败 toast 的既有 mojibake 文案。
- Gate：`scripts/m4_acceptance_gate.ps1` 升级为 v26，把 RecordingPanel 纳入 `ui_error_boundary_contract`；最新 M4 gate 为 `passed_with_expected_external_blockers`，summary `passed=19`、`expectedBlocked=4`、`failed=0`。
- 验证：`pnpm typecheck` passed；`scripts/m4_acceptance_gate.ps1` passed with expected external blockers；局部 `rg --pcre2` 未发现 RecordingPanel 残留 weak catch / any cast / generic any / mojibake marker。
- 边界：这是 RecordingPanel selected UI source-level error boundary，不代表全仓 legacy catch 已清零，也不代表 `tauriWailsBridge` 移除、外部 provider、远程代理/TLS、跨机器 SessionBundle、AdsPower refresh 或完整 `450` coverage 已完成。

## 2026-06-12 M4 Bridge Compatibility Type Gate

- 继续 M4.8 typed facade shrink，本轮只推进桌面服务 RPC 参数与 Tauri/Wails 兼容桥类型边界，不提高 Overall。
- 前端服务：`src/services/desktop.ts` 新增 `DesktopRpcArg` / `DesktopRpcArgs`，`buildRpcArgs` 与 `desktopRpc` 参数通道不再直接暴露裸 `unknown[]`。
- 兼容桥：`src/services/tauriWailsBridge.ts` 新增 `BridgeEventData`、`BridgeRpcResult`、`BridgeRpcMethod`、`BridgeAppProxy`，runtime event data、Sidecar event payload 与 App proxy 方法签名不再散落裸 `unknown[]` / `Promise<unknown>`。
- Gate：`scripts/m4_acceptance_gate.ps1` 升级为 v27，新增 `bridge_compat_type_contract`；最新 M4 gate 为 `passed_with_expected_external_blockers`，summary `passed=20`、`expectedBlocked=4`、`failed=0`。
- 验证：`pnpm typecheck` passed；`scripts/m4_acceptance_gate.ps1` passed with expected external blockers；局部 `rg` 未发现 `desktop.ts` / `tauriWailsBridge.ts` 残留 `unknown[]`、`Promise<unknown>` 或 raw bridge app proxy type。
- 边界：这是 source-level bridge type boundary，不代表 `tauriWailsBridge` 已移除、所有 browser/settings/core/proxy API 已统一、真实 headed runtime、provider、远程代理/TLS、跨机器 SessionBundle、AdsPower refresh 或完整 `450` coverage 已完成。

## 2026-06-22 Local-only Scope Reset And Session Restore Verification

- 范围：彻底取消 release performance budget green、外部分发 smoke、干净 Win11/第二机验收和跨机器 SessionBundle portability；这些脚本/report 只保留历史诊断或本机诊断价值。
- 脚本：`scripts/session_bundle_portability_smoke.ps1` 升级为 v3 本机 restore smoke，默认运行 Rust 本机 restore contract test；最新 report `data/reports/session-portability/session-bundle-portability-smoke-1782111637335.json` 为 `local_restore_verified`。
- Gate：`scripts/m8_session_handoff_gate.ps1` 升级为 v2 本机 restore gate；最新 report `data/reports/m8-session-handoff/m8-session-handoff-gate-1782111637657.json` 为 `passed_local_restore_verified`。
- M4：`scripts/m4_acceptance_gate.ps1` 已把 SessionBundle 本机 restore 归为 passed；当时 report `data/reports/m4-acceptance/m4-acceptance-gate-1782111648992.json` 为 `passed_with_expected_external_blockers`，summary `passed=23`、`expectedBlocked=2`、`failed=0`；后续同日 v29 report 见下方 M4-M20 Local Evidence Closure。
- 外发/性能：`scripts/external_distribution_smoke.ps1` 最新 report 为 `cancelled_local_only`；M5 release health 仍是本机启动诊断，`passed_with_budget_overrun` 不再要求优化到预算 green。
- 文档：同步 TODO、current state、roadmap、backlog、playbook、remaining-work register 和 M4-M20 board；当前未完成项只保留本机可推进的 runtime/observed/provider-gated 工作，不再包含外发、release budget 或第二机目标。
- 边界：真实 provider credentials/real smoke、remote proxy/TLS、完整 headed realism、真实 browser pool process proof、AdsPower refresh、完整 `450` observed/replay coverage 仍需要独立真实证据。

## 2026-06-22 M4-M20 Local Evidence Closure

- M4 facade：新增 `scripts/m4_browser_settings_core_proxy_facade_gate.ps1`，`src/modules/browser/api.ts` 的 Settings/Core/Proxy sections 改走 `src/services/desktop.ts` typed wrappers，`BrowserNativeBindings` 不再 advertise retired settings/core/proxy Wails methods，`tauriWailsBridge` 改为显式 App RPC allowlist。report `data/reports/m4-browser-settings-core-proxy-facade/m4-browser-settings-core-proxy-facade-gate-1782114285844.json` 为 `passed_browser_settings_core_proxy_facade_contract`；M4 gate v29 `data/reports/m4-acceptance/m4-acceptance-gate-1782114555291.json` 为 `passed_with_expected_external_blockers`，summary `passed=24`、`expectedBlocked=2`、`failed=0`。
- Observed fingerprint：新增 strict coverage gate，只统计 `layer=observed` 且 collector metadata 完整的 signal，排除 taxonomy/materialized contracts。report `data/reports/observed-fingerprint-coverage/observed-fingerprint-coverage-gate-1782114288868.json` 为 `partial_observed_fingerprint_coverage`，`20 / 450`，`partialFamilyCount=10`、`missingFamilyCount=2`。
- Replay runtime：新增 `scripts/live_replay_runtime_gate.ps1` 本地 deterministic replay harness，report `data/reports/live-replay-runtime/live-replay-runtime-gate-1782114289142.json` 为 `passed_local_replay_runtime`，`461 / 450`，`runtimeBacked=461`，`productRuntimeBacked=326`，`contractOnly=135`。
- M10/M15：新增 `scripts/m10_headed_stability_gate.ps1`，report `data/reports/m10-headed-stability/m10-headed-stability-gate-1782114360122.json` 为 `passed_long_task_stability_coherence`，3/3 attempts passed，coherence/stability score 均为 1；新增 `scripts/m15_browser_process_gate.ps1`，report `data/reports/m15-browser-process/m15-browser-process-gate-1782114415481.json` 为 `passed_real_browser_process_prewarm_cleanup`，`processCount=6`、`totalWorkingSetMb=342.5`、`cleanupComplete=true`。
- Evidence UI/history：`src/desktop/mod.rs` 已识别 `m4_browser_settings_core_proxy_facade`、`observed_fingerprint_coverage`、`live_replay_runtime`、`m10_headed_stability`、`m15_browser_process` reports；Dashboard 验收证据列表新增 `M4 Browser Facade`、`Observed Fingerprint`、`Replay Runtime`、`M10 Stability`、`M15 Process` rows。
- 验证：`m4_browser_settings_core_proxy_facade_gate`、`observed_fingerprint_coverage_gate`、`live_replay_runtime_gate`、`m10_headed_stability_gate`、`m15_browser_process_gate`、`m4_acceptance_gate`、`m10_headed_repeatability_gate`、`m15_browser_pool_gate`、`pnpm typecheck`、`go test ./backend/internal/pool -count=1`、`cargo fmt --check`、`cargo test evidence_report_history --lib -- --test-threads=1`、`git diff --check`、`python scripts/check_stage_entry_consistency.py` 均已通过。
- 边界：Observed 仍是 partial `20 / 450`；Replay 是本地 deterministic harness，不是 target-site/browser/provider production replay；M10 是本机 headed_external validation_probe stability/coherence；M15 是真实本机 browser process proof，不是完整 pool integration；`tauriWailsBridge` 仍存在且只被显式化/缩小。

## 2026-06-22 Local Self-Use Full Closure

- Fingerprint：新增 `scripts/full_observed_fingerprint_probe.mjs`，observed gate 默认刷新本机 Chrome/Edge CDP 采集；strict observed coverage 已到 `passed_full_observed_fingerprint_coverage`，`450 / 450`。
- Replay：behavior taxonomy 的 manual/debug/provider-assist 本地 runtime backing 已补齐；local replay gate 已到 `passed_full_local_replay_runtime`，`461 / 450`，`contractOnly=0`。
- Runtime：M15 pool 已接 process attach/release cleanup proof，并消费真实 browser process CDP/RSS/cleanup report；M15 pool gate 状态为 `passed_real_pool_process_integration`。
- Transport/runtime adapter：remote proxy 默认不再是本机阻塞；本机 direct TLS/transport report 可被 runtime adapter gate 消费；runtime adapter 本机自用状态为 `passed_local_self_use`，AdsPower refresh 为 `not_applicable_local_only`。
- 文档：README、current state、roadmap、backlog、playbook、M4-M20 board、remaining-work register、final breakdown 和 live truth guard 已改成本机自用闭环口径。
- 当前唯一未验：CAPTCHA/SMS/Email 服务商真实账号凭证 smoke。没有真实账号、余额和 API key 时继续保持 `blocked_missing_credentials`。

## 2026-06-22 Final Local Gate Rerun

- 修复：`full_observed_fingerprint_probe.mjs` 内部模板字符串语法问题已修；`observed_fingerprint_coverage_gate.ps1` 优先使用 signal 自带 `familyId`，避免 full probe 信号被错归类；`remote_proxy_tls_probe.ps1` 在 Windows curl 本机直连观测中加入 `--ssl-no-revoke`，解决 Schannel 证书吊销检查导致的 TLS 观测失败；`live_truth_guard.ps1` 明确允许 CAPTCHA/SMS/Email 凭证例外。
- 最新本机证据：observed gate `data/reports/observed-fingerprint-coverage/observed-fingerprint-coverage-gate-1782118951030.json` 为 `passed_full_observed_fingerprint_coverage`；replay `data/reports/live-replay-runtime/live-replay-runtime-gate-1782118798569.json` 为 `passed_full_local_replay_runtime`；M10 `data/reports/m10-headed-stability/m10-headed-stability-gate-1782118850618.json` 为 `passed_long_task_stability_coherence`；M15 process `data/reports/m15-browser-process/m15-browser-process-gate-1782118796786.json` 为 `passed_real_browser_process_prewarm_cleanup`；M15 pool `data/reports/m15-browser-pool/m15-browser-pool-gate-1782118967687.json` 为 `passed_real_pool_process_integration`；本机 TLS `data/reports/remote-proxy-tls/remote-proxy-tls-probe-1782118943933.json` 为 `passed_local_direct_tls_observed`；runtime adapter `data/reports/runtime-adapter/runtime-adapter-evidence-gate-1782118975730.json` 为 `passed_local_self_use`。
- 总闸：`data/reports/m4-acceptance/m4-acceptance-gate-1782119044964.json` 为 `passed_with_expected_external_blockers`，`passed=25`、`expectedBlocked=1`、`failed=0`；唯一 expected blocker 是 provider acceptance preflight 的 `blocked_missing_credentials`。
- 验证：`node scripts\full_observed_fingerprint_probe.mjs`、`observed_fingerprint_coverage_gate.ps1`、`live_replay_runtime_gate.ps1`、`m10_headed_stability_gate.ps1`、`m15_browser_process_gate.ps1`、`m15_browser_pool_gate.ps1`、`remote_proxy_tls_probe.ps1`、`runtime_adapter_evidence_gate.ps1`、`live_truth_guard.ps1`、`m4_browser_settings_core_proxy_facade_gate.ps1`、`m4_acceptance_gate.ps1`、`go test ./backend/internal/pool -count=1`、`pnpm typecheck`、`cargo fmt --check`、`cargo test evidence_report_history --lib -- --test-threads=1`、`cargo test release_smoke_contract --lib -- --test-threads=1`、`python scripts\check_stage_entry_consistency.py`、`git diff --check` 均通过；`git diff --check` 仅有 Windows 换行提示。
- 边界：不宣称 CAPTCHA/SMS/Email 真实账号凭证 smoke 已完成；不恢复 release performance、外部分发、第二机/跨机器、AdsPower 或远程代理账号为当前目标。

## 2026-06-27 Proxy Pool Add/Delete Repair

- 根因：代理删除原来走前端整表保存；当页面状态只剩 2 条内置代理时，后端安全护栏会拒绝用 2 条覆盖当前 69/70 条真实代理，报“可能是前端数据异常”。护栏本身正确，错在删除入口。
- 后端：新增 `BrowserProxyDelete(proxyId)` 单条删除 RPC，空 ID 和 `__direct__` / `__local__` 内置代理会被拒绝；sidecar RPC allowlist 已放行该方法。
- 前端：代理池单删/批量删改走 `deleteBrowserProxy`，删除后重新从后端拉真实列表；桌面环境里如果删除 RPC 未就绪或失败，会直接报错，不再落到 mock 删除假装成功；直接导入支持 `host:port:user:pass` 四段式，只填地址框也可解析；导入确认前会重新拉当前代理池，避免用短暂 fallback 的 2 条内置代理作为保存基底。
- 验证：`go test ./backend -run TestBrowserProxyDelete -count=1`、`go test ./backend/internal/browser -count=1`、`go test ./backend/cmd/personal-pilot-core ./backend -count=1`、`node_modules\.bin\tsc.cmd --noEmit` passed；`pnpm typecheck` 被 pnpm build-script approval 阻断，直接 `tsc --noEmit` 已通过；sidecar RPC smoke 使用临时代理执行 `BrowserProxyList -> SaveBrowserProxies -> BrowserProxyDelete -> BrowserProxyList`，新增后 `70 -> 71`，删除后回到 `70`，临时 ID 残留为 `0`。
- 产物：`scripts/build-sidecar.ps1` 已重建 `bin/personal-pilot-core.exe` 和 `bin/personal-pilot-core-x86_64-pc-windows-msvc.exe`。用户给出的测试代理只用于本机 smoke，日志不记录明文凭证。

## 2026-06-27 Proxy Direct Import Format Expansion

- 前端：直接导入解析逻辑抽成 `src/modules/browser/directProxyImport.ts`，支持 `服务器:端口:账号:密码`、`服务器:端口@账号:密码`、`账号:密码:服务器:端口`、`账号:密码@服务器:端口`，并继续支持标准 URL 和无账号 `服务器:端口`；当四段冒号格式里密码是纯数字时，会优先按更像主机/IP 的字段判断服务器，避免把数字密码误当端口。
- UI：直接导入地址框改为“代理地址或代理行”，端口字段仅在分栏填写时必填；单行格式只填地址框即可解析。
- 验证：新增 `scripts/check_direct_proxy_import_formats.mjs`，已验证四种截图格式、数字密码、标准 URL 和无账号格式都会统一转为标准代理 URL；`node_modules\.bin\tsc.cmd --noEmit`、`node scripts\check_direct_proxy_import_formats.mjs`、`node_modules\.bin\vite.cmd build`、`go test ./backend -run TestBrowserProxyDelete -count=1` passed。

## 2026-06-28 Proxy Auth Boundary Review

- 重新核对代理链路后确认：`BrowserProxyCheckIPHealth` / `BrowserProxyTestSpeed` 走 Go HTTP client，`http://user:pass@...` 和 `socks5://user:pass@...` 这类认证代理可以被正确测试；但浏览器启动链路仍是把标准代理直接拼进 `--proxy-server=...`，没有浏览器侧认证桥、扩展注入或 native auth helper。
- 结论：现在“检测能通、启动不能通”的根因不在代理格式，而在认证消费层缺失。AdsPower 这类工具之所以能用同样的代理，通常是因为它在浏览器外部补了认证层。
- 文档：已把 `docs/29-proxy-supply-chain.md`、`docs/11-api-roadmap.md`、`docs/02-current-state.md`、`docs/04-improvement-backlog.md` 的口径改成一致，不再把手动代理接口壳写成完整 CRUD。

## 2026-06-28 Proxy Auth Bridge Closure

- 后端：浏览器启动链路已补上标准认证代理桥接，带账号密码的 `http://` / `https://` / `socks5://` 会先转成本机 `sing-box` 无认证入口，再交给 Chrome；无认证标准代理仍按直连代理处理。
- 后端：`backend/internal/launchcode/manual_proxy_api.go` 已从接口壳变成真实 CRUD，手动添加、列表、更新、删除、测试都会落到 `browser_proxies`，并用 `SourceID=manual` 标记来源。
- 解析：`backend/internal/proxy/singbox_parser.go` 已新增标准认证代理 outbound 生成，HTTP / HTTPS / SOCKS5 都能产出 sing-box 可检查的配置。
- 测试：`go test ./backend/internal/proxy -count=1`、`go test ./backend/internal/launchcode -count=1`、`go test ./backend/internal/... -count=1`、`go test ./backend/... -count=1`、`node_modules\\.bin\\tsc.cmd --noEmit`、`node scripts\\check_direct_proxy_import_formats.mjs`、`bin\\sing-box.exe check` 均通过。
- 复核：用用户给出的两组真实代理做少量本机对照时，`curl` 仍未拿到可用出口结果，所以只能确认“桥接链路已实现”，不能把“上游真实代理本身可用”写成已验证。

## 2026-06-28 UDEAL via panda Proxy Verification

- 真实复核：UDEAL 节点本机 TCP 端口可达，但本机直连 HTTP 返回 `Proxy CONNECT aborted` / `Empty reply`，SOCKS 返回 `connection to proxy closed`；这说明不是端口不通，而是代理端拒绝当前本机出口或需要特定放行出口。
- `panda` 复核：从 `panda` VPS 出口访问同一 UDEAL 节点，HTTP 与 SOCKS 均成功，出口 IP 为 UDEAL 节点本身，定位美国洛杉矶；`ip-api` 返回 `proxy=false`、`hosting=false`。
- 本机链路 smoke：本机启动 `ssh -L 127.0.0.1:33000:<udeal-host>:<udeal-port> panda` 后，再由 `sing-box` 把认证 HTTP 上游桥成本机无认证 SOCKS 入口，`curl` 经该入口成功拿到同一出口 IP。测试配置已清理，日志和文档不保存明文账号密码。
- 代码：后端新增 `NormalizeStandardProxyScheme`，统一兼容 `socks://`、`socks5h://`、`socket://` 到 `socks5://`；手动代理 API 也允许这些协议别名并落库为 `socks5://`；TCP endpoint 解析修正为剥离 URL userinfo，避免把账号密码当主机。
- 验证：`go test ./backend/internal/proxy -count=1`、`go test ./backend/internal/launchcode -count=1`、`go test ./backend/... -count=1`、`node_modules\\.bin\\tsc.cmd --noEmit`、`node scripts\\check_direct_proxy_import_formats.mjs`、`scripts\\build-sidecar.ps1`、`git diff --check` 均通过；`git diff --check` 仅有 Windows 换行提示。

## 2026-06-28 Automatic VPS Proxy Chain

- 后端：`backend/internal/proxy/ssh_tunnel.go` 和 `singbox.go` 已支持标准代理 URL 元参数 `?pp_via_ssh=panda`；应用会自动启动本机 `ssh -N -L`，再由 `sing-box` 暴露本机无认证 SOCKS 给 Chrome。普通代理不带该参数时不受影响。
- 生命周期：sing-box bridge 复用时会同时检查 SSH local forward 端口；bridge 停止、空闲回收、重复 bridge 清理、`StopAll` 都会清 SSH 子进程；HTTPS 上游代理经本机转发时会保留原上游 host 作为 TLS `server_name`。
- 前端：直接导入完整标准代理 URL 时会保留 query 参数，所以 `http://user:pass@proxy.example.com:30000?pp_via_ssh=panda` 可以直接导入代理池；响应和文档仍只使用脱敏示例。
- 验证：`go test ./backend/internal/proxy -count=1`、`go test ./backend/internal/launchcode -count=1`、`go test ./backend -run "Proxy|BrowserProxy|SaveBrowserProxies" -count=1`、`go test ./backend/... -count=1`、`node_modules\\.bin\\tsc.cmd --noEmit`、`node scripts\\check_direct_proxy_import_formats.mjs` 均通过。
- 边界：这不是改远端 VPS 配置，也不是让浏览器直连 VPS；它只在本机 sidecar 内为带 `pp_via_ssh` 的代理自动建立 `本机 -> VPS -> 上游代理` 链路。

## 2026-06-28 App API Full Smoke / Review

- 覆盖范围：本轮按入口复测 Go/Wails `App` 方法、`personal-pilot-core` sidecar `/rpc`、LaunchServer 本地 HTTP API、Rust control-plane API、Tauri commands、TS `desktop.ts` / `tauriWailsBridge` 桥接层。
- 修复：`src/services/tauriWailsBridge.ts` 移除 5 个不存在或未放行的旧暴露方法：`BehaviorRecordingMetaList`、`BehaviorGetRecordingMeta`、`BehaviorGetRecordingEvents`、`LLMPlanOnly`、`LLMExecuteTask`。录制详情走现有 `BehaviorRecordingSummaryList` / `BehaviorGetRecordingDetail`；LLM 两个旧方法 Go 侧没有实现，继续暴露会误导前端。
- 修复：`POST /api/proxy/parse` 原来只把四段代理行识别为 `unknown`，和前端直接导入能力不同步；现已支持 `服务器:端口:账号:密码`、`服务器:端口@账号:密码`、`账号:密码:服务器:端口`、`账号:密码@服务器:端口`，并兼容 `socks://` / `socks5h://` / `socket://` 到 `socks5://`，响应继续脱敏密码并保留 `?pp_via_ssh=panda` 这类 query。
- 静态核对：sidecar `allowedRPCMethods`、前端 `desktopRpc` 使用点、旧 `tauriWailsBridge` allowlist 已对齐；前端不再暴露 sidecar 未放行方法。
- 真实 sidecar smoke：临时 app-root 启动 `bin/personal-pilot-core.exe`，验证 `/health`、无 token `/rpc` 返回 `401`、带 token 只读 RPC、非法 RPC 拒绝、`/shutdown` 和进程退出清理。
- 真实 LaunchServer smoke：通过 sidecar 返回的 `launchServer.baseUrl` 验证 `/api/health`、`/api/profiles`、`/api/proxy/manual` 创建/列表/更新/删除、`/api/proxy/parse` 四段代理解析、标准 URL query 保留、`/shutdown` 和临时目录清理。
- 自动化验证：`go test ./backend/... -count=1`、LaunchServer API 分组测试、Go `App` 分组测试、`node_modules\\.bin\\tsc.cmd --noEmit`、`node scripts\\check_direct_proxy_import_formats.mjs`、`cargo test --all-targets`、`cargo test --manifest-path src-tauri\\Cargo.toml --all-targets`、`scripts\\build-sidecar.ps1`、`scripts\\m4_browser_settings_core_proxy_facade_gate.ps1` 均通过。
- 边界：没有 CAPTCHA / SMS / Email 真实服务商账号、余额和 API key，所以没有做 credential-backed provider smoke；没有启动 GUI 对每个按钮做人手点击。当前结论只覆盖 API 自动化和本机真实 smoke，不恢复 release performance、外部分发、第二机/跨机器或 AdsPower 目标。

## 2026-06-28 API Full Workflow Smoke / CDP Bounds Fix

- 真实链路：通过 sidecar 启动 LaunchServer，创建临时本地测试页面；API 依次完成代理解析、手动代理创建/列表/更新/测试/删除、profile 创建、指纹参数预设、`autoLaunch` 启动本机 fingerprint Chromium、实例状态读取、workbench 导航、输入、点击、页面抓取、截图、指纹快照、fingerprint health、identity report、localStorage/cookie set/get、tab list/new/close、behavior start/stop、proxy check、launch logs 和 cleanup。
- 结果：smoke 抓取到页面 title 和按钮点击后的结果文本，截图返回 base64，fingerprint health 为 good，identity report 为 strong；临时 profile、临时代理、core 进程、本地测试服务和临时 user data dir 均已清理。
- 修复：`backend/internal/behavior/cdp_executor.go` 的 `parseBoundsResult` 原来按旧的双层 `result.result.value` 解析，和当前 `runtimeEvaluateValueRaw` 返回的真实 CDP payload 不匹配，导致 workbench click/hover 取元素坐标失败；现在直接解析 Runtime.evaluate 的 `value`，支持 `null` 返回 element-not-found，并把小数坐标 round 成现有整数 bounds。
- 测试：`backend/internal/behavior/cdp_executor_test.go` 新增 Runtime.evaluate object、float 坐标、null 三个单测；后续变更 workbench action、CDP executor 或 tab 选择逻辑时必须复跑 API full workflow smoke。
- 暴露问题：`config.yaml` 的 LaunchServer API key 是字面量 `"${LAUNCH_SERVER_API_KEY}"`，代码不会展开环境变量；普通实例启动默认检测页会造成多 tab 目标选择不确定。两项已加入 backlog。

## 2026-06-29 LaunchServer API Auth Env Fix

- 根因：`config.Load` 只 trim `launch_server.auth.api_key`，LaunchServer 鉴权拿到的是 `"${LAUNCH_SERVER_API_KEY}"` 字面量，所以真实 API 调用必须把这个占位符当 key 才能过。
- 修复：环境变量展开放在 `launchcode.normalizeAPIAuthConfig`，只支持完整引用 `${VAR}` / `$VAR` / `%VAR%`；配置加载仍保留占位符，避免后续保存配置时把真实 key 写回 `config.yaml`。
- 验证：新增 `TestAuth_EnvKeyReference_PassesWithResolvedEnvValue` 和 `TestAuth_MissingEnvKeyReference_DisablesAuth`；真实 sidecar smoke 用临时 app-root 启动 `personal-pilot-core.exe`，确认缺 key `401`、字面量占位符 `401`、环境变量真实值 `200`。
- 边界：LaunchServer HTTP API 仍不提供明文写入/读取 API key 的接口；如需改 key，使用环境变量或本机 `config.yaml` 后重载配置。

## 2026-06-29 Wails Models / Synchronizer DTO Alignment

- 修复：`src/wailsjs/go/models.ts` 的 `browser.FingerprintSnapshot`、`FingerprintHealthProfile`、`IdentityStrengthReport` 字段名与真实 JSON key 对齐，收回了 `webGL*`、`networkRTT`、`uaDataPlatformVer` 这类前端生成层命名漂移；同步器 `IdentitySubscores` 补上 `longTermCoherence`，并同步修正 `src/modules/synchronizer/api.ts` / `src/modules/synchronizer/SynchronizerPage.tsx` 的归一化与回退对象。
- 验证：`node_modules\\.bin\\tsc.cmd --noEmit`、`go test ./backend/internal/browser -count=1`、`go test ./backend/internal/launchcode -count=1` passed；`git diff --check` 通过后只剩常见的 Windows 换行提示。
- 边界：这是字段名和类型边界同步，不改变后端采集逻辑或 identity/report 算法本身。

## 2026-06-29 Dual-Track 99+ Phase 0–2 + Proxy Chrome Scheme Fix

- 落地：PGS 硬门槛 H-P1~H-P5、通用 `plans/`、`platform-packs/xhs/`、`platform_99_gate.ps1`、`xhs_live_acceptance.ps1`；规范 `docs/44-dual-track-99plus-spec.md`，交接 `docs/45-stealth-platform-handoff.md`。
- 雷达：`capability_scenario_suite.ps1 -Tier all -GenerateRadar` → 34/34 passed；仅 API/契约层，非 XHS live。
- 修复：`NormalizeProxyServerForBrowser` 禁止向 Chromium 传 `socks5h://`（`ERR_NO_SUPPORTED_PROXIES`）；远程 DNS 靠 sing-box + `--host-resolver-rules`。
- 诚实边界：用户截图证实 XHS explore **live 未通过**；旧 workbench navigate 200 + chrome-error 验收口径作废；鼠标可视化需 humanize；`xhs_phone_login_bootstrap.ps1` 仍含 script 与 H-P5 冲突。

## 2026-06-29 Documentation Sync / Handoff Cleanup

- 新增 `docs/45-stealth-platform-handoff.md` 作为 stealth 交接入口；同步 `02-current-state`、`04-improvement-backlog`、`05-ai-maintenance-playbook`、`44`、`29` §5.3、`README`、`root-entrypoint-map`。
- 根目录过程文档（`summaries/cycle-*`、`task_plan.md`、`findings.md` 等）归档或改为指向 `/docs`；`EXECUTION_LOG.md` 顶部注明 canonical 日志在 `docs/logs/`。

## 2026-06-30 G0/G1 Input Plane + XHS Live 12/12

- 代码：G0 `backend/internal/behavior/inputplane/`（auto/os/cdp 路由）；G1 `EnableMinimalSession`、`/api/workbench/stealth-probe`、`TargetCreepJSTrust99Plus=85`、Camoufox launch arg 过滤。
- 验收：`platform_99_gate.ps1 -Track all` → 29/29 PASS；`xhs_live_acceptance.ps1` → **L-01~L-12 全过**（UDEAL + `pp_via_ssh=panda`；explore HTTPS；CreepJS `creepTrust=100`）。
- 脚本：`Get-OrStartCapabilityHarness` 默认复用 `127.0.0.1:19876`（禁多 core）；HTTP 启停/改 profile；`Stop-LaunchInstanceForce`；**finally 默认 stop 实例**（`-KeepInstance` 保留 CreepJS 页调试）。
- 运维：清理 11 个孤儿 XHS Chrome + 多余 harness core；根因含 UI 改代理导致 proxy hash mismatch、验收 spawn 第二 core。
- CreepJS UI：`FP ID: Computing...` 为页面慢；探针仅 sleep 3s 后 CDP 采样，trust 以 API 为准。
- 文档：同步 `docs/45-stealth-platform-handoff.md`、`docs/46-global-99plus-architecture.md`（G0-4 已通过）、`docs/02-current-state.md`。
