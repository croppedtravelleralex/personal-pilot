# 2026-07 Maintenance Log

## 2026-07-11（续推：未推进项全量）

- **50-A1/A3**：`transport/impersonate`（metacubex/utls + h2）+ `graphClientForProfile` 走 profile 桥接同出口。
- **50-B1**：`trust.HydrateCDP`；`injectTrustCookiesAsync` → `hydrateTrustSurfaceAsync`。
- **52-DP1**：`browser/persona` ≥12 机型；seed 分配；`runtime_materialize` 派生。
- **49-A5 / 51-S3 / 53-L5-3 / 54-CP2**：RGBA 噪声、WebGPU 对齐、惯性 mouseWheel、`MaxConcurrentInstances`+dashboard budget。
- **门禁**：相关 `go test` 包通过；`go build ./backend` 通过。

## 2026-07-11（续推：未推进项全量）

- **50-A1/A3**：`transport/impersonate`（metacubex/utls + h2）+ `graphClientForProfile` 走 profile 桥接同出口。
- **50-B1**：`trust.HydrateCDP`；`injectTrustCookiesAsync` → `hydrateTrustSurfaceAsync`。
- **52-DP1**：`browser/persona` ≥12 机型；seed 分配；`runtime_materialize` 派生。
- **49-A5 / 51-S3 / 53-L5-3 / 54-CP2**：RGBA 噪声、WebGPU 对齐、惯性 mouseWheel、`MaxConcurrentInstances`+dashboard budget。
- **门禁**：相关 `go test` 包通过；`go build ./backend` 通过。

## 2026-07-11（全量推进）

- 用户要求「未推进的全部推进」：本地可做项批量落地。
- **已关/落地**：49 A2/A4（非 blank 跳过当前页 patch + Runtime.disable）、56 F1 无效 flag 剔除、54 CP1 端口预留导出与 app 接线、56 B1 CreepJS 结构化 parser、56 E1 ops/Graph UA 统一、51 S1/S2 chrome+permissions、52 DP2 相干硬门禁（`PERSONAL_PILOT_COHERENCE_MODE`）、56 T1 TrustSurface、56 T2 `platform-packs/xhs/trust.yaml`、49 D1 OS fallback 生产接线、56 P1 provider smoke 骨架脚本、53 L5-2 四段点击间隙。
- **门禁**：`go test` browser/behavior/detection/trust/transport/proxy 相关包通过；`go build` backend 通过。
- **仍未做（诚实）**：uTLS、Graph 同代理出口、完整 CDP hydrate、人格库、S3–S14、CP2–6、docs/55、RPA UI、横评刷分、外部凭证/节点。

## 2026-07-11

- **W1 代码落地**：
  - `docs/50` C1：环境注入改为 `installEnvironment(profile)` 自包含函数；主文档包装 `Worker`/`SharedWorker`（classic + `importScripts`）；覆盖 `WorkerNavigator` 与 `OffscreenCanvas` WebGL；`AppliedFamilies` 含 `worker_scope`。
  - `docs/49` D1：`CDPExecutor.OSClickAtFallback` + `ClickFallback` 审计；CDP press/release 重试失败后可降级 OS click（单测覆盖）。
  - `docs/53` L5-1：`mouseMove` 在 humanization active 时走 `BuildFittsTrajectory`，并按 RTT 降采样。
  - `docs/49` A2 partial：注入后 `Runtime.disable`，降低常驻 Runtime 域暴露。
- 门禁：`go test ./backend/internal/behavior/... ./backend/internal/behavior/humanize/... ./backend/internal/behavior/inputplane/... -count=1` 通过。
- 中途改动后全面复核：确认 W0 代码已落地（`makeNative`、`core_version`/UA materialize、DNS `inconclusive`、鼠标重试+轨迹压缩+去 overlay），但文档证据基线滞后。
- 诚实边界：A4 当前页双层 evaluate 仍在；OS fallback 需上层接线 `OSClickAtFallback` 才在生产生效；横评分未刷。
- 新确认缺口写入 `docs/56`：X15 多处 ops UA `PersonalPilot/1.0` 分裂面；X11 无效 flag 仅 warning 不剔除；X16/X17 L5/D1 残余（本日部分关闭）。
- 文档对齐：更新 `docs/49`/`PLAN.md`/`docs/50`/`docs/56`/`docs/04`/`docs/02`/`docs/README`/`STATUS.md`。

## 2026-07-10

- 按“外部条件阻塞先不做”执行：CAPTCHA/SMS/Email 真实凭证、Germany 同类节点、受控 DNS-token、BitBrowser 免费额度、AdsPower paid/trial API 全部暂缓；当前只推进本地可验证工程项。
- 纠正 behavior 能力口径：Rust 分为 `35` declared / `13` active-runner-backed；Go 分为 `35` declared / `30` concrete shipped branches，`drag_to_element`、`select_option`、`scroll_into_view`、`switch_tab`、`close_tab` 不再虚报 shipped。
- 完成 W0：Function.toString nativeization（WeakMap 原函数映射 + 真实 Chrome CDP smoke）、UA/UA-CH/core 单一真相源与 `ua_core_coherent`、DNS inconclusive/评分修正、鼠标 press/release 退避与轨迹/overlay 往返压缩。
- 先修正 API 诚实边界：durable store 落地前所有订阅 API 临时返回 `501 subscription_store_not_implemented`；随后本日已由真实 SQLite store/API 替代；`/api/recording/play/stop` 始终注册且 Recorder 缺失时返回 `503`；订阅 URL 改读 `PERSONAL_PILOT_PROXY_SUBSCRIPTION_URL`，刷新先成功拉取/解析再替换旧数据。
- CI/仓库卫生：CI 新增 `go test ./backend/... -count=1`、前端 `pnpm build`、Node 22；`.worktrees/` 加入忽略但未删除现有工作树；修复 11 个 Markdown 本地失效链接，200 份 Markdown 本地链接检查为 `0` broken。 复核发现严格 `cargo clippy -- -D warnings` 会被 `81` 个历史 warning 阻断，已将 CI 标为 advisory baseline 并把清零后恢复严格门禁列入 backlog。
- 最终门禁：`pnpm typecheck`、`pnpm build`、`go test ./backend/... -count=1`、`cargo test --quiet`、`cargo clippy --workspace --no-deps` 均通过；3 个 benchmark MJS 通过 `node --check`，3 个 XHS PowerShell 脚本 parser 通过，CI YAML 解析通过，`git diff --check` 退出码 `0`（仅换行提醒）。严格 `cargo clippy -- -D warnings` 的 `81` 个历史 warning 已单列，不计作 lint-clean。
- 完成本地 P1：为 `ExecuteMutatedAction` 新增类型化结果 API，Screenshot/HTML/Text 不再被丢弃；旧 error-only 方法兼容，未知 action type 不再静默成功。测试先因接口不存在失败，落地后 targeted behavior tests 通过。
- 完成 durable proxy subscription store：新增 SQLite migration 16、`SQLiteProxySubscriptionStore`、HTTP/base64/Clash 解析器、URL CRUD/validate/nodes/transactional refresh、静态 Clash import、分钟级 auto-refresh due loop 和生产注入。节点 ID 对同一配置稳定；大小写敏感凭证不错误去重；拉取/SQL 失败保留旧节点；响应、浏览器代理 source metadata 和非标准协议配置均脱敏。
- 订阅批次验证：`TestSubscription*`、migration tests、`go vet ./backend/internal/launchcode ./backend/internal/database`、`go test ./backend/... -count=1`、`pnpm typecheck`、`pnpm build`、`cargo test --quiet`、advisory clippy、Markdown/YAML/脚本/diff 门禁均通过。额外 `go test -race` 被本机 CGO 的 32-bit `cc1.exe` 阻塞，属于工具链限制，未伪报 race-clean。
- 待续本地项：workflow detail/history/cancel/pause、workbench target binding/full smoke、XHS humanized type/click、`SMSPoolProvider.GetPrices`、Worker 注入/相干性硬门禁/信任双栈/完整 L5。

## 2026-07-08

- 继续三方横评准备阶段，按用户要求不再追问 Clash / panda / UDEAL 信息，直接本机检测并回写 docs。
- 确认当前监听：`verge-mihomo` mixed-port `7897`、controller `127.0.0.1:9097`；`ssh` 本机 forward `18030`；`sing-box` 本机桥 `18082` / `18090`；`personal-pilot-core` API `19876`。
- 复核 panda SSH：BatchMode 探测通过。
- 完成 UDEAL-LA 现有本机桥预检：`18082`、`18090` 均可用，HTTP/SOCKS 本地消费方式均出 LA；ip-api `proxy=false`、`hosting=false`，ASN 名称 `COGENT-174`。报告：`data/reports/three-browser-benchmark/proxy-preflight/udeal-la-existing-local-bridge-preflight-1783442306848.json`。
- 复核 Clash 机场预检并脱敏原始出口 IP：US `3/3` ok、JP `9/15` ok、DE `0`；成功节点均为 `AS137409 GSL Networks Pty LTD`，ip-api `proxy=true`、`hosting=false`。报告：`data/reports/three-browser-benchmark/proxy-preflight/clash-airport-preflight-1783439936062.json`。
- 新增代理预检汇总：`data/reports/three-browser-benchmark/proxy-preflight/proxy-preflight-summary-1783442692772.json`。结论：Clash 机场 US/JP 和 UDEAL-LA 必须拆成两个 proxy submatrix，不混进同一个 IP 质量横评分；DE 是 provider gap。
- 同步 `docs/48-three-browser-benchmark-matrix-plan.md`、`docs/47-personal-pilot-adspower-bitbrowser-benchmark.md`、`docs/29-proxy-supply-chain.md`、`docs/02-current-state.md`、`docs/03-roadmap.md`、`docs/04-improvement-backlog.md`、`docs/05-ai-maintenance-playbook.md`、`docs/README.md`、`PLAN.md`、`TODO.md`、`STATUS.md`。
- 接手旧只读线程后新增 `scripts/three_browser_benchmark_readiness.mjs`，用于 benchmark readiness 和最小 dry-run；当前默认自动化产品为 PersonaPilot + BitBrowser，输出目录 `data/reports/three-browser-benchmark/readiness/`。
- 启动并确认 BitBrowser API `54345`、PersonalPilot LaunchServer `19876`、AdsPower API `50325/status` 均可达；AdsPower `local.adspower.com` 在当前 Clash 环境解析到 `198.18.*`，脚本改直连 `127.0.0.1:50325`。
- 运行 `PERSONAL_PILOT_API_KEY=... node scripts/three_browser_benchmark_readiness.mjs --dry-run`，生成 `data/reports/three-browser-benchmark/readiness/readiness-1783473069728.json`。结果：PersonaPilot UDEAL-LA profile 创建、启动、CDP `/json/version`、关闭通过；BitBrowser UDEAL-LA profile 创建、`/browser/open`、CDP `/json/version`、`/browser/close` 通过。
- AdsPower profile API 当前返回 `Require api-key`；后续用户截图确认这是 Free 账号 API & MCP 付费墙，不再作为本轮免费 key 获取任务。报告脱敏扫描未命中明文代理 URL、密码、token、secret、Authorization 或原始 UDEAL 出口 IP。
- 接手旧线程权限失效后的继续工作：`scripts/three_browser_benchmark_readiness.mjs` 已补 AdsPower v2 create/start/stop gated dry-run 分支；`node --check scripts\three_browser_benchmark_readiness.mjs` 通过；无 key 的 readiness 复跑生成 `data/reports/three-browser-benchmark/readiness/readiness-1783475041481.json`，阻塞已改记为 AdsPower Free plan API paywall。
- 用户截图确认 AdsPower Free 账号的 API & MCP 页面标注“该功能仅限付费套餐可用”。因此 AdsPower API 自动化不是继续找免费 key 的问题，而是 paid/trial blocker；不付费时本轮 AdsPower 只能作为 API-paywalled/manual-only 竞品观察项，自动化矩阵继续优先跑 PersonaPilot + BitBrowser。
- 按用户确认收敛为 PersonaPilot + BitBrowser 自动化实测，AdsPower 只保留安装/官方资料/免费 UI 手工观察。扩展 `scripts/three_browser_benchmark_readiness.mjs` 支持 `--products=personal-pilot,bitbrowser` 并采样 CDP tab 列表；运行 `PERSONAL_PILOT_API_KEY=... node scripts\three_browser_benchmark_readiness.mjs --dry-run --products=personal-pilot,bitbrowser`，生成 `data/reports/three-browser-benchmark/readiness/readiness-1783475270066.json`，结果 ok：PersonaPilot Chrome `139.0.7258.154`、BitBrowser Chrome `126.0.6478.271`，均打开 `https://browserleaks.com/ip`、CDP 可达、关闭成功；AdsPower skipped。
- 将 `scripts/three_browser_benchmark_readiness.mjs` 默认产品集改为 PersonaPilot + BitBrowser；不传 `--products` 时也会跳过 AdsPower。默认 no-dry-run 生成 `data/reports/three-browser-benchmark/readiness/readiness-1783478822718.json`，blockers 为空，AdsPower skipped。
- 新增 `scripts/two_browser_benchmark_matrix.mjs`，用于 PersonaPilot + BitBrowser 基础 launch-loop 子矩阵；修正 Node 正则语法、将浏览器页 IP 探针从 `ip-api` 换成 HTTPS `ipwho.is`、并让 CDP 优先绑定 `browserleaks.com/ip` 目标页。UDEAL smoke `matrix-1783479977514` 为 `2/2 ok` 且国家匹配 `2/2`。
- 首轮 UDEAL 正式 run `matrix-1783480022601` 受系统内存 `98%+` 污染，BitBrowser 因“内存使用率超出 95%”仅 `1/10`；停止两个旧全盘 `Get-ChildItem -Recurse` PowerShell 扫描后，系统内存降到约 `66%`。
- 重跑 UDEAL-LA 正式候选 `data/reports/three-browser-benchmark/matrix/matrix-1783480223173/summary.json`：PersonaPilot `10/10 ok`，BitBrowser `9/10 ok`，总计 `19/20 ok`；唯一失败仍为 BitBrowser Local API 内存保护。
- Clash US/JP smoke `matrix-1783480496063` 为 `4/4 ok`；正式子矩阵 `data/reports/three-browser-benchmark/matrix/matrix-1783480553485/summary.json` 为 `40/40 ok`，PersonaPilot `20/20`、BitBrowser `20/20`，国家匹配 `40/40`。Clash controller mode 和 GLOBAL hash 已恢复到运行前状态。
- 矩阵报告敏感扫描只命中 `scripts/two_browser_benchmark_matrix.mjs` 自身脱敏正则，未命中明文 proxy credential、Bearer/API key 或已知 UDEAL 原始出口 IP。同步更新 `docs/02-current-state.md`、`docs/03-roadmap.md`、`docs/04-improvement-backlog.md`、`docs/05-ai-maintenance-playbook.md`、`docs/47-personal-pilot-adspower-bitbrowser-benchmark.md`、`docs/48-three-browser-benchmark-matrix-plan.md`、`PLAN.md`、`TODO.md`、`STATUS.md`。
- 用户要求“把未执行的全部执行”后，新增 `scripts/two_browser_benchmark_deep_matrix.mjs`，支持 `--products`、`--submatrices`、`--regions`、`--launches`、`--smoke`、`--no-external-detectors`、`--allow-ip-screenshots`、`--rate-limit-ms` 和 detector allowlist；输出 `config.redacted.json`、`profiles.redacted.json`、`raw/`、`screenshots/`、`har-lite/`、`cdp-trace/`、`detector-reports/`、`transport/`、`behavior/`、`summary.json`、`scorecard.md`。
- UDEAL smoke `data/reports/three-browser-benchmark/deep-matrix/deep-1783482616216/summary.json` 为 `2/2 ok`，覆盖 PersonaPilot + BitBrowser 的 CDP/IP/TLS-H2/WebRTC/行为截图链路；外部 detector 按 `--no-external-detectors` 跳过。
- 当前可执行完整 deep matrix `data/reports/three-browser-benchmark/deep-matrix/deep-1783482661915/summary.json` 为 `6/6 ok`：Clash US/JP + UDEAL-LA，双产品各 1 次；国家匹配 `6/6`、TLS/H2 observed `6/6`、行为 `5/6`、detector 主跑 PersonaPilot `18/18`、BitBrowser `12/12`。PersonaPilot Clash US 行为唯一失败是 CDP `Input.dispatchMouseEvent timeout`；ASCII/中文 CDP insertText、textarea、scroll、iframe 均通过。
- BitBrowser Clash JP detector 首轮出现瞬时 `Page.navigate timeout`，后续修正 deep script 的 detector 统计和 per-site 容错；补跑 `data/reports/three-browser-benchmark/deep-matrix/deep-1783490711021/summary.json` 为 `1/1 ok`、detector `6/6`。
- 验证：`node --check` 通过 `scripts/two_browser_benchmark_deep_matrix.mjs`、`scripts/two_browser_benchmark_matrix.mjs`、`scripts/three_browser_benchmark_readiness.mjs`；Clash 复检为 `rule`，GLOBAL hash `d0abc6973adb79ba`；敏感扫描未命中新 deep reports 的明文代理凭证、Bearer/API key 或已知 UDEAL 原始出口 IP，仅命中脚本脱敏正则和历史示例。同步更新 benchmark 相关 docs、PLAN/TODO/STATUS。
- 用户追问 A/B/C 详细横评后，基于 `matrix-1783480553485`、`matrix-1783480223173`、`deep-1783482661915`、`deep-1783490711021` raw artifact 补写 `docs/47-personal-pilot-adspower-bitbrowser-benchmark.md` §11：A=Clash US/JP、B=UDEAL-LA、C=DE/外部条件。结论：A 中 PersonaPilot 启动速度胜、BitBrowser 行为稳定性胜；B 中 PersonaPilot 稳定性和速度胜；C 因 DE 同类节点、AdsPower API 和受控 DNS-token 缺失不打分。同步在 `docs/48-three-browser-benchmark-matrix-plan.md` 加 A/B/C 摘要。
- 继续按用户要求补 100 分评分表到 `docs/47-personal-pilot-adspower-bitbrowser-benchmark.md` §12。初版实测综合分：PersonaPilot `72/100`，BitBrowser `74/100`，AdsPower 因 API 付费墙 `N/A`；随后已被低配额 missing probe 修正。评分拆分覆盖指纹真实性/IP/行为/证明/API、协议供应链/验证深度/稳定性/泄漏证据、内核 materialize/TLS/RPA/Chrome/CreepJS、静态指纹与泄漏细项、schema/materialize/地理/TLS/双引擎成熟度；缺失项单列 trust/lies、DNS-token、跨 session hash drift、JA3/UA baseline、OS Unicode、Firefox/Camoufox 等。
- 用户提示 BitBrowser 免费额度 50 次/日且当前约 46 次已用后，新增 `scripts/two_browser_missing_probe_matrix.mjs` 作为低配额 targeted probe。策略：今天最多用 3 次 BitBrowser 打开，保留 1 次余量；10-run drift、DNS-token 等高消耗项等额度刷新后再跑。`node --check scripts\two_browser_missing_probe_matrix.mjs` 通过。
- 运行 PersonaPilot missing probe：`data/reports/three-browser-benchmark/missing-probes/missing-1783494756348/summary.json`，`3/3 ok`；country/timezone/TLS/canvas in-session/BrowserLeaks DNS observed 均 `3/3`，WebRTC candidate `0/3`，UA/core major match `0/3`，CreepJS trust/lies parser `0/3`。
- 运行 BitBrowser missing probe：`data/reports/three-browser-benchmark/missing-probes/missing-1783495149879/summary.json`，`3/3 ok`；country/timezone/TLS/canvas in-session/BrowserLeaks DNS observed 均 `3/3`，WebRTC candidate `0/3`，UA/core major match `0/3`，CreepJS trust/lies parser `0/3`。该 run 预计消耗 3 次 BitBrowser 打开，不再继续烧最后一次免费额度。
- 根据 missing probe 修正评分：PersonaPilot `73/100`，BitBrowser `71/100`，AdsPower `N/A`。主要修正是 BitBrowser 新建 profile 也存在 Browser `126` / UA `125` 或 `127` mismatch，不能继续按 UA/core 一致性优势加分；双方 WebRTC/canvas/timezone 初证增强，但 DNS-token、CreepJS trust/lies、10-run cross-session drift 仍缺。

## 2026-07-07

- 用户明确重开 AdsPower / BitBrowser / PersonaPilot 三方横评轨道，要求本轮只做深入检查、安装准备、矩阵设计和待办同步，下一阶段再实测。
- AdsPower 官方 8.6.3 Windows x64 安装器已从 `https://version.adspower.net/software/win64-global/8.6.3/AdsPower-Global-8.6.3-x64.exe` 下载到 `D:\SelfMadeTool\ads\AdsPower-Global-8.6.3-x64.exe`；SHA256 `99AC2ABEF961520919B8376448923DCE28A5BDBB543B77895BC3BE40AE90BD90`；Authenticode valid。
- AdsPower 已静默安装到 `D:\SelfMadeTool\ads\AdsPowerGlobal`，卸载表显示 `AdsPower Global 8.6.3`。未启动、未登录、未创建 profile、未跑 Local API 实测。
- BitBrowser 目录确认为 `D:\SelfMadeTool\bitbrowser`，版本 `7.1.3`，Local API `54345` 可访问，`browser/list totalNum=0`。
- 新增 `docs/48-three-browser-benchmark-matrix-plan.md`，设计 3 产品 x US/JP/DE x 10 连启矩阵，覆盖启动资源、CreepJS/BrowserLeaks/BrowserScan/Pixelscan、IP/DNS/WebRTC/TLS/H2、行为/RPA/API、artifact 和脱敏规则。
- 同步 `docs/02-current-state.md`、`docs/03-roadmap.md`、`docs/04-improvement-backlog.md`、`docs/05-ai-maintenance-playbook.md`、`docs/47-personal-pilot-adspower-bitbrowser-benchmark.md`、`docs/README.md`、`PLAN.md`、`TODO.md`、`STATUS.md`。
- 重要边界：该 benchmark 是独立重开轨道，不改变 Mainline / Local self-use `100% / 0% / green`；当前不付费使用 AdsPower API 时，自动化实测 raw artifact 以 PersonaPilot + BitBrowser 双产品矩阵为准，完成前不得刷新最终实测评分。

## 2026-07-09

- 完成指纹/反检测轨道文档收口：新增 `docs/56-boundary-closure-and-commercial-parity.md`（Provider 闭环、信任双栈、Graph 出站、CreepJS parser、RPA 商业补齐）；强化 `PLAN.md` 为统一执行枢纽（波次 W0–W4、Task 速查、门禁、文档生命周期）。
- 文档归档：`git mv` 27 份 `docs/` 过程/专题文档至 `docs/archive/`；12 份根目录历史文档至 `docs/archive/root/` 并在根目录保留兼容 stub。更新 `docs/archive/README.md`。
- 同步 `docs/README.md`、`docs/03-roadmap.md`、`docs/04-improvement-backlog.md`、`docs/05-ai-maintenance-playbook.md`、`docs/root-entrypoint-map.md`、`docs/42`（历史摘要横幅）、`docs/99`、`docs/00-master-todo`、`TODO.md`、`task_plan.md`。
- 深挖补充缺口（相对 49–55）：iframe 指纹隔离、Privacy Sandbox/QUIC、双栈信任、platform trust.yaml、Graph 出站分裂、无效 fingerprint flag、CreepJS 结构化解析、RPA 可视化 MVP。
