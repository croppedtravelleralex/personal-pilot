# 2026-05 维护日志

## 2026-05-28

- 按用户要求开始执行 `docs/99-implementation-roadmap.md`。尝试启用 `$team` 多 subagent 前置检查：`omx --version` 可用，但当前 Win11 PowerShell 环境没有 `tmux`，`TMUX` 为空，因此 `omx team` 不满足运行前置；本轮改为主线程按 manager-worker 切片推进，并记录该限制。
- 冻结本轮执行边界为首批可验证 P0：Phase 1 环境参数注入编译器与 Phase 2 鼠标轨迹/点击基础模型；未把 43d 全量路线图伪装成一次完成。
- 新增 `backend/internal/behavior/environment_injector.go`：可把 profile 环境参数编译为单次 `Page.addScriptToEvaluateOnNewDocument` 注入脚本，并协调 `Network.setExtraHTTPHeaders` 的 `Accept-Language`；覆盖 browser API、plugins/mimeTypes、canvas、timezone/locale、WebGL 和 media devices 基础 hook。
- 增强 `backend/internal/behavior/humanize/trajectory.go`：新增 Fitts Law movement time、三段式 ease path、8-12Hz 等价粉噪、四段式点击、双击间隔/微移、拖拽和右键菜单计划。
- 新增/更新 Go 单测：`environment_injector_test.go` 和 `trajectory_test.go` 覆盖环境注入脚本、header 协调、warning、轨迹端点、点击阶段、双击、拖拽和右键计划；`go test ./backend/internal/behavior ./backend/internal/behavior/humanize` passed。
- 回写 `docs/99-implementation-roadmap.md`、`docs/02-current-state.md`、`docs/03-roadmap.md` 和 `docs/04-improvement-backlog.md`：标记 Phase 1 1.1、Phase 2 2.1-2.6 已落地，1.2-1.4 为基础 hook partial；仍明确未完成 audio/fonts/WebRTC/consistency matrix、typing/scroll P1、lifecycle、workflow、pool 和完整 observed/replay runtime。

## 2026-05-27

- 按用户要求完成入口清理收口：唯一用户可打开 GUI exe 固定为 `D:\SelfMadeTool\personal-pilot\personal-pilot-tauri.exe`；删除第二套 Tauri/Vite console UI 源码、`gateway-ui/` 静态模板 UI、历史 root/target GUI exe 产物和 build target 生成目录。
- 新增 `scripts/sync-main-entry.ps1` 并接入 `pnpm desktop:release`：release 构建后复制 `src-tauri/target/release/personal-pilot-tauri.exe` 到根目录，再删除 target release 临时产物，避免用户再次打开旁路 exe。
- 最新根目录入口 SHA256 `ACC04B0224D09D736BC5F5575B0F92B61C8D4C60091F69D0072137F95ECB5CE9`，大小 `20,104,192` bytes；非依赖 GUI exe 扫描只剩该 root exe。
- 当前 release performance smoke report：`data/reports/release-smoke/release-performance-smoke-1779895292285.json`，状态 `warning`，`10274ms` cold start、`672MB` idle RSS、`22` processes；external distribution smoke report：`data/reports/external-distribution/external-distribution-smoke-1779895317484.json`，状态仍为 `blocked_external_smoke_required`。
- 数据层复核：`data/app.db` 未被删除，当前 core/RPC 可见实例 `4`、代理 `93`、内核 `3`；历史 `95` 代理数是 2026-05-25 快照，当前不能再写成 live truth。
- 文档已明确规则：后续不得恢复 `PersonaPilot.exe`、`portable.exe`、`persona-pilot-desktop.exe`、installer exe、`gateway-ui/` 或第二套 UI；`bin/` sidecar 和 `chrome/` 浏览器引擎是运行依赖，不属于用户入口。
- 针对用户反馈“控制台显示 4 个实例，点进去变成 0”，主线程并行复核 dashboard 统计、实例列表、详情路由和旧 UI 残留。根因不是后端数据掉零，而是旧第二套 UI 的详情/编辑/复制路由参数和页面读取字段不一致，导致页面拿不到当前实例。
- 已修复上述 3 个页面为读取 `profileId`，保持 `BrowserProfileList` / `GetDashboardStats` / `BrowserInstanceGetTabs` 后端链路不变；该修复属于主线 UI 路由参数回归修复，不涉及第二套 UI 回滚或数据层口径切换。
- 继续按用户要求推进主线 UI 深测与 API 收口：派出多个 `gpt-5.5` subagent 做 browser/settings 代码核查、主线 API 风险扫描和旧入口残留扫描；其中 verifier 类 agent 因本地 distributor 无可用固定通道失败，主线程已接管同等检查。
- 主线程完成 `dashboard/profile/monitor` 收口：`src/services/desktop.ts` 新增 dashboard、profile remote-author、event log typed wrapper；`src/modules/monitor/api.ts` 新增模块 facade；`src/modules/dashboard/api.ts`、`src/modules/profile/api.ts`、`src/modules/monitor/EventMonitorPage.tsx` 不再直接探测旧 Wails runtime。
- `EventMonitorPage` 历史查询新增 `300ms` debounce 和 stale-result 保护，满足 Win11/Tauri baseline 对搜索交互的默认要求。
- 运行 `powershell -ExecutionPolicy Bypass -File scripts/windows_local_verify.ps1 -SkipContinuityTest`：`pnpm typecheck`、`pnpm build`、Win11 baseline enforcement、`cargo test --lib -- --test-threads=1`、`pnpm desktop:release`、`cargo test --quiet` 全部通过。
- 已重新同步根目录主线入口；本日最终 hash 以本节顶部记录的 `ACC04B0224D09D736BC5F5575B0F92B61C8D4C60091F69D0072137F95ECB5CE9` 为准。
- 按用户要求将 Camoufox 集成方案写入文档：`docs/18-external-browser-integration-plan.md` 新增 `P1.5：Camoufox 单任务生产级 Runner（90 分完整方案）`，明确它是可选任务执行引擎，不替换 Tauri/WebView2，不默认 remote server，不内置 Python runtime。
- 同步回写 `docs/02-current-state.md`、`docs/03-roadmap.md` 和 `docs/04-improvement-backlog.md`：当前只是文档化方案，尚未实现；下一步从设置、能力检测、错误码和单任务执行闭环开始。
- 继续按用户要求深化 Camoufox 方案并尝试多 subagent 执行：已请求 4 个 explorer subagent 分别分析 runner/core、Tauri/native 边界、前端设置/任务详情、性能/构建约束，但本地 distributor 均返回 `503 Service Unavailable`，因此降级为主线程并行只读扫描。
- 基于项目结构证据补强方案：确认现有 `TaskRunner`、`RunnerKind`、Lightpanda pid/timeout/cancel/stdout/stderr 生命周期、artifacts 表、RunDetailPanel、分页/虚拟滚动/300ms debounce、release performance smoke 都可复用；文档已补充性能最优策略（不常驻、不预热、并发默认 1、artifact 按需、capability 缓存、task-run 性能单独记账）和后续 5-agent 切片执行建议。
- 在当前分支 `codex/camoufox-runner-90` 上记录 Camoufox 切片 D 进入实施记录阶段：范围限定为 profile/proxy 轻量映射和 explain 合同，当前只更新 `docs/18-external-browser-integration-plan.md` 与本月日志；不宣称 Camoufox runner 完成、不新增默认引擎、不改变 Lightpanda/FakeRunner 行为。
- 切片 D 最小验证口径已补入方案：后续必须验证未配置 Camoufox 不影响现有 runner、profile/proxy 不完整时输出 `ignored` 与 `failureReason`、可映射时输出 `profileVersion` / `proxyId` / `applied` / `ignored` / `runtimeAdapter=camoufox`、取消/超时后无残留进程、artifact 只保留引用。
- 切片 D 性能约束已补入方案：主应用 release baseline 仍按 cold start `<= 2.0s`、idle RSS `<= 220MB`、process count `<= 4` 独立记账；Camoufox 不得启动预热、自动检测或常驻，task-run 指标需单独记录启动耗时、峰值 RSS、子进程数、artifact size 和 cleanup result。
- 切片 E 完成验证与文档/脚本补充：新增 `scripts/camoufox_smoke.ps1`，默认只做非破坏性合同检查，不启动 Camoufox、不写设置、不杀进程；当前预期状态为 `contract_ready_runtime_smoke_required`。
- 切片 E 同步回写 `docs/18-external-browser-integration-plan.md`、`docs/02-current-state.md`、`docs/03-roadmap.md` 和 `docs/04-improvement-backlog.md`：当前代码已有 `RunnerKind::Camoufox`、settings/capability commands 和 desktop wrappers，但 `src/runner/camoufox.rs` 仍是 skeleton，真实打开页面、artifact、取消/超时清理和 task-run 性能证据仍未完成。

## 2026-05-26

- 按用户要求固定后续工作纪律：每轮涉及 UI / 入口 / 路由的大更新后，必须先由 AI 自测门禁并启动主线 UI，再交给用户检查 app 外观。
- 固化 `react-router-dom@6.30.3` 到 `package.json` / `pnpm-lock.yaml`，避免本地 `node_modules` 残留掩盖干净安装问题。
- 将 release performance smoke、external distribution smoke、Tauri fingerprint regression 和 two-instance workbench 验证脚本的默认目标收敛到 `D:\SelfMadeTool\personal-pilot\personal-pilot-tauri.exe`，不再默认验证 `PersonaPilot.exe` 或 `persona-pilot-desktop.exe`。
- 将 `.codex_tmp/` 与 `.tmp/` 加入 `.gitignore`，降低临时产物误提交风险。
- 重新构建并覆盖根目录入口：`pnpm desktop:release` passed，最新 `D:\SelfMadeTool\personal-pilot\personal-pilot-tauri.exe` SHA256 `BD4DA78B485CAEBE31A16C1A55E66603E6A12E5E93D31484C025DCDACAFF8C68`，大小 `19,943,424` bytes。
- 复跑 release performance smoke：`data/reports/release-smoke/release-performance-smoke-1779797166703.json`，状态仍为 `warning`，实测 `2437ms` cold start、`465MB` RSS、`9` processes。
- 复跑 external distribution smoke：`data/reports/external-distribution/external-distribution-smoke-1779797144455.json`，本机资产 gate 已按单入口通过，但仍因 manual/external gates 未跑而 `blocked_external_smoke_required`。
- 用户批准继续派 subagent 执行并更新文档；尝试派出入口验证、UI 收敛风险扫描、文档一致性扫描 3 个 subagent，但本地分发器均返回 `503 Service Unavailable`，无可用模型通道。
- 主线程接管完成同等只读核验：根目录 `personal-pilot-tauri.exe` 存在，大小 `19,924,480` bytes，SHA256 `BA456FE762CBD4772E7796F5874CF210E827B46AA71595C921B7853B19274E2E`；最新 release smoke 仍为 warning，external distribution smoke 仍因外部/manual gates blocked。
- 主线程扫描主线 UI 收敛风险：旧入口/旁路 UI 没有重新成为主线；剩余主要风险是 `src/modules/*` 直接 `wailsjs` 调用，集中在 `src/modules/browser/**`、`src/modules/settings/**`、`src/modules/profile/api.ts`、`src/modules/dashboard/api.ts`、`src/modules/monitor/EventMonitorPage.tsx`、`src/modules/synchronizer/api.ts`。

## 2026-05-25

- 新增 `docs/00-ai-maintenance-entry.md` 作为低上下文 AI 接手入口，专门处理 Codex / Claude 历史记录过大导致的 context window 超限问题。
- 更新 `docs/README.md` 和 `docs/05-ai-maintenance-playbook.md`，把低上下文入口放到接手顺序第一位。
- 补全 `docs/01-project-charter.md`，固定 PersonaPilot 的目标、边界和维护底线。
- 明确历史记录用法：Codex / Claude 记录只作为证据索引，live truth 以 `docs/02-current-state.md` 为准。
- 用户确认唯一主线 UI 是截图所示 `personal-pilot` 1.1.0 / Wails v2 + React，唯一入口为 `D:\SelfMadeTool\personal-pilot\personal-pilot-tauri.exe`。
- 同步维护文档：`PersonaPilot.exe`、`portable.exe`、`persona-pilot-desktop.exe` 和第二套 UI 不再作为主线；后续新功能必须迁入/集成到 `personal-pilot-tauri.exe` UI。
- 产出第一版验收入口：`pnpm build` passed，`pnpm desktop:release` passed，生成 `src-tauri/target/release/personal-pilot-tauri.exe` 并同步到根目录 `personal-pilot-tauri.exe`。
- 验证：Win11/Tauri baseline enforcement passed；根目录 exe 启动后进程响应正常，且可见窗口包含 `personal-pilot`；release smoke report `data/reports/release-smoke/release-performance-smoke-1779692271595.json` 为 warning，原因是 cold start/RSS/process count 超预算。
- 用户验收反馈：该 Tauri 第一版 app logo 异常，实例/代理/内核数据全部显示为 `0`，并出现卡顿/空白窗口。根因是只迁了旧 UI 壳，未迁入旧 Wails 后端数据/API 绑定。
- 已回滚根目录和 `src-tauri/target/release` 的 `personal-pilot-tauri.exe` 到备份 `.codex_tmp/exe-backups/personal-pilot-tauri.20260524-211021.exe`；恢复后 hash 为 `6E8576527BC54BFF2FA41514570BBB9C6F3DBE4D7EED340F44FFA77C238FF7C9`。
- 已确认 `data/app.db` 原数据仍在：实例 `4`、代理 `95`、内核 `3`、书签 `7`、事件日志 `36334`。后续新包必须先证明读取这些真实数据，再给用户验收。
- 重新产出可验收第一版入口：`pnpm build` passed，`pnpm tauri build` passed，生成 `src-tauri/target/release/personal-pilot-tauri.exe` 和 `src-tauri/target/release/bundle/nsis/personal-pilot_1.1.0_x64-setup.exe`。
- 已覆盖根目录入口 `D:\SelfMadeTool\personal-pilot\personal-pilot-tauri.exe`；SHA256 `AA58F4A12FBBE07D5F42F13035762A55EB306F8663093D814CEC46FBFF654D64`，大小 `19,935,232` bytes。
- 已启动新根目录入口验证：窗口标题 `personal-pilot`，进程 `personal-pilot-tauri`；`tauriWailsBridge` 启动 `bin/personal-pilot-core.exe` 后，RPC 核验 `GetDashboardStats` / `BrowserProfileList` / `BrowserCoreList` / `BrowserProxyList` 均通过。
- 数据链路核验结果：dashboard/core bridge 返回实例 `4`、代理 `95`、内核 `3`、profile list `4`、proxy list `95`、core list `3`，确认不再是全 `0` 空壳；已清理前次启动残留 core 进程，仅保留当前入口关联 core。
- 修复用户验收发现的 UI 问题：设置页空白根因是 `ThemeSwitcher` 使用 `useTheme` 但当时第二套 console 根节点未包 `ThemeProvider`；黑色大图标根因是 `src-tauri/icons/icon.ico` 仅 `155` bytes，已用现有 PNG 资源重建为有效 `13,322` bytes ico。
- 重新构建并覆盖根目录入口：`pnpm build` passed，`pnpm tauri build` passed，最新 `D:\SelfMadeTool\personal-pilot\personal-pilot-tauri.exe` SHA256 `87AB55464AD86BB805AE9F839B682D9B7A5A314B1F753942DE10E0967E734D6D`；启动后 core bridge 再次核验实例 `4`、代理 `95`、内核 `3`。

## 2026-05-28 Roadmap Implementation Slice

- 按 `docs/99-implementation-roadmap.md` 继续推进后端能力切片：Phase 1 P1/P2、Phase 2 P1 已补可测试模型；Phase 3-5 新增 contract-ready 包。
- 新增/增强：environment injector WebGL/audio/fonts/media/WebRTC hooks、browser consistency matrix、typing/scroll humanize P1、lifecycle engine、workflow/plugin/provider contract、reference/session/pool/discovery/transport contract。
- 验证：`go test ./backend/internal/...` passed。
- 边界：Phase 3-5 仍未接真实执行器/API/跨机器验收，不得写成生产闭环；`EXECUTION_LOG.md` 已有既存未归属改动，本轮未处理。
- Release/build gate：`pnpm typecheck`, `pnpm build`, Win11 Tauri baseline enforcement, `pnpm desktop:release`, `scripts/live_truth_guard.ps1` passed；root `personal-pilot-tauri.exe` SHA256 `E4B5273CAA8F0ACDB1AE2F8288075D438482F6E0DC469D1A62151E8E959EEBA5`, size `20,104,192` bytes。

## 2026-05-28 Roadmap Implementation Slice 2

- 同步推进 Phase 3-5：lifecycle SQLite store/risk、workflow recorder/playback/executor/form/provider adapters、LaunchCode workflow API、SessionBundle AES、pool prewarm、discovery flow/template、transport outbound config。
- 验证：`go test ./backend/internal/...` passed。
- 边界：这些是可测试实现或 adapter/contract；真实 provider 凭证、profile browser observed proof、cross-machine restore、真实 browser prewarm、Xray/Sing-Box 出站运行仍需 smoke 证据。
- Release/build gate：第二批实现后 `pnpm desktop:release` passed；root `personal-pilot-tauri.exe` SHA256 `E9F1C54EB7E4AF5E2917EBA0B4C7854124A49C01297BED4C4454FC36D9843ACC`, size `20,104,192` bytes。

## 2026-05-28 Camoufox Core Integration

- 按用户要求继续推进并同步文档：Camoufox 已作为主线浏览器内核类型接入，不再停留在文档/skeleton 层。
- 后端改动：新增 `browser_cores.kind` 迁移、`CoreKindCamoufox`、core kind 归一化、Camoufox executable candidates/path resolution、`BrowserCoreValidateForKind` sidecar RPC、实例启动参数按 kind 分发；Camoufox 使用 `--profile` + `--remote-debugging-port`，Chromium/Lightpanda 路径不回归。
- 前端改动：`CoreManagementPage`、`BrowserListPage` 基础配置弹窗、`BrowserSettingsModal` 均可选择 Chromium / Lightpanda / Camoufox，并按 kind 进行路径校验和保存。
- 验证：`go test ./backend/...`、`pnpm typecheck`、`pnpm build`、Win11/Tauri baseline enforcement、`scripts/live_truth_guard.ps1`、`pnpm desktop:release` passed；`scripts/camoufox_smoke.ps1 -AllowBlocked` 输出 `contract_ready_runtime_smoke_required`。
- 边界：Rust Camoufox runner 仍是 skeleton，真实打开页面、artifact、取消/超时清理和 task-run 性能证据未完成，不能宣称 Camoufox task runtime-ready。
- Release/build gate：root `personal-pilot-tauri.exe` SHA256 `75063F9906CCDE8BCDACD7622F2349BD819D755C2B9D4F095CA736D70C4D06DE`, size `20,104,192` bytes；`src-tauri/target/release` 持久 GUI exe 已清理。

## 2026-05-28 Roadmap Evidence Gates

- 按用户“全部执行并同步文档”继续执行剩余证据入口；新增 `scripts/roadmap_evidence_smoke.ps1`，用于固化 1.2 browser_api_surface、1.3 canvas_rendering、1.4 timezone_locale 和 3.1 lifecycle 的本机实现/本机 runtime-store 证据，同时保留 observed proof 边界。
- 本机 roadmap evidence report：`data/reports/roadmap-evidence/roadmap-evidence-smoke-1779941218312.json`，状态 `passed_with_external_evidence_pending`。
- 外部/真实环境 gate 已执行并保留实际状态：profile-browser comparison `blocked_missing_validation_report`，provider acceptance `blocked_missing_credentials`，SessionBundle portability `local_contract_passed`，runtime adapter `blocked_evidence_required`，Camoufox smoke `contract_ready_runtime_smoke_required`，external distribution `blocked_external_smoke_required`。
- 通过项：taxonomy audit `data/reports/taxonomy-audit/taxonomy-audit-1779940275062.json` passed；live truth guard `data/reports/governance/live-truth-guard-1779941218292.json` passed；`go test ./backend/...`、`pnpm typecheck` 和 `pnpm build` passed。
- 文档同步：`docs/02-current-state.md`、`docs/03-roadmap.md`、`docs/04-improvement-backlog.md`、`docs/99-implementation-roadmap.md` 已记录最新 report、状态和不能冒进为 observed/runtime-ready 的边界。

## 2026-05-28 Roadmap Completion Evidence

- 按用户“把 roadmap 未完成的通通完成”继续推进：新增 `scripts/profile_browser_environment_probe.mjs`，不新增 npm 依赖，启动仓库内 fingerprint Chromium，通过 CDP 真实观测 browser API / canvas / timezone-locale 注入效果。
- Profile-browser evidence passed：`data/validation-reports/profile-browser-environment-1779951691042.json` 与 `data/reports/profile-browser-environment/profile-browser-environment-1779951691042.json` 均为 `passed`，覆盖 roadmap 1.2 / 1.3 / 1.4。
- 复跑 evidence gate：`data/reports/roadmap-evidence/roadmap-evidence-smoke-1779951732878.json` 为 `passed_with_external_evidence_pending`；`data/reports/profile-browser-comparison/profile-browser-comparison-1779951732949.json` 为 `partial_comparison_only`，原因是最新 validation report 是 profile-browser 专项报告，不含同份 desktop-webview 对照信号。
- `docs/99-implementation-roadmap.md` 已将 1.2 / 1.3 / 1.4 / 3.1 勾为 `[x]`；真实 provider、跨机器 SessionBundle、Camoufox runtime、外部分发和同份 desktop/profile comparison 仍保留在 Overall 外部验收轨道。


## 2026-05-28 Roadmap 4/5/6/7/9 Implementation

- 按用户要求先实现 roadmap 缺口 4/5/6/7/9：环境注入已接入实例 debug-ready 流程，`environment_audit` 扩到 8 个注入族；同一实例加注入去重，停止实例清理去重记录。
- Camoufox runner 从 skeleton 改为最小 CDP runner：支持 open/fetch/html/title/final-url/text/validation-probe，输出 title/finalUrl/content/stdout/stderr preview，结束或超时后清理子进程；`validation_probe` 覆盖 navigator/canvas/timezone/WebGL/audio/fonts/media/WebRTC/storage。
- Phase 6 P0 推进：`transport/` 被 Xray/SingBox 配置生成引用，安全合并 ALPN；runtime family/header-order/HTTP2 细节只保留为 explain metadata，不写入真实 outbound 未知字段。
- Behavior audit contract 更新：shipped primitives 的 workflow/debug trace 进入 runtime evidence backed；仍不等同完整 `450+` replay runtime。
- 边界：真实 Camoufox binary page-open 已完成；真实 headed_external browser binary task-run、provider 凭证、跨机器 SessionBundle、远程代理出站/TLS 指纹观测、完整 observed fingerprint/live behavior replay 仍待外部证据。
- 已跑验证：`go test ./backend/internal/behavior ./backend/internal/transport ./backend/internal/proxy`、`go test ./backend -run "TestBuildEnvironmentInjectionProfileFromLaunchFlags|TestProfileEnvironmentInjectionClaimDeduplicatesSameLaunch|TestWaitForBrowserDebugReadyMarksProfileReady"`、`go test ./backend/internal/proxy ./backend/internal/transport ./backend`、`go test ./backend/...`、`cargo test camoufox`、`cargo test behavior_audit_contract_preserves_shipped_vs_target_boundary`、`cargo test --lib -- --test-threads=1`、`pnpm typecheck`、`pnpm build`、Win11/Tauri baseline enforcement、`pnpm desktop:release` passed。
- 新证据 report：Camoufox source smoke `data/reports/camoufox-smoke/camoufox-smoke-1779976246144.json` 为 `passed`；roadmap evidence `data/reports/roadmap-evidence/roadmap-evidence-smoke-1779976243311.json` 为 `passed_with_external_evidence_pending`；runtime adapter gate `data/reports/runtime-adapter/runtime-adapter-evidence-gate-1779977785909.json` 为 `blocked_evidence_required`；live truth guard `data/reports/governance/live-truth-guard-1779976243305.json` 为 `passed`。
- Release 性能烟测：`data/reports/release-smoke/release-performance-smoke-1779977378110.json` 为 `warning`，实测 cold start `6440ms`、idle RSS `343MB`、process count `10`，仍超过默认预算。
- Release/build gate：root `personal-pilot-tauri.exe` SHA256 `704B14A6B358276124CBD5AF43E8BE0820507F2AD8A579CDBD3A87869DE701DC`, size `20,585,984` bytes。
## 2026-05-28 Runtime Evidence Completion Slice

- 按用户要求继续补 450 指纹/450+ 行为、headed_external、Xray/SingBox 和 Camoufox 真实 binary 证据；本轮不把 contract/materialized 证据写成 observed/live runtime。
- Taxonomy coverage materialization 已通过：`data/reports/taxonomy-coverage/taxonomy-coverage-materialized-1779981626750.json` 为 `passed_materialized_contract`，fingerprint `450 / 450`，behavior `461 / 450`；边界是 materialized contract，不是 observed fingerprint 或 live replay runtime。
- `headed_external` 泛化 source contract 已落地：`src/runner/headed_external.rs`、`RunnerKind::HeadedExternal`、主进程/Tauri state 选择链和 smoke 脚本已接入；`data/reports/headed-external-smoke/headed-external-smoke-1779981351410.json` 为 `passed_source_contract`；真实 headed browser binary task-run 仍未跑。
- Xray/SingBox 本地真实二进制配置验证已通过：`data/reports/transport-binary-smoke/transport-binary-smoke-1779981454305.json` 为 `passed`；Xray `26.2.6` 的 `run -test -config` 输出 `Configuration OK`，SingBox `1.12.17` 的 `check -c` exit code 为 `0`；边界是 direct SOCKS/direct outbound 配置接受性，不是远程代理出站或 TLS/HTTP2 指纹观测。
- Camoufox 真实 binary page-open 已通过：`data/reports/camoufox-binary-task/camoufox-binary-task-smoke-1779983524496.json` 为 `passed`，`C:\Users\Lenovo\AppData\Local\camoufox\camoufox\Cache\camoufox.exe` 打开 `https://example.com` 并产出持久截图 `data/reports/camoufox-binary-task/camoufox-binary-task-smoke-1779983524496.png`；边界是 Firefox-compatible headless screenshot，不宣称 Chromium `/json/version` CDP attach 成功。
- Runtime adapter gate 复跑后仍为 blocked：`data/reports/runtime-adapter/runtime-adapter-evidence-gate-1779983066984.json` 为 `blocked_evidence_required`，原因是 B1-B5 / AdsPower refresh 外部证据仍不完整。

## 2026-05-30

- 按当前 Overall `60%` 剩余项继续推进本机 evidence：使用仓库内 `chrome/fingerprint-chromium-144.0.7559.132/chrome.exe` 跑 `scripts/headed_external_smoke.ps1 -Action get_title -Url https://example.com -RunRustTests`。
- 结果：`data/reports/headed-external-smoke/headed-external-smoke-1780109927517.json` 为 `passed_real_binary_task`；真实浏览器执行链返回 title `Example Domain`、finalUrl `https://example.com/`、`realBrowserExecution=true`，并保留 CDP attach / process lifecycle 最小任务证据。
- 验证：`cargo test --quiet headed_external` 6 个相关测试 passed；Windows target incremental 目录出现一次 `os error 5` finalize warning，但测试结果为 passed。
- Runtime adapter gate 已刷新：`data/reports/runtime-adapter/runtime-adapter-evidence-gate-1780109948793.json` 仍为 `blocked_evidence_required`，但 `headed_external` adapter 已从 source contract 升级为 `real_binary_task_recorded`；AdsPower refresh 仍 `deferred_by_evidence_gate`。
- 边界：本轮不提高 Overall `40% / 60% / yellow`，不宣称完整 headed runtime fingerprint/leak/coherence、provider acceptance、跨机器 SessionBundle、远程代理出站/TLS 指纹观测或 live behavior replay 已完成。
- Release/build gate：`pnpm desktop:release` passed；root `personal-pilot-tauri.exe` SHA256 `704B14A6B358276124CBD5AF43E8BE0820507F2AD8A579CDBD3A87869DE701DC`, size `20,585,984` bytes。

## 2026-05-30 Headed External Validation Probe

- 继续推进真实 headed runtime evidence：执行 `powershell -ExecutionPolicy Bypass -File .\scripts\headed_external_smoke.ps1 -Action validation_probe -TimeoutSeconds 45`。
- 结果：`data/reports/headed-external-smoke/headed-external-smoke-1780121855313.json` 为 `passed_real_binary_validation_probe`；真实仓库内 fingerprint Chromium 通过 CDP 返回 `realBinaryTask.status=passed`、`action=validation_probe`、`validation_signals=9`。
- Runtime adapter gate 已刷新：`data/reports/runtime-adapter/runtime-adapter-evidence-gate-1780121877907.json` 仍为 `blocked_evidence_required`，但已消费 headed probe，`runtimeAdapterEvidence=partial_real_binary_validation_probe_recorded`、`fingerprintRuntimeDepth=partial_headed_profile_browser_observed`、categories `audio/canvas/detector/fingerprint/leak/timezone/webgl/webrtc`。
- 边界：这只证明 headed_external 的真实 profile-browser validation probe 已可复现；远程代理/TLS、provider 凭证验收、跨机器 SessionBundle、完整 `450` observed coverage、repeatability/coherence matrix、AdsPower refresh 和外部分发人工 smoke 仍未完成。

## 2026-05-31 Runtime Evidence Closeout

- 修复 evidence 选择回退：`scripts/runtime_adapter_evidence_gate.ps1` 与 desktop release smoke contract 改为按 evidence strength 选择 headed_external report，避免新生成的 source-only / weak report 覆盖更强的 `passed_real_binary_validation_probe`；`scripts/headed_external_smoke.ps1` 修复 cargo stdout 污染，避免命令输出破坏 `realBinaryTask`。
- 验证：`cargo test -q release_smoke_contract_keeps_stronger_headed_external_evidence_when_source_report_is_newer` passed；`cargo test -q release_smoke_contract` 4 tests passed；`powershell -ExecutionPolicy Bypass -File scripts\headed_external_smoke.ps1 -Action validation_probe -TimeoutSeconds 45 -RunRustTests` 生成 `data/reports/headed-external-smoke/headed-external-smoke-1780198191425.json`，状态 `passed_real_binary_validation_probe`，并跑过 `cargo test --quiet headed_external` 8 tests passed。
- 本机可执行 evidence 复刷：Camoufox source smoke `data/reports/camoufox-smoke/camoufox-smoke-1780198459643.json` 为 `passed`；Camoufox binary page-open `data/reports/camoufox-binary-task/camoufox-binary-task-smoke-1780198493050.json` 为 `passed`；SessionBundle portability `data/reports/session-portability/session-bundle-portability-smoke-1780198283603.json` 为 `local_contract_passed`。
- 外部阻塞保持原样：remote proxy/TLS `data/reports/remote-proxy-tls/remote-proxy-tls-probe-1780198283532.json` 为 `blocked_remote_proxy_required`；provider acceptance `data/reports/provider-acceptance/provider-acceptance-preflight-1780198283660.json` 为 `blocked_missing_credentials`；profile-browser comparison `data/reports/profile-browser-comparison/profile-browser-comparison-1780198283892.json` 为 `blocked_missing_desktop_webview_report`。
- Runtime adapter gate 已复跑：`data/reports/runtime-adapter/runtime-adapter-evidence-gate-1780198524266.json` 为 `blocked_evidence_required`，但 ranked selection 正确选中 `headed-external-smoke-1780198191425.json`，记录 `partial_real_binary_validation_probe_recorded`、`partial_headed_profile_browser_observed`、`signalCount=9`、categories `audio/canvas/detector/fingerprint/leak/timezone/webgl/webrtc`；AdsPower refresh 仍为 `deferred_by_evidence_gate`。
- Governance：`data/reports/governance/live-truth-guard-1780198316165.json` 为 `passed`。本轮不提高 Overall `40% / 60% / yellow`，不把 taxonomy materialized contract、Camoufox page-open 或 headed validation probe 写成完整 `450` observed coverage / complete headed realism。

## 2026-05-31 M4-M20 Execution Harness

- 按用户要求创建并推进 `codex/m4-m20-full-implementation` goal/branch；新增 `docs/40-m4-m20-execution-board.md`，固定 M4-M20 路线、极限验收标准、10% 漂移规则、分批 commit 纪律和 live truth 边界。
- 新增 `scripts/m4_acceptance_gate.ps1`：聚合 live truth、runtime adapter、provider acceptance、SessionBundle portability 和 profile-browser comparison 五类关键 gate；阻塞项必须分类为 `expected_blocked`，不能被误报为 passed。
- 最新 M4 report：`data/reports/m4-acceptance/m4-acceptance-gate-1780234896011.json`，状态 `expected_blocked`，summary `passed=1`、`expectedBlocked=4`、`failed=0`；live truth guard 同步刷新为 passed。
- Dashboard 验收证据面板已扩展到 M4 gate、release performance、runtime adapter、profile/browser comparison、remote proxy/TLS、headed browser、Camoufox、provider、SessionBundle、taxonomy coverage/audit 和 external distribution；Rust evidence history 已支持读取 `m4-acceptance`。
- 自动化切片：Go CDP task runner 和 behavior CDP executor 修复 `Runtime.evaluate.result.value` 嵌套布尔/字符串解析；补 raw CDP dialog/download/upload 派发测试、selector resolve 测试和 wait 真实 CDP 布尔解析测试。
- 验证：`go test ./backend/internal/scheduler ./backend/internal/behavior -count=1` passed；`cargo test evidence_report_history` 5 tests passed；`pnpm typecheck` passed；`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\m4_acceptance_gate.ps1` exit 0，状态为预期 external blocked。
- 边界：真实 provider 凭证、远程代理/TLS、跨机器 SessionBundle、同份 desktop/profile comparison、完整 headed repeatability/coherence、AdsPower refresh 和完整 `450` observed/replay coverage 仍未完成，不提高 Overall。
