# 2026-04 维护日志

## 2026-04-15

### 本轮已执行
- 项目品牌改名已完成并落地为 `PersonaPilot`
- 本地目录已迁到 `D:\SelfMadeTool\persona-pilot`
- 修复了 `src/humanize/failure.rs` 与 `src/humanize/middleware.rs` 的 lib-test 编译阻塞
- `cargo check -q` 通过
- `cargo test --no-run -q --lib` 通过

### 本轮确认的新边界
- 全量 `cargo test --no-run -q` 的剩余阻塞已明显收窄
- 当前主要只剩 `integration_continuity_control_plane` 一组 continuity 控制面测试
- 缺口不是新的通用 build 噪音，而是：
  - `append_continuity_event`
  - `run_persona_heartbeat_tick`
  这组主线能力在当前 `src/api/handlers.rs` 中不可用
- 另外确认：`src/api/handlers/core.rs` 是大面积过时的旧分支文件，不能直接重新接回主编译

### 下一切片
- 优先做 continuity control-plane compile gap 收口
- 再推进 `stable_v1 prod-live 30min x2`
- 再推进 `gateway-upstream` 独立验收

### 阶段判断
- 当前这轮执行已经把“泛化本地构建噪音”缩成“单一 continuity 主链缺口”
- 这对下一阶段推进是正向收敛，不是新扩散

## 2026-04-11

### 已完成
- 新增共享指纹消费 schema，并接入 validator / runner / explainability。
- status DTO 与 explainability DTO 增加 `mode` / `source_tier` / `verification_path` / `continuity_level` 等字段。
- proxy source metadata 增加 `for_demo` / `for_prod` / `source_tier` 等字段。
- `/status` 拆分 reported / effective active ratio。
- longrun driver / report 增加 mode 口径。
- verify batch 开始按 mode + source eligibility 过滤。
- 建立 docs 维护入口。
- 开始重构 preflight / release 脚本到 profile 化结构。

### 当前阻塞
- 远端整库 `cargo test -q` 仍被脏分支中的 `handlers/core.rs`、`runner/engine.rs`、`db/schema.rs` 编译漂移阻塞。

### 下一步
- 同步脚本到远端。
- 先做 `bash -n` / `python3 -m py_compile`。
- 远端执行 `public-smoke` / `prod-live` 预检与快验。

### 远端验证 follow-up
- 重新拉起远端 control plane 到新 debug binary，确认 `/status.mode` 与 effective ratio 新口径真实上线。
- 修复 `scripts/lightpanda_verify.sh` 的 create body 与 `json_get` 原始 JSON 解析问题。
- public-smoke preflight pass。
- public-smoke release_fast_verify pass。

## 2026-04-12

### 已完成
- prod-live preflight 已通过。
- prod-live release_fast_verify 已通过。
- prod-live 快速长跑已经观测到：
  - `mode == prod_live`
  - active pool positive
  - `stateful_continuity_observed == true`
- `scripts/proxy_longrun_report.py` 新增固定输出：
  - `reports/source_quality_summary_latest.txt|json`
  - `reports/session_continuity_summary_latest.txt|json`

### 当前结论
- release / continuity / explainability 已明显收口。
- 代理质量仍是主短板，当前 prod-live 快照大致为：
  - `effective_active_ratio_percent ≈ 0.52`
  - `promotion_rate ≈ 6%`
  - `reject_rate ≈ 94%`

### 下一步
- 继续做 source/provider 自动 quarantine。
- 把 replenish 指标改成 mode-aware + windowed。
- 继续提升 hot region 的有效 active 覆盖。

### 2026-04-12 晚间补充：prod-live 代理池整治已实跑
- 本地新增 `scripts/prod_proxy_pool_hygiene.py`，支持：
  - 读取 `/root/prod_proxy_sources.controlled.json`
  - 回填 prod source metadata
  - 对 `candidate/candidate_rejected` 按 source 做窗口压缩
  - 对 `zero-active + low-promotion` source 写入 quarantine
- 远端实跑结果：
  - 删除低价值代理记录 `16457` 条
  - 同步 metadata `4` 条
  - `github_monosans_http` 已被 quarantine
- 由于运行进程保留旧内存池分母，后续重启了**同一份** `target/debug/PersonaPilot`，未重编。
- 重启后 `/status` 已收敛到新口径：
  - `proxy_pool_status.total = 481`
  - `active = 147`
  - `candidate = 271`
  - `candidate_rejected = 63`
  - `effective_active_ratio_percent = 30.5613`
  - `promotion_rate = 0.6774`
  - `reject_rate = 0.3226`
- 当前剩余问题：
  - `hot_regions` 仍为空
  - active 仍主要集中在 `github_speedx_http`
  - 还需要新的 30 分钟 prod-live 长跑把这轮提分固化进长期报告

### 2026-04-12 晚间补充：持续 hygiene 与错误模式门禁
- 已把周期性 hygiene 接入 `scripts/proxy_real_longrun_driver.py`，`prod_live` 默认开启。
- 已把 hygiene 结果接入 `scripts/proxy_longrun_report.py`，现在长跑报告会显示：
  - `hygiene_total`
  - `hygiene_deleted_proxy_rows_total`
  - `hygiene_ratio_before_last / hygiene_ratio_after_last`
- 远端 240 秒验证结果：
  - harvest 曾把池从 `491` 拉到 `3264`
  - hygiene 自动删掉 `6066` 条低价值记录后把 latest 池重新压到 `481`
  - latest `effective_active_ratio_percent = 32.4324`
  - median `effective_active_ratio_percent = 31.7719`
  - browser `12/12` 成功
  - `stateful_continuity_observed = true`
- 已补两层错误模式门禁：
  - `proxy_real_longrun_driver.py` 拒绝 `prod_live + repo demo config`
  - `proxy_mainline_verify.sh` 拒绝 `prod_live + PROXY_VERIFY_REAL_ALLOW_DEMO=1`

### 2026-04-12 深夜补充：geo enrichment 与区域 demand 已打通
- 本地新增 `scripts/prod_proxy_geo_enrich.py`，并已同步远端。
- 远端实跑：
  - `python3 scripts/prod_proxy_geo_enrich.py --db data/persona_pilot.db --mode prod_live --config /root/prod_proxy_sources.controlled.json --only-status active --limit 200 --apply`
- 实际结果：
  - `updated_proxy_rows = 176`
  - `updated_source_rows = 3`
  - active region 从 `0` 个已知值提升到 `13` 个
  - top regions 已覆盖 `ap-southeast / eu-west / cn / jp / us-east / us-west`
- 已继续改 `scripts/proxy_real_longrun_driver.py`：
  - 增加周期性 geo enrich
  - 增加 `--auto-browser-regions-from-db`
  - browser event 现在会记录 `requested_region` 与 `hot_regions_during_request`
- 已继续改 `scripts/proxy_longrun_report.py`：
  - 新增 `browser_requested_regions`
  - 新增 `browser_hot_regions_observed`
  - 新增 `geo_enrich_active_regions_after_last`
- 已补 `scripts/prod_proxy_pool_hygiene.py` 的 metadata merge 保护：
  - config 中若 `expected_geo_quality` 仍是 `unknown`，不再覆盖已落地的 `host_geo_inferred`
- 远端 120 秒验证结果：
  - `reports/proxy_real_longrun_geo_hot_latest.txt|json`
  - `effective_active_ratio_percent median = 33.0357`
  - browser `8/8` 成功
  - continuity 继续为真
  - `browser_requested_regions = ap-southeast=4, eu-west=2, cn=2`
  - `browser_hot_regions_observed = ap-southeast=4, eu-west=2, cn=2`
- 当前结论：
  - latest `/status.hot_regions` 仍可能为空，但这已明确是**采样语义问题**，不是 region demand 没打到
  - 现在报告侧已经能证明 in-flight 热区需求真实存在


### 2026-04-12 ????????????????????????
- ?? continuity control-plane ??????????????
  - `heartbeat` ???????
  - `manual gate` 6 ?????
  - continuity snapshot ????
  - `StorePlatformOverride` ?????????
  - Telegram ?? + 30 ????
- ?? runtime mode ??? `AppState`??????????????????????
- `/status.proxy_pool_status` ? `verify_batch` ???? mode-aware source eligibility?
- explainability ??????????
  - unresolved proxy ????? execution identity
  - Lightpanda runner ? runtime fingerprint explain ??? control-plane fallback explain
- Lightpanda Windows `.sh` ?????? runner ???????timeout / non-zero-exit / cancel ??????
- ?????????
  - `cargo test --test integration_continuity_control_plane` pass
  - `cargo test --tests` pass
- ?????????
  - ?? dead-code warning
  - ??? sample-ready ??? 30/90 ? persona health ?????????

## 2026-04-13

### ???
- ???? continuity / proxy-harvest warning debt ???
  - `src/api/handlers.rs` ?????? persona/network-policy ????
  - `src/network_identity/proxy_harvest.rs` ?? `HarvestSourceRow` ??????????? source ?? SQL
- ????????? DTO / API contract?????????????

### ??
- `cargo test --tests` ??
- `cargo build --release` ??

### ????
- ????/?????????????? warning / ?????????
- B-010 ???????????????????????? mode-aware legacy source ????????


### 2026-04-13 本地主链路 correctness / status / geo 收口
- 已把这一轮 95+ 计划里的第一批控制面改造正式落到本地 Rust 主链路：
  - `tasks` typed 列：`proxy_id` / `requested_region` / `proxy_mode`
  - `/tasks`、verify batch、replenish batch 全部显式写入 typed 列
  - verify / replenish 事务内显式 claim，减少 claim-lost / proxy-not-found 风险
  - `/status.proxy_pool_status` 增加 `recent_hot_regions`、source concentration、active inventory 指标
  - source summary 增加 `declared_geo_quality / effective_geo_quality / geo_coverage_percent`
  - hygiene 脚本保护 in-flight / recent-used / sticky-bound proxy
- 额外修复：
  - mode-aware 集成测试不再依赖易串台的进程级 env 竞态，改为线程内 runtime mode override
  - `runner::engine` 单测已同步最新 verify-source 权重：`local_verify > runner_verify > geoip_host_enrich > imported/manual/backfill`
- 本地验证结果：
  - `cargo test --test integration_api` 通过（125/125）
  - `cargo test --tests` 通过
  - `cargo build --release` 通过
- 当前结论：
  - 本地主链路 correctness / status 口径已明显收口
  - 下一步重点已转到远端 `prod-live` 30 分钟验收、source/provider 均衡度、release gate 95+ 指标固化

### 2026-04-13 release/report 门禁继续收口
- 已继续把 95+ 计划里的 release/report 指标内生化到脚本主链：
  - `proxy_longrun_report.py` 现在会输出 `browser_success_rate_percent`、`browser_proxy_not_found_failures`、`recent_hot_regions_union`、`source_concentration_top1_percent`、`effective_geo_quality_summary`
  - `proxy_real_longrun_driver.py` 的 browser 事件现在会记录 `recent_hot_regions_during_request`、`browser_failure_signal`、`selection_reason_summary`、`error_message`
  - `release_baseline_verify.sh` 的 `prod-live` gate 现在会直接读取 longrun report 并检查：effective ratio / promotion / browser success / proxy-not-found / recent hot regions / source concentration
- 已新增或落稳的 reason code：
  - `proxy_claim_lost`
  - `hot_region_window_empty`
  - `source_concentration_too_high`
  - `geo_coverage_too_low`
- 本地脚本验证：
  - `python -m py_compile scripts/proxy_longrun_report.py scripts/proxy_real_longrun_driver.py`
  - `bash -n scripts/release_baseline_verify.sh`
  - `bash -n scripts/proxy_mainline_verify.sh`
  - `bash -n scripts/lightpanda_verify.sh`
  - 合成 longrun 样本已验证新增 JSON/TXT 字段与 proxy-not-found 统计生效
- 当前下一步：
  - 去远端真实 `prod-live` 数据面跑 30 分钟，观察新 gate 的真实失败分布
  - 若失败集中在 concentration / geo coverage / provider diversity，再继续做数据面而不是补脚本表层

### 2026-04-13 脚本级回归也已补上
- 已把 `prod-live` 报告门禁提取到 `scripts/release_prod_live_gate.py`
- 已新增 `scripts/tests/`：
  - `test_proxy_longrun_report.py`
  - `test_release_prod_live_gate.py`
- 本地验证新增：
  - `python -m unittest discover -s scripts/tests -p "test_*.py"` 通过（6/6）
- 这意味着这轮新增的 longrun / release 关键门禁字段已经不再只是 ad-hoc 验证，而有固定脚本级回归兜底

### 2026-04-13 continuity control-plane：warning/技术债收口 + 小红书 sample_ready 打透
- 本轮代码面完成：
  - 修正 `handlers.rs` 中 platform template 解析类型漂移
  - 修正 heartbeat 事件签名扩展后的遗漏 call-site
  - 修正 `runner::engine` 中 continuity probe 结果注入时的可变/不可变借用冲突
  - 修正 `integration_continuity_control_plane` 小红书模板 seed 与 canonical bootstrap 的唯一键冲突
  - 修正 `integration_api` 使用时间戳库名导致的并发 DB 路径碰撞，改为 `UUID`
  - `init_db` 已补 `busy_timeout + WAL`
- 本轮能力面完成：
  - canonical 小红书 `sample_ready` 模板会在 fresh DB 自动落地
  - `xiaohongshu + sample_ready` heartbeat 已改为 `extract_text`
  - `/dashboard` 与 `/notes` 已按 round-robin 调度
  - 5 个 continuity checks 已在运行时执行：
    - `login_state`
    - `identity`
    - `region`
    - `dashboard`
    - `notes`
  - continuity probe 证据会进入：
    - `continuity_events.event_json`
    - `persona_health_snapshots.snapshot_json`
- 本轮新增测试：
  - `runner::engine` sample-ready continuity probe 成功/失败单测
  - `integration_continuity_control_plane` fresh DB bootstrap / round-robin / snapshot 字段验证
  - `integration_api` JSON 失败输出增强，后续定位会更直接
- 本轮验证结果：
  - `cargo check` 通过
  - `cargo build --release` 通过
  - `cargo test --lib` 通过
  - `cargo test --test integration_continuity_control_plane` 通过
  - `cargo test --tests` 通过
- 当前下一步：
  - 继续把同一套 continuity 模板与 probe 逻辑推广到 Shopify / 独立站后台
  - 再推进 Amazon / eBay

### 2026-04-13 source / region balance 控制面落地
- 已把 source / region balance 直接接到 Rust 控制面选池逻辑：
  - verify batch 会优先 underrepresented source
  - replenish global batch 会优先 underrepresented source
  - recent hot regions / underrepresented region 也进入排序
- 新增回归：
  - `verify_batch_prioritizes_underrepresented_source_when_top1_is_concentrated`
  - `replenish_tick_global_prioritizes_underrepresented_source_candidates`
- 全量验证：
  - `cargo test --test integration_api` 通过（127/127）
  - `cargo test --tests` 通过
  - `cargo build --release` 通过
- 这轮之后，本地剩余更像“真实数据面提分”而不是“控制面 correctness / 口径缺失”

### 2026-04-13 provider balance + hygiene top1 source 收紧
- 本轮继续把 95+ 计划里的下一层 balance 真正落到本地主链：
  - `src/api/handlers.rs`
    - `ActiveProxyBalanceSnapshot` 新增 `active_by_provider / top1_provider_key`
    - `sort_balance_candidates()` 现在同时吃 source/provider/region/recent-hot-region
    - `verify_batch` / `replenish` SQL 预排序也开始感知 active provider inventory
  - `scripts/prod_proxy_pool_hygiene.py`
    - 新增 top1 source concentration summary
    - dominant source 触发 concentration 时自动收紧 `candidate_keep_limit`
    - underrepresented non-top1 source 允许保留更宽松 candidate
    - `apply_result` 新增 `deleted_proxy_rows_by_source`
- 本轮新增回归：
  - Rust：
    - `verify_batch_prioritizes_underrepresented_provider_when_source_balanced`
    - `replenish_tick_global_prioritizes_underrepresented_provider_candidates`
  - Python：
    - `scripts/tests/test_prod_proxy_pool_hygiene.py`
- 本轮还顺手修掉一个会卡全量回归的既有单测漂移：
  - `runner::engine` sample-ready 默认 identity marker 顺序改为优先 `seller`
  - `evaluate_sample_ready_continuity_probe_reports_dashboard_success` 恢复通过
- 本轮本地验证结果：
  - `cargo test --test integration_api` 通过（129/129）
  - `cargo test --tests` 通过
  - `cargo build --release` 通过
  - `python -m unittest discover -s scripts/tests -p "test_*.py"` 通过（8/8）
  - `python -m py_compile scripts/prod_proxy_pool_hygiene.py scripts/release_prod_live_gate.py scripts/proxy_longrun_report.py scripts/proxy_real_longrun_driver.py` 通过
  - `bash -n scripts/release_baseline_verify.sh` 通过
  - `bash -n scripts/proxy_mainline_verify.sh` 通过
  - `bash -n scripts/lightpanda_verify.sh` 通过
- 当前主线继续收缩为远端真实数据面：
  - 远端 `prod_live` 30 分钟长跑
  - provider/source concentration 是否真实下降
  - hygiene keep-cap 是否足够把 dominant source 压回目标线
  - 若要继续冲 95+，仍需至少 2 个独立 private/paid provider

### 2026-04-13 小红书 sample_ready：identity markers / event / snapshot 闭环补强
- 本轮继续只聚焦小红书，把 `sample_ready` 从“已有 5 信号 continuity checks”推进到“店铺级 identity continuity 真闭环”：
  - schema 与迁移已新增：
    - `platform_templates.identity_markers_json`
    - `store_platform_overrides.identity_markers_json`
  - DTO / CRUD / persona resolve / heartbeat task payload 全链已接入这 2 个字段
- 本轮修掉的真实问题：
  - `create_store_platform_override` 的 SQL values 个数比列数多 1，导致 API 500；现已修正
  - `persona_health_snapshots` 在最近窗口里如果最新 probe 行没有 marker，会丢掉更早 probe 的 `matched_identity_marker`；现已改为支持回填最近非空 marker
- 本轮新增回归：
  - `platform_template_crud_roundtrips_identity_markers_json`
  - `store_platform_override_crud_roundtrips_identity_markers_json`
  - `xiaohongshu_store_identity_markers_flow_into_probe_event_and_snapshot`
- 本轮小红书能力面新增结果：
  - store override 可配置严格 identity marker
  - continuity probe 会输出 `matched_identity_marker`
  - `continuity_events.event_json` 与 `persona_health_snapshots.snapshot_json` 都能看到该 marker
  - `skipped_checks` 继续保留并进入 snapshot 汇总
- 本轮验证结果：
  - `cargo check` 通过
  - `cargo build --release` 通过
  - `cargo test --lib` 通过
  - `cargo test --test integration_continuity_control_plane` 通过（8/8）
  - `cargo test --tests` 通过
- 这轮之后，小红书样板平台可视为已完成第二阶段打透；后续只再做“精度增强”，而不是补主链路空洞。

### 2026-04-14 prod-live real-live blocker 拆除 + continuity / hygiene / gate 收口
- 远端真实 blocker 已确认不是 control plane binary，而是脚本漂移：
  - 旧版 `scripts/proxy_real_longrun_driver.py` 仍硬依赖 `/behavior-profiles`
  - 旧版 `scripts/proxy_mainline_verify.sh` 还在传 `--behavior-profile-id / --persona-id / --form-stateful-url`
- 2026-04-14 已把远端 real-live 主入口重新收口到当前兼容主线：
  - `bash scripts/proxy_mainline_verify.sh real-live` 重新可跑
  - `120s prod_live real-live` 已连续产出最新 `reports/proxy_real_longrun_latest.{txt,json}`
- 本轮继续增强了 short-run continuity 证明强度：
  - `scripts/proxy_real_longrun_driver.py`
    - 新增 `stateful_followup_count`
    - 新增 `sticky_stateful_region`
    - 新增 `PROXY_REAL_LONGRUN_HYGIENE_EXTRA_ARGS`
  - `scripts/proxy_longrun_report.py`
    - `stateful_followup*` 已并入 stateful browser 统计
- 2026-04-14 远端 short real-live 现已稳定观测到：
  - `browser_success_rate_percent = 100%`
  - `browser_proxy_not_found_failures = 0`
  - `recent_hot_regions = ap-southeast, cn, eu-west`
  - `stateful_continuity_observed = true`
  - `browser identity_session_statuses = auto_reused=9`
  - `cookie/local/session storage restore + persist` 均为正
- 2026-04-14 远端 aggressive hygiene keep-cap 实测结果：
  - `effective_active_ratio_percent median = 43.1174`
  - `promotion_rate median = 0.7802`（即 78.02%）
  - `reject_rate median = 0.2198`
  - `eligible_pool_total = 494`
  - `candidate = 228`
  - `candidate_rejected = 53`
- 2026-04-14 还修掉了一个真实发布门禁 bug：
  - `scripts/release_prod_live_gate.py` 之前把 `promotion_rate=0.7802` 按 0.7802% 处理
  - 现已归一到：
    - `0~1` ratio -> 自动乘 100
    - `0~100` percent -> 原样使用
  - 新增脚本回归：fractional promotion rate 也必须正确通过
- 截至 2026-04-14，这轮 prod-live 最新 gate 已明确只剩一个未过项：
  - `source_concentration_too_high`
  - 当前 `source_concentration_top1_percent = 87.79`
  - 这说明本轮后续若想再冲 95+，重点已不在脚本兼容或 continuity，而在**真实 provider/source 供给去集中化**。

### 2026-04-14 小红书 sample_ready：precision hardening 收口
- 本轮继续只打小红书精度，不扩平台。
- 代码面新增/收口：
  - `detect_login_loss_signal()` 现在只看页面可见证据，不再扫描整份 `result_json`
  - `continuity_probe_haystack()` 现已纳入 `message / error_message`
  - `configured_identity_markers / identity_markers_source` 已正式进入：
    - `continuity_check_result`
    - `continuity_events.event_json`
    - `persona_health_snapshots.snapshot_json.last_continuity_check_results`
  - `integration_continuity_control_plane` 的 snapshot helper 已改为查最近窗口中的带 marker 快照，收口 full `cargo test --tests` 下的时间窗竞争
- 本轮新增/增强回归：
  - `evaluate_sample_ready_continuity_probe_does_not_trigger_login_from_config_echo`
  - `xiaohongshu_store_identity_markers_flow_into_probe_event_and_snapshot` 现已断言：
    - payload 中的 `identity_markers_source`
    - probe 中的 `configured_identity_markers / identity_markers_source`
    - event / snapshot 中的同名字段
- 本轮验证结果：
  - `cargo check` 通过
  - `cargo build --release` 通过
  - `cargo test --lib` 通过（62/62）
  - `cargo test --test integration_continuity_control_plane` 通过（8/8）
  - `cargo test --tests` 通过
- 结论：
  - 小红书当前已不再只是“能识别 marker”，而是“能解释 marker 来自哪里、当前用了哪些 marker、并避免登录信号假阳性”。

### 2026-04-14 prod-live / real-live gate 入口继续解耦
- 本轮继续提分做了发布主入口拆分，避免 prod-live 被 real-live 连带失败污染：
  - `scripts/proxy_mainline_verify.sh` 新增 `prod-live` 子命令
  - `prod-live` 子命令会强制 `--mode prod_live`，并调用 `scripts/release_prod_live_gate.py`
  - `real-live` 子命令继续调用 `scripts/release_real_live_gate.py`
- `scripts/release_baseline_verify.sh --profile prod-live` 已切换为调用：
  - `bash scripts/proxy_mainline_verify.sh prod-live`
  - 并新增 `RELEASE_VERIFY_PROD_LIVE_MIN_SAMPLE_COUNT` -> `--min-sample-count` 透传
- 现在两条门禁失败口径可稳定拆分：
  - prod-live: `prod_live_sample / proxy_pool / hot_regions / source_concentration...`
  - real-live: `real_live_sample / continuity / proxy_pool...`
- 本轮本地验证：
  - `bash -n scripts/proxy_mainline_verify.sh` 通过
  - `bash -n scripts/release_baseline_verify.sh` 通过
  - `python -m unittest discover -s scripts/tests -p "test_*.py"` 通过（17/17）
  - `python -m py_compile scripts/release_prod_live_gate.py scripts/release_real_live_gate.py scripts/proxy_longrun_report.py scripts/proxy_real_longrun_driver.py` 通过

### 2026-04-14 release 摘要统一 + stale prod-live 报告污染修复
- 本轮继续把 release/report 主链做成可测而不是 shell 粘贴逻辑：
  - 新增 `scripts/release_report_summary.py`
  - 统一从以下 artifact 渲染 release 摘要：
    - `proxy_real_longrun_latest.json`
    - `source_quality_summary_latest.json`
    - `session_continuity_summary_latest.json`
    - `real_live_evidence_summary_latest.json`
- `scripts/release_baseline_verify.sh` 已切到调用该 helper，不再内联 Python 拼 prod-live 指标。
- 这轮顺手修掉一个真实口径 bug：
  - 旧逻辑只要检测到本地存在 `proxy_real_longrun_latest.json`
  - 就会在 `public-smoke` / `gateway-upstream` 报告里误带历史 prod-live 指标
  - 现在 prod-live 摘要只会写入 `--profile prod-live` 的 release 报告
- 新增脚本回归：
  - `scripts/tests/test_release_report_summary.py`
- 本轮本地验证：
  - `python -m unittest discover -s scripts/tests -p "test_*.py"` 通过（20/20）
  - `python -m py_compile scripts/release_report_summary.py scripts/release_prod_live_gate.py scripts/release_real_live_gate.py scripts/proxy_longrun_report.py scripts/proxy_real_longrun_driver.py` 通过
  - `bash -n scripts/proxy_mainline_verify.sh` 通过
  - `bash -n scripts/release_baseline_verify.sh` 通过

### 2026-04-14 real-live host-level evidence 继续提分
- 本轮继续把 `real_live_evidence_summary` 从“总量摘要”补到“host 可审计摘要”：
  - `proxy_longrun_report.py` 新增：
    - `extract_event_host()`
    - `build_site_host_evidence()`
  - `build_real_live_evidence_summary()` 现会写入：
    - `site_host_count`
    - `stateful_site_host_count`
    - `continuity_ready_site_host_count`
    - `continuity_ready_site_hosts`
    - `site_host_summaries`
- `render_real_live_evidence_text()` 现会直接打印 host-level 摘要，便于 operator 不看原始 JSON 也能判断：
  - 哪些 host 真跑起来了
  - 哪些 host 看到 continuity chain
  - 每个 host 的 region / recent hot region / proxy 多样性
- `release_report_summary.py` 已继续接出 host-level evidence 字段：
  - `real_live_site_host_count`
  - `real_live_stateful_site_host_count`
  - `real_live_continuity_ready_site_host_count`
  - `real_live_continuity_ready_site_hosts`
- 本轮本地验证：
  - `python -m py_compile scripts/proxy_longrun_report.py scripts/release_report_summary.py scripts/release_prod_live_gate.py scripts/release_real_live_gate.py scripts/proxy_real_longrun_driver.py` 通过
  - `python -m unittest discover -s scripts/tests -p "test_*.py"` 通过（20/20）
  - `bash -n scripts/proxy_mainline_verify.sh` 通过
  - `bash -n scripts/release_baseline_verify.sh` 通过
  - `cargo test --tests` 通过（216 个测试全部通过）
  - `cargo build --release` 通过
- 用现有本地 artifact 再跑 `python scripts/release_report_summary.py ...` 已确认 host-level 字段可被 release 摘要读取。
- 当前结论：
  - 脚本/报告/门禁层的“real-live 证据厚度”已经明显增强
  - 但最新本地 `reports/proxy_real_longrun_latest.json` 样本量仍只有 `1`
  - 所以这证明的是**契约与可审计结构已到位**，不是远端 30 分钟真实样本已经自然达到 92+ 背书线

### 2026-04-14 prod-live 稳态默认化 Phase 1
- 本轮按“无 private/paid provider 阶段的 prod_live 稳态默认化计划”完成了本地脚本收口：
  - 新增 `scripts/prod_live_presets.py`
  - 正式引入 `legacy / stable_v1` 双 preset
  - `stable_v1` 把当前验证有效的一组 hygiene + continuity + geo 参数写入 repo 单一真源
- `scripts/proxy_real_longrun_driver.py` 现已支持：
  - `--preset`
  - raw payload 固定写出 `preset`
  - `stateful_followup_count / auto_browser_regions_from_db / geo_enrich / pool_hygiene / pool_hygiene_extra_args`
    统一按 `命令行 > env override > preset` 解析
- `scripts/proxy_mainline_verify.sh` 现已支持：
  - `PROXY_VERIFY_REAL_PRESET=legacy|stable_v1`
  - `real-live / prod-live` 共用同一 preset 入口
- 本轮新增 maintenance 入口：
  - `bash scripts/prod_live_maintenance_tick.sh --preset stable_v1`
  - 该入口会按与 longrun 一致的 preset 参数跑：
    - `prod_proxy_geo_enrich.py`
    - `prod_proxy_pool_hygiene.py`
- `scripts/release_prod_live_gate.py` 本轮升级为双结论：
  - `strict_verdict`
  - `operational_verdict`
  - `provider_supply_class`
  - `provider_cap_reason`
- 当前语义固定为：
  - 严格 95+ gate 继续保留
  - 若 supply class 为 `lab_only` 且唯一失败项是 `source_concentration_too_high`
    则运营结论允许 `provider_capped`
- `scripts/proxy_longrun_report.py` / `scripts/release_report_summary.py` 已把以下字段打透到 artifact：
  - `preset`
  - `provider_supply_class`
  - `strict_verdict`
  - `operational_verdict`
  - `provider_cap_reason`
- 本轮本地验证结果：
  - `python -m py_compile scripts/prod_live_presets.py scripts/proxy_real_longrun_driver.py scripts/proxy_longrun_report.py scripts/release_prod_live_gate.py scripts/release_report_summary.py` 通过
  - `python -m unittest discover -s scripts/tests -p "test_*.py"` 通过（29/29）
  - `bash -n scripts/proxy_mainline_verify.sh` 通过
  - `bash -n scripts/prod_live_maintenance_tick.sh` 通过
  - `bash -n scripts/release_baseline_verify.sh` 通过
- 当前结论：
  - 本地“preset 默认化 + maintenance 入口 + 双结论 gate + release/report 透出”已完成
  - 仍未做远端同步与 `stable_v1` 短跑 / 30 分钟验收
  - 因此 Phase 2 默认切换（把默认 preset 从 `legacy` 切到 `stable_v1`）仍未开始

### 2026-04-14 prod-live legacy-status compatibility + remote provider-capped verification
- 本轮继续推进远端 Phase 1，不再停在“本地已完成”：
  - 已同步远端：
    - `scripts/proxy_longrun_report.py`
    - `scripts/proxy_real_longrun_driver.py`
    - `scripts/release_prod_live_gate.py`
    - `scripts/preflight_release_env.sh`
- 先确认了一个关键事实：
  - 在线 `127.0.0.1:3000` 进程环境已经是 `PERSONA_PILOT_PROXY_MODE=prod_live`
  - 但 `/status` 仍是旧 contract，缺少 `mode / recent_hot_regions / source_concentration_top1_percent`
- 因此本轮补了兼容修复，避免旧 contract 误判：
  - preflight 在 `/status.mode` 为空时回退读 `port 3000` 进程环境
  - report 在 status snapshot 缺 `mode` 时回退 raw payload `mode`
  - report 在 status snapshot 缺 `recent_hot_regions` 时回退 browser event
  - report 在 status snapshot 缺 concentration 字段时回退 `source_summaries[].active_count`
  - driver 在旧 `/status` 缺 `recent_hot_regions` 时回填本次 `requested_region`
- 本地回归更新后通过：
  - `python -m unittest discover -s scripts/tests -p "test_*.py"` -> `33/33`
  - `python -m py_compile scripts/proxy_longrun_report.py scripts/proxy_real_longrun_driver.py scripts/release_prod_live_gate.py`
  - `bash -n scripts/preflight_release_env.sh`
  - `bash -n scripts/proxy_mainline_verify.sh`
- 远端先跑：
  - `bash scripts/prod_live_maintenance_tick.sh --preset stable_v1`
  实际结果：
  - `active_total = 362`
  - `candidate_total = 260`
  - `rejected_total = 43`
  - `estimated_effective_active_ratio_percent = 54.4361`
  - `source_concentration_top1_percent = 87.8453`
- 随后跑 `120s stable_v1 prod-live`，并把 `PROXY_VERIFY_REAL_STATUS_INTERVAL_SECONDS` 调到 `20`，避免默认 `60s` 只采到 `3` 个样本：
  - `summary.mode = prod_live`
  - `sample_count = 7`
  - `effective_active_ratio_percent median = 54.0`
  - `promotion_rate median = 88.51%`
  - `browser_success_rate_percent = 100.0`
  - `browser_proxy_not_found_failures = 0`
  - `recent_hot_regions = ["ap-southeast", "cn", "eu-west"]`
  - `source_concentration_top1_percent_max = 87.8453`
  - `strict_verdict = fail`
  - `operational_verdict = provider_capped`
  - `provider_cap_reason = source_concentration_too_high`
- 这说明当前“无 private/paid provider 阶段”的目标已经真实落地：
  - 严格 95+ gate 继续保留
  - 当前运营结论会明确落成 `provider_capped`
  - 当前真正剩余项仍只有供给结构去集中化，不是脚本或 mode/gate 口径错误
### 2026-04-15 local continuity control-plane runtime closure
- restored the minimum mainline continuity compatibility slice across schema, DTO, handlers, and runner
- fixed the runtime miss where terminal `tasks.result_json` could finish without `continuity_check_result`
- root cause was legacy slash-escaped JSON in seeded template fields; handlers now parse that legacy shape compatibly instead of silently falling back to `[]`
- verification:
  - `cargo check -q`
  - `cargo test --no-run -q`
  - `cargo build --release -q`
  - `cargo test --test integration_continuity_control_plane -- --nocapture`
- boss-view impact:
  - local status moves from "continuity gap narrowing" to "continuity local closure complete"
  - next-stage execution order now starts from remote acceptance, gateway upstream, and behavior active-path promotion
