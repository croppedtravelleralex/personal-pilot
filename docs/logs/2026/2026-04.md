# 2026-04 Log

Updated: 2026-04-17 (Asia/Shanghai)

## 2026-04-17 Mainline Closeout Complete

- `mainline delivery = 100% / 0% / green`
- `overall end-state = 35% / 65% / yellow`
- closed `A1 + A2 + A3 + A4` with full gate green
- older `95% / 7%`, `30% / 70%`, and `77% / 23%` references remain historical only
## 2026-04-16 双轴口径统一

- 当前 live truth 统一为双轴：
  - `mainline delivery = 95% / 7% / green`
  - `overall end-state = 30% / 70% / yellow`
- 明确 `50+` 指纹控制现在只表示最低控制面门槛；当前 first-family 已声明 `80` 个核心控制字段
- 明确当前 `Lightpanda` 运行时只实际投影 `12` 个 env-backed 指纹字段
- 明确当前行为运行时只有 `13` 个 shipped primitives
- 明确 cookie / localStorage / sessionStorage continuity 已支持关软件重启后恢复
- 明确 `450+` 指纹信号与 `450+` 事件类型都属于整体终局目标，不是当前已交付 runtime 深度
- 明确 AdsPower 追评与外部浏览器整合方案属于 overall `70%` 轨道，不属于当前 mainline `7%` closeout
- 先前日志中的 `85% / 15%` 与 `77% / 23%` 仅保留为历史过程记录，不再作为 live truth source

## 2026-04-16 Delivery Delta

- whole-app progress moved to `85%`
- remaining delivery slice moved to `15%`
- acceptance remains `yellow`
- passed again in this round: `pnpm typecheck`, `pnpm build`, Win11 baseline, `cargo test --lib -- --test-threads=1`, `cargo check --manifest-path src-tauri/Cargo.toml`, `pnpm desktop:release`
- still open: `cargo test --quiet` with 13 failing integration cases

## 2026-04-15 本地 Windows 主线重置

- 主维护入口切回本地 `Windows 11`
- 固定 `Tauri 2 + Vite + React + TypeScript`
- 固定单窗口、单实例、统一 `desktop service` 边界
- 新增本地验证脚本与桌面方案文档

## 2026-04-15 Tauri 桌面壳落地

- 新增 `src-tauri/`
- 新增 `src/services/desktop.ts`
- 初步落地 `Overview / Tasks / Logs / Settings`
- 建立 Win11 Tauri 基线检查

## 2026-04-16 共享桌面底座推进

- Runtime 启停命令落地
- Runtime settings `apply / restore` 落地
- Local API `read / apply / restore` 落地
- Browser environment policy `read / apply / restore` 落地
- Local asset workspace / import-export skeleton 落地
- Profiles 批量动作接入 desktop / tauri
- Proxies batch check 接入真实 native 路径
- Recorder 切到 native-first `read / start / stop`
- `compileTemplateRun` 接入真实 native compile manifest 写入
- Synchronizer 基础命令 `snapshot / main / layout / focus` 落地

## 2026-04-16 三线整合收口

- 复核 Automation / Synchronizer / 本地平台底座三条线的真实实现
- 修正 Automation 旧口径：`compileTemplateRun` 不再视为 blocked
- 修正 Synchronizer 旧口径：desktop snapshot / desktop commands 为主路径，local matrix 只是 fallback console
- 统一 Templates / Recorder / Automation / Synchronizer 的 `sourceMessage / banner / feedback` 文案
- 回写维护文档，按代码事实更新当前状态、roadmap、完成度拆分

## 2026-04-16 全量验证

已通过：

- `pnpm typecheck`
- `pnpm build`
- `pnpm desktop:release`
- `powershell -ExecutionPolicy Bypass -File scripts/windows_local_verify.ps1`
- `powershell -ExecutionPolicy Bypass -File C:\Users\Lenovo\.codex\templates\win11-tauri-vite-react-ts\scripts\enforce-win11-tauri.ps1 -ProjectRoot D:\SelfMadeTool\persona-pilot`

非阻塞提醒：

- 部分 Rust warnings 仍待清理
- Vite chunk size warning 仍在

## 2026-04-16 全量体检口径重置

- 按“完整可交付 Win11 本地 App”重新做了一次全仓体检，不再沿用最近局部任务的进度外推
- 新默认口径改为：
  - whole-app progress `77%`
  - remaining delivery slice `23%`
  - AdsPower 对比不再报固定百分比
- 明确废弃旧的 `82% / 18% / 90% / 99%` 作为当前 truth source
- 当前主线顺序改为：
  - `Proxy / IP`
  - `Synchronizer`
  - `Recorder / Templates`
  - `Tasks route decision`
  - `warning cleanup / release hardening`

## 2026-04-16 本轮同步热更新

- 新增 `docs/17-full-app-audit-progress-reset.md`
- 同步更新：
  - `README.md`
  - `docs/README.md`
  - `docs/02-current-state.md`
  - `docs/final-goal-progress-breakdown.md`
  - `docs/03-roadmap.md`
  - `docs/04-improvement-backlog.md`
  - `docs/05-ai-maintenance-playbook.md`
  - `docs/12-final-18-percent-delivery-plan.md`
  - `docs/13-adspower-deep-comparison.md`
  - `CURRENT_TASK.md`
  - `STATUS.md`
  - `PROGRESS.md`
  - `TODO.md`

## 当前剩余主线

1. `provider-grade proxy IP rotation`
2. `synchronizer native batch / broadcast`
3. `recorder / templates de-fallback`
4. `tasks route / surface unification`
5. `warning cleanup / release hardening`
## 2026-04-16 全量体检复验增量

- 重新执行并确认通过：
  - `pnpm typecheck`
  - `pnpm build`
  - `powershell -ExecutionPolicy Bypass -File C:\Users\Lenovo\.codex\templates\win11-tauri-vite-react-ts\scripts\enforce-win11-tauri.ps1 -ProjectRoot D:\SelfMadeTool\persona-pilot`
  - `cargo build --release -q`
- 重新执行并确认未全绿：
  - `cargo test --lib -- --test-threads=1`
  - `cargo test --quiet`
- 新结论：
  - 当前整 app 默认进度口径继续维持 `77% / 23%`
  - 但 `verification / acceptance` 需要下调为 `yellow`
  - 当前主要噪音来自 `humanize::*` 随机失败与 `database is locked`
