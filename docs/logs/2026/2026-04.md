# antbrowser 2026-04 维护日志

## 2026-04-29

- 目标：初始化维护文档骨架
- 观察到的事实：已创建 `docs/` 维护计划书结构，正文待结合代码与现有文档补齐
- 已完成：补齐维护目录与基础文件
- 未完成：基于仓库事实回写完整内容
- 下一步：阅读代码、配置、README 和已有文档后更新各主档
- 关联文件/命令：`scripts/bootstrap_maintenance_docs.py`

## 2026-04-29 行为录制技术评估入档

- 目标：把行为录制待修复、待完善、待改进事项写入长期维护文档
- 观察到的事实：行为录制已具备 MVP 骨架，但点击重复、跨页面丢事件、会话恢复未初始化、LLM 执行与录制语义不一致等问题阻止其被标记为完善
- 已完成：新增 `docs/06-behavior-recording-technical-guidance.md`，并更新现状、路线图、改进池和 AI 接手规则
- 未完成：尚未开始代码修复
- 下一步：优先执行行为录制 P0 修复，并补最小验证
- 关联文件/命令：`go test ./backend/internal/behavior`、`npm --prefix frontend run build`、`go test ./backend/...`

## 2026-04-29 非容器化实例工作台 MVP

- 目标：实现软件内实例内容可视化管理、API 操作和自动化任务编排，不做浏览器容器化。
- 观察到的事实：现有 `SynchronizerPage` 已有运行中分组和广播导航/刷新基础；`BrowserInstanceOpenUrl` 原为占位实现。
- 已完成：升级 `/browser/synchronizer` 为“实例工作台”；新增卡片化实例管理、搜索/状态/分组筛选、批量启动/停止/打开 URL/刷新/截图、单实例操作、截图预览和前端任务队列；后端新增单实例导航/刷新/截图 CDP 接口，并修正 `BrowserInstanceOpenUrl`。
- 未完成：尚未实现 Windows 外部窗口激活/排列；任务队列仍为前端内存态；尚未做真实浏览器手工验收。
- 下一步：用 2 个真实实例验收导航、刷新、截图和批量任务，再补 PID/HWND 外部窗口激活。
- 关联文件/命令：`backend/app_synchronizer.go`、`backend/app_instance.go`、`frontend/src/modules/synchronizer/SynchronizerPage.tsx`、`npm --prefix frontend run build`、`go test ./backend ./backend/internal/behavior ./backend/internal/browser ./backend/internal/launchcode ./backend/test/launchcode`。

## 2026-04-29 行为录制 P0/P1 执行完成

- 目标：按技术指导完成行为录制 P0/P1 修复，并由子 agent 分片执行后主线程集成验收
- 观察到的事实：P0/P1 代码问题可在当时的 Wails/Go 架构内修复；Win11 Tauri 模板检查在该时间点因仓库历史结构缺少 `src/pages` 失败，后续已由新条目复核通过
- 已完成：修复点击重复回放、同 page target 刷新/导航重注入、session 目录初始化、LLM 录制语义、CDP 回放响应确认、store nil guard、target 选择、录制元数据、坐标缩放、滚动容器路径、输入/敏感保护、速度扰动、正负 jitter、自动录制写入错误；同时修复全仓测试中的 Darwin HOME 模拟路径和随机 typing 测试
- 未完成：尚未跑真实指纹浏览器端到端录制/回放；跨 target、iframe 仍是后续增强
- 下一步：用真实实例跑手工验收或 E2E，再推进 P2 状态/接口修复和 P3 长录制性能
- 关联文件/命令：`backend/internal/behavior/`、`backend/app_behavior.go`、`backend/app_llm.go`、`backend/internal/apppath/apppath.go`、`frontend/src/modules/browser/`、`go test -count=1 ./backend/internal/behavior`、`go test -count=1 ./backend -run "TestLlm|TestBehavior"`、`go test -count=1 ./backend/...`、`npm --prefix frontend run build`、`git diff --check`

## 2026-04-29 实例工作台外部窗口辅助控制

- 目标：在非容器化主线下补齐多实例切换体验，不改变浏览器 profile、指纹参数、debugPort 或真实顶层窗口身份。
- 观察到的事实：工作台 API 操作和截图预览已可用，但缺少按实例激活真实窗口、排列多个外部窗口的能力。
- 已完成：新增 Windows-only PID 顶层窗口查找、激活、平铺和主辅排列；前端增加激活首个、单实例激活、平铺、主辅按钮；Wails 绑定和维护文档已同步。
- 未完成：尚未用 2 个真实运行实例手工确认窗口置前/排列效果；多屏、最小化其他窗口和布局恢复仍是后续增强。
- 下一步：启动至少 2 个实例，验收导航、刷新、截图、激活、平铺/主辅排列和停止隔离。
- 关联文件/命令：`backend/app_synchronizer.go`、`backend/window_control_windows.go`、`backend/window_control_others.go`、`frontend/src/modules/synchronizer/SynchronizerPage.tsx`、`npm --prefix frontend run build`、`go test ./backend ./backend/internal/behavior ./backend/internal/browser ./backend/internal/launchcode ./backend/test/launchcode`。

## 2026-04-29 实例工作台少人工验收增强

- 目标：最大程度减少人工介入，补齐可自动化的工作台产品化缺口。
- 观察到的事实：任务队列此前只在前端内存中；实例列表此前以 200 条截断替代虚拟滚动；真实浏览器窗口验收仍需要本机实例，但 Win32 窗口控制层可用系统窗口自动验证。
- 已完成：新增后端任务持久化 `data/workbench/tasks.json`、前端任务恢复/保存、实例列表固定行高虚拟滚动、工作台持久化/布局单测、Notepad 窗口控制 E2E。
- 未完成：尚未启动真实指纹浏览器实例做 2 实例端到端验收；预览节流、多屏排列、最小化其他窗口和布局恢复仍是后续增强。
- 下一步：真实实例可用时执行 2 实例工作台验收；否则继续以后端 CDP/Win32 自动测试覆盖基础能力。
- 关联文件/命令：`backend/app_synchronizer.go`、`backend/app_synchronizer_test.go`、`backend/window_control_windows_e2e_test.go`、`frontend/src/modules/synchronizer/SynchronizerPage.tsx`、`ANTBROWSER_WINDOW_CONTROL_E2E=1 go test ./backend -run TestWindowControlE2EWithNotepad -count=1`、`npm --prefix frontend run build`。

## 2026-04-29 行为录制 P2/P3 文档与测试收口（历史口径，已被后续真实 E2E 收口覆盖）

- 目标：在不抢改代码实现的前提下，用当前代码和命令证据回写行为录制 P2/P3 状态。
- 观察到的事实：当时记录为后端全量测试、行为包测试、前端生产构建和 `git diff --check` 通过；真实浏览器用例 `TestAutoRecordNurturing` 因 `127.0.0.1:59222` 不可连接而 SKIP，不能算真实端到端验收。当前全量后端测试状态以后续最终验收条目为准。
- 已完成：当时文档已明确 P3 敏感输入脱敏为 Done；P2 错误码映射、前端 API 收口、自然语言任务事件 off、P3 产品化工具为部分完成；cleanup POST、统一 active recording 状态、list/detail 拆分、搜索 debounce/虚拟滚动、真实浏览器验收在该时间点仍未完成，后续已由新条目覆盖。
- 未完成：当时未改代码、未启动真实指纹浏览器实例、未跑真实录制/回放；后续已由“行为录制真实 E2E 与 P2/P3 收口完成”覆盖。
- 下一步：先补可重复真实浏览器验收，再推进 cleanup POST、active recording 状态统一和长录制性能切片。
- 关联文件/命令：`docs/06-behavior-recording-technical-guidance.md`、`docs/02-current-state.md`、`docs/03-roadmap.md`、`docs/04-improvement-backlog.md`、`docs/05-ai-maintenance-playbook.md`、`go test -count=1 ./backend/...`、`npm --prefix frontend run build`、`go test -count=1 ./backend/internal/behavior -run TestAutoRecordNurturing -v`、`git diff --check`。

## 2026-04-29 行为录制前端 P2/P3 切片（历史口径，后续已补后端/API）

- 目标：修复行为录制前端事件释放、API 收口、自然语言任务状态收敛，并补录制搜索和长列表/详情分页。
- 观察到的事实：后端暂未提供真实 metadata/events 分离接口；前端可先把现有完整 Recording 归一化为 summary 状态，并为未来 detail page API 保留兼容入口。
- 已完成：行为录制页改走 browser API；播放和 LLM 事件订阅改用 `EventsOn` 返回 off 函数释放；自然语言任务补 planning/executing/complete/failed/cancelled 状态收敛；录制搜索增加 300ms debounce；录制列表增加分页；详情事件列表增加分页并移除“最近 50 条”固定截断。
- 未完成：当时后端 cleanup POST、active recording 统一状态、真实 metadata/events 分离 API、真实浏览器端到端录制回放验收尚未补齐；后续已完成主线收口。
- 下一步：补后端 summary/detail API 后，把前端 fallback 从“兼容旧完整 Recording”切到真实按需加载。
- 关联文件/命令：`frontend/src/modules/browser/api.ts`、`frontend/src/modules/browser/types.ts`、`frontend/src/modules/browser/components/RecordingPanel.tsx`、`frontend/src/modules/browser/components/RecordingDetailModal.tsx`、`frontend/src/modules/browser/components/NaturalLanguageTask.tsx`、`frontend/src/modules/browser/pages/BehaviorRecordingPage.tsx`、`npm --prefix frontend run build`。

## 2026-04-29 行为录制后端 P2/P3 切片（历史口径，后续已补前端/API）

- 目标：修复录制 HTTP API 错误语义、cleanup 方法语义、active recording 状态暴露，以及录制 list/detail 后端拆分。
- 观察到的事实：当前仓库仍是 Wails/Go + Vite/React/TS；本轮不做 Tauri 迁移，不改前端 UI；仓库已有大量未提交改动，按增量修改处理。
- 已完成：新增 `RecordingSummary`、`ActiveRecordingStatus`、store typed errors；HTTP `/api/recording/list` 默认返回 metadata；`/api/recording/{id}` 继续返回完整 events；`/api/recording/status` 暴露 active/profileIds；cleanup 支持 POST 并保留 GET 兼容说明。
- 未完成：当时前端尚未切换到 `BehaviorRecordingSummaryList` / `BehaviorRecordingStatus`；后续已切换并补 `BehaviorGetRecordingDetail`。
- 下一步：前端服务层切到 summary/status 后再处理搜索 debounce、列表虚拟化和详情分页/虚拟化。
- 关联文件/命令：`backend/app_behavior.go`、`backend/internal/behavior/recording_store.go`、`backend/internal/behavior/recording_types.go`、`backend/internal/launchcode/recording_api.go`、`backend/internal/launchcode/server.go`、`go test -count=1 ./backend/internal/behavior ./backend/internal/launchcode`、`go test -count=1 ./backend -run "TestBehavior|TestRecording"`、`go test -count=1 ./backend/...`。

## 2026-04-29 行为录制真实 E2E 与 P2/P3 收口完成

- 目标：跑真实指纹浏览器录制/回放验收，并完成后续 P2/P3 主线切片。
- 观察到的事实：`TestRealFingerprintBrowserRecordPlayback` 可启动真实指纹 Chromium，完成点击、输入、滚动、刷新后继续点击、停止录制和回放断言；前端已能使用 summary/status/detail 分页接口。
- 已完成：修复导航后录制重注入前缺 `Page.enable` 的问题；新增真实 E2E；补 `RecordingDetailPage`、Wails 详情分页、HTTP detail 分页查询；`BrowserListPage.tsx` 与 `RecordingPanel.tsx` 改从 `fetchRecordingStatus()` 同步后端 active recording；文档状态已回写。
- 未完成：当时跨 target/新标签/iframe、超大录制 manifest/index 仍是后续增强；裁剪/导入导出/复制模板/回放进度/失败重试已由后续 P3 产品工具切片补齐，跨上下文与 manifest/index 也已由后续收口覆盖。
- 下一步：按真实业务页面补跨上下文场景，再做超大录制索引化。
- 关联文件/命令：`backend/internal/behavior/real_browser_e2e_test.go`、`backend/internal/behavior/recording_types.go`、`backend/app_behavior.go`、`backend/internal/launchcode/recording_api.go`、`frontend/src/modules/browser/api.ts`、`frontend/src/modules/browser/components/RecordingPanel.tsx`、`frontend/src/modules/browser/pages/BrowserListPage.tsx`、`$env:ANT_RECORDING_E2E='1'; go test -count=1 ./backend/internal/behavior -run TestRealFingerprintBrowserRecordPlayback -v`、`go test -count=1 ./backend/...`、`npm --prefix frontend run build`。

## 2026-04-29 行为录制 P3 产品工具完成

- 目标：按用户要求完整执行后续 P2/P3，补齐 P3-004 产品化工具，并重跑真实指纹浏览器录制/回放验收。
- 观察到的事实：后端已有录制 store、summary/detail 和回放引擎；前端已有录制面板和详情弹窗，适合用最小入口补齐工具，不需要引入新依赖。
- 已完成：新增录制 JSON 导出/导入、复制模板、按 1-based inclusive 事件范围裁剪另存；回放新增 `automation:playback:progress` 进度事件，完成/失败事件携带 `recordingId`；前端新增 JSON 文本导入/导出弹窗、列表行复制、详情裁剪、进度条和失败重试。
- 已验证：`go test -count=1 ./backend -run "TestBehaviorRecording|TestBehaviorPlayback|TestBehaviorGetRecordingDetail"`、`go test -count=1 ./backend/internal/behavior -run "TestPlaybackProgress"`、`npm --prefix frontend run build`、`$env:ANT_RECORDING_E2E='1'; go test -count=1 ./backend/internal/behavior -run TestRealFingerprintBrowserRecordPlayback -v` 已有通过记录；当时真实 E2E 事件计数为 `map[change:1 down:3 input:3 key:3 move:2 scroll:1 up:3]`。后续最终验收已重新跑通 `go test -count=1 ./backend/...`。
- 未完成：当时跨 target/新标签/iframe 和超大录制 manifest/index 仍保留为下一阶段；后续已收口。当前剩余边界是 100ms 极早事件窗口 warning、真实业务页回归样例和全量后端测试修复收口。
- 关联文件/命令：`backend/app_behavior.go`、`backend/internal/behavior/recording_types.go`、`backend/internal/behavior/playback.go`、`backend/internal/events/events.go`、`frontend/src/modules/browser/api.ts`、`frontend/src/modules/browser/components/RecordingPanel.tsx`、`frontend/src/modules/browser/components/RecordingDetailModal.tsx`、`frontend/src/wailsjs/go/main/App.d.ts`、`frontend/src/wailsjs/go/main/App.js`、`frontend/src/wailsjs/go/models.ts`。
## 2026-04-29 实例工作台真实 2 实例闭环验收

- 目标：把非容器化实例工作台从“已实现但依赖手工验收”推进到可重复自动验收，且避免脚本强杀真实浏览器 profile。
- 观察到的事实：LaunchServer 已有启动和 profile API，但缺少同等级安全停止入口；工作台后端能力主要暴露给 Wails UI，不便于真实 2 实例自动化闭环。
- 已完成：新增 `POST /api/instances/stop`；新增 `/api/workbench/navigate`、`refresh`、`screenshot`、`activate`、`arrange`；新增 `scripts/verify-workbench-two-instances.ps1`；补充接口单测和维护文档。
- 已验证：`go test ./backend/...`、`npm --prefix frontend run build`、`wails build`、`wails build -skipbindings`、`git diff --check` 均通过。
- 已验证：`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\verify-workbench-two-instances.ps1` 实际启动 2 个指纹 Chromium 实例，完成导航、刷新、截图、窗口排列、停止第一个后第二个仍可控、最终停止两个实例并关闭脚本启动的 App。
- 未完成：多屏指定、最小化其他窗口、布局恢复、预览节流和窗口截图降级仍属于后续增强。
- 关联文件/命令：`backend/internal/launchcode/instance_api.go`、`backend/internal/launchcode/workbench_api.go`、`backend/app_launchcode.go`、`scripts/verify-workbench-two-instances.ps1`。
## 2026-04-29 Win11 Tauri 基线脚本复核

- 目标：按全局规则运行 `enforce-win11-tauri.ps1`。
- 结果：脚本当时复核通过，输出 `[PASS] Win11 Tauri baseline check passed.`；当时仍提示 table-related code 缺少 virtualization 关键字的 warning。
- 处置：旧失败口径和 table warning 均已过时；当前 Table 虚拟滚动 warning 已消除，后续继续把表格/长列表虚拟滚动或分页作为结构门禁。

## 2026-04-29 行为录制 P2/P3 最终验收收口

- 目标：按用户要求跑真实指纹浏览器录制/回放验收，完成后续 P2/P3，并把最新门禁结果回写维护文档。
- 观察到的事实：P2/P3 主线已由后端 App/Wails/HTTP 方法、前端 API 收口、产品工具入口和真实 Chromium E2E 共同覆盖；Tauri 基线脚本最新已通过。
- 已完成：真实指纹 Chromium 录制/回放 E2E 通过，事件计数为 `map[change:1 down:3 input:3 key:3 move:2 scroll:1 up:3]`；P2 错误码、cleanup POST、active recording、事件释放、list/detail 拆分均收口；P3 搜索 debounce、详情分页、敏感输入脱敏、导入导出、复制模板、裁剪、回放进度、失败重试均收口。
- 已验证：`go test -count=1 ./backend -run "TestBehaviorRecording|TestBehaviorPlayback|TestBehaviorGetRecordingDetail"`、`go test -count=1 ./backend/internal/behavior ./backend/internal/launchcode`、`npm --prefix frontend run build`、`$env:ANT_RECORDING_E2E='1'; go test -count=1 ./backend/internal/behavior -run TestRealFingerprintBrowserRecordPlayback -v`、`git diff --check`、`powershell -ExecutionPolicy Bypass -File C:\Users\Lenovo\.codex\templates\win11-tauri-vite-react-ts\scripts\enforce-win11-tauri.ps1 -ProjectRoot D:\SelfMadeTool\antbrowser` 已有通过记录；后续最终验收已重新跑通 `go test -count=1 ./backend/...`。
- 未完成：约 100ms 极早事件窗口 warning、真实业务页面内部滚动容器样例和全量后端测试修复收口仍是下一阶段边界；跨 target/新标签/iframe、超大录制 manifest/index 和 Table 虚拟滚动 warning 已由后续收口覆盖。
- 下一步：先补跨上下文录制策略和失败提示，再做超大录制索引化。

## 2026-04-29 行为录制剩余问题收口文档更新

- 目标：把本轮剩余问题收口状态写回维护文档，避免旧 P0-P3 评估表和历史日志被误读为当前待办。
- 观察到的事实：iframe 聚合已完成；新 tab/多 page target 轮询同步已完成但保留约 100ms 极早事件窗口 warning；manifest/index 已完成，detail page 已走索引；Table 虚拟滚动 warning 已消除；当时全量后端测试尚未重新跑通，后续最终验收已覆盖。
- 已完成：更新 `docs/06-behavior-recording-technical-guidance.md`，在旧 P0-P3 表格前说明其为 2026-04-29 初始评估基线，当前状态以执行/收口段为准。
- 已完成：更新 `docs/02-current-state.md`，清掉“至少 2 个真实实例手工验收”的过时下一步，替换为 100ms warning、真实业务页回归、manifest/index 成本复核和全量后端测试修复收口。
- 已完成：更新 `docs/05-ai-maintenance-playbook.md`，加入后续 AI 接手规则：不要重复标记已收口主线，不要虚报未跑通过的全量后端测试。
- 未完成：本条当时未改代码、未运行后端全量测试；该缺口已由后续“行为录制剩余问题代码收口与最终验收”覆盖。
- 关联文件/命令：`docs/06-behavior-recording-technical-guidance.md`、`docs/02-current-state.md`、`docs/05-ai-maintenance-playbook.md`、`docs/logs/2026/2026-04.md`、`git diff --check -- docs/06-behavior-recording-technical-guidance.md docs/02-current-state.md docs/05-ai-maintenance-playbook.md docs/logs/2026/2026-04.md`

## 2026-04-29 行为录制剩余问题代码收口与最终验收

- 目标：按用户要求把剩余 blocker 全部修完，并继续使用多 agent 并行执行。
- 观察到的事实：只读复核发现旧收口仍漏掉 OOPIF/CDP iframe target 同步、EventMonitor 大表格渲染和 BrowserLogs 全量日志刷新；同时全量后端测试曾被 Wails 非 lifecycle context 下的 `EventsEmit` 退出阻塞。
- 已完成：统一事件桥在非 Wails lifecycle context 下安全跳过 `runtime.EventsEmit`；可录制 target 扩展为 page + iframe，能连接的 OOPIF/CDP iframe target 会在 start/monitor/stop 同步注入；EventMonitor live/history 改为共享 Table 虚拟滚动/分页和 300ms debounce；BrowserLogs 改为 `GetAppLogsPage` 分页过滤、page-size 控制、300ms debounce 和跟随最新页增量刷新。
- 已完成：manifest/index、detail page 索引读取、Table 虚拟滚动 warning、new tab/多 page target 同步、iframe/OOPIF target 同步、日志分页增量加载均不再作为未完成主线。
- 已验证：`go test -count=1 ./backend/internal/behavior`、`go test -count=1 ./backend/internal/launchcode -run "TestRecording"`、`go test -count=1 ./backend -run "TestBehaviorRecording|TestBehaviorPlayback|TestBehaviorGetRecordingDetail|TestBehaviorRecordingExportImportCopyAndTrim|TestBackupExportPackageToPathWritesZip|Test.*Log"`、`go test -count=1 ./backend/...`、`npm --prefix frontend run build`、`npm run build`、`$env:ANT_RECORDING_E2E='1'; go test -count=1 ./backend/internal/behavior -run TestRealFingerprintBrowserRecordPlayback -v`、`powershell -ExecutionPolicy Bypass -File C:\Users\Lenovo\.codex\templates\win11-tauri-vite-react-ts\scripts\enforce-win11-tauri.ps1 -ProjectRoot D:\SelfMadeTool\antbrowser`、`git diff --check` 均通过；最新真实 E2E 事件计数为 `map[change:1 down:3 input:3 key:3 move:4 scroll:1 up:3]`。
- 未完成：约 100ms 极早事件窗口仍是已知边界；真实业务页面内部滚动容器、复杂嵌套 iframe 和发布前性能基线仍建议单独回归。
- 关联文件/命令：`backend/internal/behavior/recorder.go`、`backend/internal/events/event_bridge.go`、`backend/internal/logger/memory_writer.go`、`backend/app.go`、`frontend/src/modules/monitor/EventMonitorPage.tsx`、`frontend/src/modules/browser/pages/BrowserLogsPage.tsx`、`docs/06-behavior-recording-technical-guidance.md`、`docs/02-current-state.md`、`docs/05-ai-maintenance-playbook.md`。

## 2026-04-29 行为录制 target discovery 事件驱动收口

- 目标：把新 tab/多 page target 的 100ms 轮询窗口从主路径中移除，保留为 browser websocket 不可用时的兜底。
- 观察到的事实：当前 CDP 结构可以额外连接 `/json/version` 的 browser websocket，并用 `Target.setDiscoverTargets` 接收 `Target.targetCreated/targetInfoChanged`。
- 已完成：新增浏览器级 target discovery 监听；事件到达时立即触发 target 同步，新 target 不再依赖 100ms 轮询主路径；补单测把轮询间隔拉到 10 秒，验证只靠 targetCreated 事件也能注入新 target。
- 未完成：真实业务页面内部滚动容器、复杂嵌套 iframe 和发布前性能基线仍建议单独回归。
- 下一步：用业务样例验证 browser websocket 可用性；不可用时继续按 100ms 轮询兜底提示处理。
- 关联文件/命令：`backend/internal/behavior/recorder.go`、`backend/internal/behavior/recording_target_test.go`、`go test -count=1 ./backend/internal/behavior -run "TestRecordingSynchronizes(TargetCreatedEventBeforePolling|NewPageTargetSnapshots|IframeTargetSnapshots)|TestTargetWarnings|TestRecordablePageTargets"`、`go test -count=1 ./backend/internal/behavior`
