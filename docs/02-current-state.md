# Current State

Updated: 2026-07-11 (Asia/Shanghai)

## 当前 live truth

- Mainline delivery：`100% / 0% / green` (3 P0 items closed)
- Local self-use：`100% / 0% / green`
- 唯一未验：CAPTCHA / SMS / Email 服务商真实账号凭证 smoke。没有真实账号、余额和密钥时，只能保留为 `blocked_missing_credentials`，不能伪造 accepted。
- 当前用户确认的唯一主线 UI 是截图所示 `personal-pilot` 1.1.0 壳，唯一用户入口为 `D:\SelfMadeTool\personal-pilot\personal-pilot-tauri.exe`。
- 后续所有更新、新增功能和修复都必须集成到 `personal-pilot-tauri.exe` 对应 UI；`PersonaPilot.exe`、`portable.exe`、`src-tauri/target/release/persona-pilot-desktop.exe`、`src-tauri/target/release/personal-pilot-tauri.exe`、安装包 exe 和第二套 UI 都不得作为持久入口。
- 当前仓库只保留一份用户可打开 GUI exe：根目录 `personal-pilot-tauri.exe`。`bin/` 下 sidecar、代理工具和 `chrome/` 下浏览器引擎 exe 是运行时依赖，不属于用户入口。
- 2026-06-22 范围重置：项目只服务本机自用；release performance 预算达标、外部分发 smoke、干净 Win11/第二机验收、跨机器 SessionBundle portability 全部取消，不再作为阻塞项、未完成项或下一步目标。
- 2026-06-28 已复测 app API 面：Go/Wails App 方法、`personal-pilot-core` sidecar RPC、LaunchServer 本地 HTTP API、Rust control-plane API、Tauri commands、TS desktop bridge 均有最新自动化或真实本机 smoke 证据；`tauriWailsBridge` 仍作为过渡兼容层存在，但已移除 5 个不存在的旧方法暴露，当前前端 `desktopRpc` 和旧桥 allowlist 均不再指向 sidecar 未放行方法。本轮还把 LaunchServer `/api/proxy/parse` 补齐到支持前端直接导入已有的四种认证代理格式，并继续对响应中的密码脱敏。
- 2026-06-28 API full workflow smoke 已真实跑通：配置/解析代理、手动代理 CRUD、预设指纹创建 profile、`autoLaunch` 启动本机 fingerprint Chromium、实例状态检查、workbench 导航、输入、点击、抓取页面、截图、指纹快照、fingerprint health、identity report、localStorage/cookie、tab 创建关闭、behavior start/stop、代理检测、日志读取和清理均通过。此次 smoke 同时修复了 `parseBoundsResult` 不匹配真实 CDP `Runtime.evaluate` 返回结构的问题，并支持小数坐标 round 后继续执行 click/hover。
- 2026-06-29 已修复 LaunchServer API key 环境变量引用：`launch_server.auth.api_key` 支持 `${VAR}`、`$VAR`、`%VAR%` 这类完整环境变量引用，鉴权运行时使用展开后的真实值；配置加载仍保留占位符，避免后续保存配置时把真实 key 写回 `config.yaml`。真实 sidecar smoke 已确认：缺 key 返回 `401`，`${LAUNCH_SERVER_API_KEY}` 字面量返回 `401`，环境变量真实值返回 `200`。
- 2026-06-29 已把 Wails 生成模型里的 fingerprint/identity 字段与后端真实 JSON 对齐：`browser.FingerprintSnapshot` / `FingerprintHealthProfile` / `IdentityStrengthReport` 这批前端类型现在使用 `webgl*`、`webgpuAvailable`、`webrtcSupported`、`uaDataPlatformVersion`、`uaDataFullVersionList`、`networkRtt` 等真实 key；同步器 `IdentitySubscores` 也补上了 `longTermCoherence`，前端报表不再吞掉这项分数。
- 2026-06-29 双轨 99+（PGS + XBS）Phase 0–2 骨架已落地：sing-box 端口 hold、禁 silent direct、Injection Ready Gate、全 API Audit、workbench `script` 旁路禁令、通用 `plans/`、`platform-packs/xhs/`、验收脚本 `platform_99_gate.ps1` / `xhs_live_acceptance.ps1`；规范见 `docs/44-dual-track-99plus-spec.md`，交接见 `docs/45-stealth-platform-handoff.md`。
- 2026-06-29 capability 雷达离线回归：`scripts/capability_scenario_suite.ps1 -Tier all -GenerateRadar` 为 **34/34 passed**，ObservabilityScore 100%；报告 `data/reports/capability-scenarios/suite-all-*.json`。这是 API/契约/可观测层证据，**不等于**目标站（如 XHS explore）live 可达。
- 2026-06-29 修复 Chrome 启动代理：`NormalizeProxyServerForBrowser` 将 sing-box 桥的 `socks5h://127.0.0.1:port` 规范为 `socks5://127.0.0.1:port`；Chromium `--proxy-server` 不支持 `socks5h`，否则会 `ERR_NO_SUPPORTED_PROXIES`。远程 DNS 仍由 sing-box 出站 + `--host-resolver-rules=MAP * ~NOTFOUND , EXCLUDE 127.0.0.1` 负责（见 `docs/29-proxy-supply-chain.md` §5.3）。
- 2026-06-29 **XHS live 验收未通过（已修复）**：曾报 `ERR_NO_SUPPORTED_PROXIES` / 多 core 分裂 / proxy hash mismatch；2026-06-30 复验 **12/12 PASS**。历史截图 chrome-error 判定无效。
- 2026-06-30 继续推进四项目标：**(1) UDEAL** sing-box/xray 桥统一返回 `socks5://`；LaunchServer 手动代理测速走 bridge manager；manual API 支持 `viaSsh`。**(2) 隐匿** 默认关闭鼠标 overlay；全局 `AutomationControlled`；`auto-99` stealth preset；UA/CH 对齐 131；host-resolver 可用标签 `skip-host-resolver-rules` 跳过。**(3) API 接管** navigate 校验 chrome-error；HTTP `/api/workbench/actions` 支持 `tabId`；切换 tab 重置 CDP 池；`xhs_live_acceptance.ps1` v3 默认复用 `19876` LaunchServer、HTTP 启停 profile、finally stop 实例（`-KeepInstance` 可保留浏览器）。**(4) 反自动化** G0 Input Plane + G1 CreepJS probe 已落地；XHS live **12/12 PASS**（CreepJS trust=100）；headed 登录仍待 plan live。
- 2026-06-30 **G0/G1 + XHS live 验收通过**：`scripts/platform_99_gate.ps1 -Track all` 29/29；`scripts/xhs_live_acceptance.ps1` L-01~L-12 全过（explore HTTPS、script 403、input plane、stealth-probe）。验收后浏览器默认关闭；L-12 会短暂打开 CreepJS 页，UI 可能仍显示 Computing（3s CDP 采样 + 启发式 trust）。详见 `docs/45-stealth-platform-handoff.md`、`docs/46-global-99plus-architecture.md`。
- 2026-07-07 用户明确重开 AdsPower / BitBrowser / PersonaPilot 横评轨道；这是独立 benchmark track，不覆盖 Mainline / Local self-use `100% / 0% / green`。准备事实：AdsPower 官方 8.6.3 x64 安装器已下载到 `D:\SelfMadeTool\ads\AdsPower-Global-8.6.3-x64.exe`，SHA256 `99AC2ABEF961520919B8376448923DCE28A5BDBB543B77895BC3BE40AE90BD90`，Authenticode valid，已静默安装到 `D:\SelfMadeTool\ads\AdsPowerGlobal`；BitBrowser 目录确认为 `D:\SelfMadeTool\bitbrowser`，版本 `7.1.3`，Local API `54345` 可用。2026-07-08 已完成代理脱敏预检：Clash 机场当前可覆盖 US/JP（US `3/3` ok、JP `9/15` ok、DE `0`，成功节点均 ip-api `proxy=true`），UDEAL 经 panda/本机桥只覆盖 LA 单出口（本机 `18082`/`18090` 可用，ip-api `proxy=false`、`hosting=false`）。AdsPower `127.0.0.1:50325/status` 可用，但 profile list 返回 `Require api-key`；用户截图确认当前 Free 账号的 API & MCP 页面标注“仅限付费套餐可用”，因此 AdsPower 不进入本轮 API 自动化矩阵，只保留安装、官方资料和免费版 UI/手工观察项。当前自动化实测收敛为 PersonaPilot + BitBrowser：`scripts/three_browser_benchmark_readiness.mjs` 默认只选双产品，默认 no-dry-run 报告 `data/reports/three-browser-benchmark/readiness/readiness-1783478822718.json` 为 blockers `[]` 且 AdsPower skipped；`--dry-run` 报告 `data/reports/three-browser-benchmark/readiness/readiness-1783475270066.json` 已跑通 UDEAL-LA 双产品 smoke，PersonaPilot Chrome `139.0.7258.154`、BitBrowser Chrome `126.0.6478.271`，两者均创建 profile、打开 `https://browserleaks.com/ip`、CDP `/json/version` 通过并关闭成功。两类代理必须拆成 proxy submatrix，不能混入同一 IP 质量分；DE 仍是 provider gap。
- 2026-07-08 已新增并运行 `scripts/two_browser_benchmark_matrix.mjs` 双产品 launch-loop 矩阵，报告均只保存脱敏 proxy/hash/IP hash。Clash 机场 US/JP 正式子矩阵 `data/reports/three-browser-benchmark/matrix/matrix-1783480553485/summary.json` 为 `40/40 ok`，PersonaPilot `20/20`、BitBrowser `20/20`，国家匹配 `40/40`，Clash controller 已恢复 `rule` 且 GLOBAL hash 恢复。UDEAL-LA 正式候选 `data/reports/three-browser-benchmark/matrix/matrix-1783480223173/summary.json` 为 `19/20 ok`：PersonaPilot `10/10`，BitBrowser `9/10`，唯一失败是 BitBrowser Local API 报“内存使用率超出 95%”；前一轮高内存污染 run `matrix-1783480022601` 保留为 resource-contaminated 证据。
- 2026-07-08 已新增并运行 `scripts/two_browser_benchmark_deep_matrix.mjs` 深度矩阵。UDEAL smoke `data/reports/three-browser-benchmark/deep-matrix/deep-1783482616216/summary.json` 为 `2/2 ok`，覆盖 CDP/IP/TLS-H2/WebRTC/行为截图链路且外部 detector 按参数跳过。当前完整可执行主跑 `data/reports/three-browser-benchmark/deep-matrix/deep-1783482661915/summary.json` 为 `6/6 ok`：Clash US/JP + UDEAL-LA 双产品各 1 次，国家匹配 `6/6`，TLS/H2 observed `6/6`，行为 `5/6`（PersonaPilot Clash US 一次 CDP `Input.dispatchMouseEvent` timeout，ASCII/中文 CDP insertText、textarea、scroll、iframe 均通过），detector 主跑 PersonaPilot `18/18`、BitBrowser `12/12`。BitBrowser Clash JP detector 首轮有瞬时 `Page.navigate timeout`，已用 `data/reports/three-browser-benchmark/deep-matrix/deep-1783490711021/summary.json` 补跑同 cell 为 `1/1 ok`、detector `6/6`。Clash controller 复检为 `rule`，GLOBAL hash `d0abc6973adb79ba`。报告敏感扫描未命中 deep reports 的明文代理凭证、Bearer/API key 或已知 UDEAL 原始出口 IP；只命中脚本自身脱敏正则和历史文档示例。
- 2026-07-08 按 BitBrowser 免费打开额度执行低配额缺口补测，新增 `scripts/two_browser_missing_probe_matrix.mjs`。PersonaPilot `data/reports/three-browser-benchmark/missing-probes/missing-1783494756348/summary.json` 和 BitBrowser `data/reports/three-browser-benchmark/missing-probes/missing-1783495149879/summary.json` 均为 `3/3 ok`：country/timezone/TLS `3/3`，WebRTC candidate `0/3`，canvas in-session `3/3`，BrowserLeaks DNS observed `3/3`，UA/core major match `0/3`，CreepJS trust/lies parser `0/3`。由此修正三方横评 100 分评分为 PersonaPilot `73/100`、BitBrowser `71/100`、AdsPower `N/A`；BitBrowser 新建 profile 也存在 Browser `126` / UA `125` 或 `127` mismatch，不能继续按 UA/core 一致性优势加分。权威 DNS-token、CreepJS structured trust/lies、10-run cross-session drift、DE 同类节点和 AdsPower API 仍未补齐。
- 2026-07-10 本地工程 W0 已落地：环境注入统一用 WeakMap 维护原函数映射并 nativeize `Function.prototype.toString`，本机真实 Chrome CDP smoke 已确认 Canvas/WebGL/webdriver getter/toString 均呈现原生签名；UA/UA-CH/brand version 改为 binary path → core identity → runtime args → fallback 的单一真相源，移除脚本中的 Chrome 131 硬编码并新增 `ua_core_coherent`；系统 resolver/CDN A 记录改为 `inconclusive`，只有确认观测到且与代理出口冲突时才判 DNS 泄漏；鼠标 press/release 增加 `200/400/800ms` 退避、移除逐事件 overlay round-trip，并按 humanization/CDP RTT 把轨迹上限从 80 压到 32/24/16/12。
- 2026-07-10 能力与 API 口径已纠偏：Rust 区分 `35` 个 declared primitives 与 `13` 个 active-runner-backed primitives；Go 区分 `35` 个 declared 与 `30` 个 `ExecutePrimitive` shipped 分支，不再把 `drag_to_element`、`select_option`、`scroll_into_view`、`switch_tab`、`close_tab` 虚报为 shipped。代理订阅 API 在 durable store 落地前统一返回显式 `501`；录制停止播放路由始终注册且缺 Recorder 时返回 `503`；硬编码订阅 URL 改为 `PERSONAL_PILOT_PROXY_SUBSCRIPTION_URL`，刷新流程改为先拉取/解析成功再替换旧数据。CI 已加入全量 Go tests、前端 build，并统一 Node 22；`.worktrees/` 已加入忽略但未删除现有工作树。 Rust clippy 严格基线当前仍有 `81` 个历史 warning，CI clippy 明确为 advisory；只有测试与构建是硬门禁，warning 清零前不得宣称 lint-clean。 同日 `ExecuteMutatedActionWithResult` 已提供类型化 Screenshot/HTML/Text payload，旧方法保持兼容，未知 action type 改为显式错误。 代理订阅现已从临时 `501` 升级为 migration 16 + SQLite durable store：create/list/delete/nodes/validate/refresh/import-clash 全部有真实实现；URL 节点稳定 ID、事务回滚、自动刷新、lastError、订阅 token/节点凭证脱敏均有测试。
- 第二套 Tauri/Vite/React 控制台 UI 源码和 `gateway-ui` 静态 UI 已从主工作树清理；后续不得继续把新功能只落到旁路 exe/UI。
- 第一族控制 schema 已声明 `80` 个 core control fields。
- 当前 runtime projection 是 `26` 个 env-backed fingerprint fields，其中 `25` 个来自 first-family control fields，另含 derived `platform`。
- 当前 behavior runtime 已交付 `13` 个 primitives；P10 已新增 behavior audit contract，覆盖 `8` 个 page archetypes、workflow/debug/manual-gate/recovery audit 状态；P14 已新增 machine-readable `450` event taxonomy seed；2026-06-22 `scripts/live_replay_runtime_gate.ps1` 已生成本地 deterministic replay report，状态 `passed_full_local_replay_runtime`，`461 / 450` events replayed，`461` product-runtime-backed、`0` contract-only。该证据不代表 CAPTCHA/SMS/Email 真实账号注册成功，只代表本机 replay runtime 已闭环。
- P14 已新增 machine-readable `450` fingerprint signal taxonomy seed、provider acceptance preflight 和 taxonomy audit 脚本；2026-06-22 新增 `scripts/full_observed_fingerprint_probe.mjs`，通过本机 Chrome/Edge + CDP 真实采集 browser/network/webrtc/canvas/audio/webgl/fonts/media/storage/timezone/hardware/coherence，再展开为 `450` 条 `layer=observed` signal。`scripts/observed_fingerprint_coverage_gate.ps1` 严格只统计 metadata 完整的 observed signal，最新为 `passed_full_observed_fingerprint_coverage`，`450 / 450`。materialized contract、taxonomy seed 和 projected/applied fields 仍不单独算 observed proof；现在的 full observed proof 来自本机真实采集。
- cookie / localStorage / sessionStorage 跨 app restart 持久化与恢复已落地。
- profile-scoped `SessionBundle` export/import preflight/dry-run/confirmed local restore write path 已落地：可导出 profile refs、fingerprint/network/continuity/behavior references、session binding metadata、cookie/localStorage/sessionStorage 存在性与计数；默认脱敏，显式本地开关才包含敏感 payload；import preflight 可检查 schema、引用缺失和 profile 冲突；dry-run 不写 DB；confirmed restore 可 upsert target profile 和 `proxy_session_bindings`，敏感 payload 只在 bundle 包含时恢复。
- 2026-06-22 最终复跑证据：observed gate `data/reports/observed-fingerprint-coverage/observed-fingerprint-coverage-gate-1782118951030.json` 为 `passed_full_observed_fingerprint_coverage`；replay runtime `data/reports/live-replay-runtime/live-replay-runtime-gate-1782118798569.json` 为 `passed_full_local_replay_runtime`；M10 `data/reports/m10-headed-stability/m10-headed-stability-gate-1782118850618.json` 为 `passed_long_task_stability_coherence`；M15 process `data/reports/m15-browser-process/m15-browser-process-gate-1782118796786.json` 为 `passed_real_browser_process_prewarm_cleanup`；M15 pool `data/reports/m15-browser-pool/m15-browser-pool-gate-1782118967687.json` 为 `passed_real_pool_process_integration`；本机 TLS `data/reports/remote-proxy-tls/remote-proxy-tls-probe-1782118943933.json` 为 `passed_local_direct_tls_observed`；runtime adapter `data/reports/runtime-adapter/runtime-adapter-evidence-gate-1782118975730.json` 为 `passed_local_self_use`；M4 `data/reports/m4-acceptance/m4-acceptance-gate-1782119044964.json` 为 `passed_with_expected_external_blockers`，`passed=25`、`expectedBlocked=1`、`failed=0`。

历史 `77% / 23%` 或 `82% / 18%` (historical-only) 不再作为当前进度口径。

## Runtime Alive

- 2026-05-28 已重新构建 Tauri 承载的 `personal-pilot-tauri.exe`，并覆盖根目录验收入口 `D:\SelfMadeTool\personal-pilot\personal-pilot-tauri.exe`；最新 SHA256 `704B14A6B358276124CBD5AF43E8BE0820507F2AD8A579CDBD3A87869DE701DC`，大小 `20,585,984` bytes。
- 该第一版保留 `personal-pilot` 1.1.0 UI 壳，并通过 `tauriWailsBridge` 启动 `bin/personal-pilot-core.exe` 兼容旧 Wails API；2026-05-28 已重建 `bin/personal-pilot-core.exe` 和 Tauri sidecar 副本 `bin/personal-pilot-core-x86_64-pc-windows-msvc.exe`，避免根目录入口继续拉起旧 core。
- 当前验收数据链路已确认不是空壳：本地 `data/app.db` 保留 `browser_profiles=4`、`browser_proxies=69`，`browser_cores=4`、`browser_bookmarks=7`，其中包括 `kind=camoufox` 的 `core-camoufox-manual` 预置项。
- 2026-05-25 曾修复设置页空白：根因是 `ThemeSwitcher` 使用 `useTheme`，但当时第二套 console app root 未包 `ThemeProvider`。当前主线已回到 `src/main.tsx` / `src/App.tsx` / `src/modules/**` 的 `personal-pilot` 1.1.0 UI；旧 console bootstrap 已随第二套控制台 UI 删除。
- 2026-05-25 已修复窗口黑色坏图标：根因是 `src-tauri/icons/icon.ico` 只有 `155` bytes 的无效 ico；现已用现有 PNG 资源重新生成有效 `13,322` bytes ico，并重新打包 release exe。
- 2026-05-26 已固化 `react-router-dom@6.30.3` 到 `package.json` / `pnpm-lock.yaml`，避免干净安装依赖缺失；已将 release / external / fingerprint / workbench 验证脚本默认目标收敛到根目录 `personal-pilot-tauri.exe`。
- `data/app.db` 仍保留真实数据：`browser_profiles=4`、`browser_proxies=69`、`browser_cores=4`、`browser_bookmarks=7`。
- 2026-05-28 复核 `personal-pilot-core` 真实 RPC：`BrowserCoreList` 返回 `4` 条内核，其中 `kind=camoufox` 的 `Camoufox（配置路径后可用）` 可见；2026-06-22 复核 `data/app.db` 当前为实例 `4`、代理 `69`、内核 `4`、书签 `7`、Camoufox 内核 `1`。
- 后续迁功能或剔除 UI 前，仍必须继续验证 Wails 兼容 API 等价链路，不能只看 UI 外观。
- `persona-pilot-desktop.exe` / `PersonaPilot.exe` / `portable.exe` / 根目录 `benchmark.exe` / 根目录 `deepseek-register.exe` 已从当前工作树清理；`bin/` 下 sidecar 和工具产物不属于用户入口，按需保留。
- Runtime alive 只代表应用和本地能力可启动/调用，不等同于 Overall 终态闭环。
- Gateway real-upstream acceptance 仍需按 `upstream_configured=false` guardrail 单独验收。

## Build Status

- 2026-05-27 构建规则已收口：`pnpm desktop:release` 先执行 Tauri build，再运行 `scripts/sync-main-entry.ps1`，只保留根目录 `personal-pilot-tauri.exe`，并清理 `src-tauri/target/release` 下的临时 GUI exe / installer exe；`target/` 与 `src-tauri/target/` 均视为可删除构建输出。
- 2026-05-28 已通过 `pnpm desktop:release` 重新构建并同步根目录 `personal-pilot-tauri.exe`，hash `704B14A6B358276124CBD5AF43E8BE0820507F2AD8A579CDBD3A87869DE701DC`，大小 `20,585,984` bytes。
- 历史 M5 release health smoke：`data/reports/release-smoke/release-performance-smoke-1781933571814.json`，schema `release_performance_smoke_v2`，状态 `warning` / `budgetStatus=over_budget`，实测 cold start `3899ms`、idle RSS `433MB`、process count `9`；`data/reports/m5-release-health/m5-release-health-gate-1781941776163.json` 为 `passed_with_budget_overrun`。2026-06-22 范围重置后，该报告只作为诊断材料，不再要求优化到预算 green。
- 2026-05-25 Win11/Tauri baseline enforcement passed。
- 2026-05-25 root `personal-pilot-tauri.exe` 启动验证 passed：进程 `personal-pilot-tauri`，窗口标题 `personal-pilot`，响应正常；兼容 core bridge 当前进程为 `bin/personal-pilot-core.exe`。
- 2026-05-25 错误 Tauri 第一版 release smoke：`14779ms` cold start、`489MB` idle RSS、`7` processes，状态 warning；该报告只作为回滚原因，不代表当前可验收入口性能。
- 2026-05-23 的 Tauri release gate 和 2026-05-24/25 的 `persona-pilot-desktop.exe` 构建只能作为历史/旁路证据；主线门禁需要改为验证 `personal-pilot-tauri.exe`。
- 后续 meaningful code change 仍需重新跑对应门禁；但门禁目标必须改成 `personal-pilot-tauri.exe`，operator manual smoke、continuity integration test 和外部分发验收不再作为当前范围要求。
- P13 外部分发前检查文档 `docs/24-external-distribution-readiness.md` 已改为 historical-only；当前本机自用范围下不再执行 manual operator smoke、干净 Win11 安装/卸载或外部分发验收。

## Reporting Rule From Now On

- current shipped app / closeout / native mainline：`100% / 0% / green`。
- local-only self-use：`100% / 0% / green`。
- only remaining unverified item：CAPTCHA / SMS / Email 真实账号凭证 smoke。
- 历史 Overall `40% / 60% / yellow`、AdsPower catch-up、外部分发、release performance budget、第二机/跨机器目标只作历史上下文，不再作为当前剩余工作。

## 已确认落地

- Win11 desktop shell 已有两条历史路线：用户当前确认只保留 `personal-pilot-tauri.exe` 1.1.0 / Wails v2 + React UI；第二套 Tauri/Vite console UI 源码和 `gateway-ui/` 静态模板 UI 已删除，不再作为主线 UI。
- `src/services/desktop.ts` 是 Tauri 路线的 native / invoke 统一边界；主线 Wails UI 中仍存在 `wailsjs` 调用和 `tauriWailsBridge` 兼容层，需作为迁移/收敛风险处理。
- 截图主线 UI 包含 Dashboard / 实例列表 / 实例工作台 / 行为录制 / 自动化接口 / 内核管理 / 代理池配置 / 默认书签 / 标签管理 / 事件监控 / 系统设置 / 使用教程 / 日志查看 / API docs；`/charts` 仍存在于路由中，但未进入导航，当前按内部 demo/sample route 处理。
- `Tasks -> Automation` surface unification 已完成。
- provider-aware / sticky-aware `changeProxyIp` local desktop contract 已落地。
- Recorder desktop step-write 已落地。
- Synchronizer live desktop snapshot、native focus、native set-main、work-area-aware native physical layout 已落地。
- full Rust / integration gate 已恢复 green。
- route-level code splitting 已清掉旧 Vite chunk warning。
- CAPTCHA 后端 handler/route、solver 边界、dry-run、failure taxonomy、Settings operator surface 已落地；2Captcha/Capsolver 真实账号凭证 smoke 未验。
- SMS 后端 handler/route、provider 边界、dry-run、failure taxonomy、Settings operator surface 已落地；5sim/SMSPool 真实账号凭证 smoke 未验。
- Email `EmailService`、inbox / wait-code REST API、dry-run、failure taxonomy、Settings operator surface 已落地；真实邮箱账号/收件服务凭证 smoke 未验。
- 备份单体已拆成 core/import/merge/utils；browser launch args 与 process monitor 已迁入 `internal/browser`。
- Validation Board 已落地到桌面导航：8 类 evidence（detector / leak / DNS / WebRTC / canvas / audio / worker / transport）按 declared / applied / observed 分层展示；DNS/transport observed 首批 native collector、desktop WebView scoped WebRTC/canvas/audio/storage probes、profile browser runtime validation probe action、WebRTC/leak native contract warning、JSON report、report history 和 profile-level evidence export 已接入。P5 已通过 WSL2 Lightpanda nightly 真实 CDP smoke；P6 已收敛 validation evidence schema；P7 已新增 fingerprint observation audit。2026-06-22 strict observed coverage gate 当前只承认 metadata 完整的 `layer=observed` signals，最新为 `450 / 450`，不把 taxonomy seed 或 materialized contract 单独计入 observed proof。
- P17 扩展 desktop WebView observed probes：timezone/locale、hardware/os、screen/display、navigator hints、permissions/media devices 已有 desktop WebView scoped signals；它们仍不是 profile browser proof。
- Automation 已接入 P10 behavior audit contract：报告 `13` shipped primitives、`8` page archetypes、workflow graph/debug trace/manual gate/recovery semantics 覆盖状态，并明确 `450+` event taxonomy 仍不是 live replay runtime。
- P18 已把 behavior taxonomy 前 5 个 family 补成 replay-semantics backed：readiness_wait、settle_idle、scroll_scan、hover_focus、typing_input 均有 replaySemantics、auditPayload、failureStates、recoveryBehavior；2026-05-28 `scripts/taxonomy_coverage_materialize.ps1` 已物化 `461 / 450` 条 behavior replay contract；2026-06-22 `scripts/live_replay_runtime_gate.ps1` 已把 `461 / 450` 条 event 跑过本地 deterministic replay runtime，完整 target-site/browser/provider replay 仍未交付。
- Runtime adapter contract 已接入桌面 API：记录 Fake、Lightpanda、headed_external adapter 边界和根目录 `personal-pilot-tauri.exe`；M5 release performance report 只作诊断材料。M10 3-run headed_external validation_probe stability/coherence matrix 最新 `passed_long_task_stability_coherence`，`3/3` attempts passed，`coherenceScore=1`、`stabilityScore=1`、`signalCount=9`。M15 已把本地 pool lifecycle、process attach/release cleanup proof、真实本机 browser process prewarm/CDP/RSS/cleanup proof 收敛到 `passed_real_pool_process_integration`。本机 TLS/transport 默认直连观测已通过；远程代理账号不属于本机自用阻塞。runtime adapter gate 最新目标状态为 `passed_local_self_use`，AdsPower 刷分不适用于当前本机范围。
- P16 已新增 unified evidence report history：provider acceptance、taxonomy audit、profile/browser comparison、runtime/headed/M4 等本地 JSON report 可通过桌面 API 读取，并在 Overview/Dashboard 显示最近报告；release performance、external distribution 和跨机器 portability report 只保留历史诊断价值。
- P19 已深化 provider production closure contract：Settings 现在区分 credentials、manager wiring、CDP detect、CDP fill、operator UI、dry-run、failure taxonomy、real provider smoke、failure reason 和 latest report path；scripts/provider_acceptance_preflight.ps1 输出 v3 dry-run/failureTaxonomy/blockers/failureReason。真实 provider acceptance 仍必须有凭证和真实 smoke 证据。
- P20 外部条件 smoke gates 已取消为目标：`external_distribution_smoke.ps1` 和 cross-machine `session_bundle_portability_smoke.ps1` 不再要求复跑，也不再把缺少第二环境或人工 smoke 记为当前阻塞。
- P21 runtime adapter evidence gate 已按本机自用收口：release contract 暴露 adapter capability/evidence requirements/process lifecycle/CDP attach 状态；`scripts/runtime_adapter_evidence_gate.ps1` 消费 observed/replay/session/M10/M15/transport reports，当前目标状态为 `passed_local_self_use`；AdsPower refresh 为 `not_applicable_local_only`。
- P22-P27 已执行新一轮 6 批本机推进：Settings Import/Export 显示最新本机 SessionBundle report；Validation desktop WebView 新增 WebGL 与 font/text metrics observed probes；fingerprint taxonomy 全 family 补 collector/layer/adapter/failure/repeatability/schema 元数据；behavior taxonomy 全 family 补 replay/audit/failure/recovery 定义；新增 `scripts/live_truth_guard.ps1` 防止 live-truth 口径回退。
- P28-P33 已按本机优先继续推进：fingerprint audit 统计 desktop WebView `fingerprint` observed category；behavior audit contract 暴露 workflow graph/replay debugger/deterministic evidence 状态；新增 provider manager wiring gate 和 profile-browser comparison gate；外部/人工实测已从当前目标中取消。
- 2026-05-28 按 `docs/99-implementation-roadmap.md` 启动全量路线图首批实现：新增 `backend/internal/behavior/environment_injector.go`，可把 profile 环境参数编译为单次 `Page.addScriptToEvaluateOnNewDocument` 注入脚本，并协调 `Accept-Language` header；已覆盖 `navigator.webdriver/languages/platform/vendor/userAgent/hardwareConcurrency/deviceMemory`、plugins/mimeTypes、canvas `toDataURL/getImageData/fillText`、timezone/locale、WebGL vendor/renderer 和 media devices 基础 hook。该切片仍是注入编译与 CDP 调用入口，不等同于完整 450 observed proof。
- 2026-05-28 已增强 Go behavior humanize 轨迹基础：新增 Fitts Law movement time、三段式 ease path、8-12Hz 等价粉噪、四段式点击、双击间隔/微移、拖拽和右键菜单计划；`go test ./backend/internal/behavior ./backend/internal/behavior/humanize` passed。它推进 Phase 2 P0 基础模型。
- 2026-05-28 继续按 `docs/99-implementation-roadmap.md` 推进：Phase 1 P1/P2 已补 WebGL extension filter、audio frequency/buffer noise、font allowlist/document.fonts hook、media getUserMedia permission stub、WebRTC offer/SDP/ICE candidate filter，以及 `backend/internal/browser/consistency_matrix.go`；Phase 2 P1 已补 typing finger/IME/modifier/fatigue/context/tab/clipboard 和 scroll inertia/content/reread/infinite/microjitter plan；Phase 3-5 新增 lifecycle/workflow/reference/session/pool/discovery/transport contract 包。验证：`go test ./backend/internal/...` passed。Phase 3-5 仍是 contract-ready，不等同真实调度器、真实浏览器池、本机加密 restore 深度或传输层内核闭环；跨机器加密迁移已取消。
- 2026-05-28 继续同步推进 roadmap 第二批：lifecycle 已补 SQLite `Store` 和 `AssessRisk`；workflow 已补 CDP events→steps recorder、playback plan、executor route、form mapper、provider adapters，以及 LaunchCode `/api/workflow` create/execute/status/export 和 `/api/plugin/install`；SessionBundle 已补 AES-GCM encrypt/decrypt 和 default runtime mapping；pool 已补 prewarm plan；discovery 已补 form/challenge/flow inference 与 workflow template conversion；transport 已补 Xray/Sing-Box outbound config 和 header-order template。验证：`go test ./backend/internal/...` passed。后续仍需真实 provider 凭证 smoke、full observed coverage、真实 browser pool acquire/release integration 和出站代理运行证据；跨机器 portability 已取消。
- 2026-05-28 Camoufox 已作为主线浏览器内核类型接入：`browser_cores.kind` SQLite 迁移、Go core DAO/解析/校验、实例启动参数分发、sidecar RPC `BrowserCoreValidateForKind`、内核管理 UI/基础配置 UI/实例列表设置 UI 的 Chromium / Lightpanda / Camoufox 选择均已落地；Camoufox 启动参数使用 `--profile` + `--remote-debugging-port`，不再套用 Chromium `--user-data-dir`。同日修复 live DB 未迁移和旧 sidecar 未重建导致“内核管理仍无 Camoufox”的回归：`data/app.db` 已补 `browser_cores.kind` 与 `core-camoufox-manual`，启动时会自修复缺失 `kind` 列并确保 Camoufox 预置项可见。验证：`go test ./backend/cmd/personal-pilot-core ./backend/internal/database ./backend/internal/browser`、`pnpm typecheck`、`pnpm build` passed；本轮又把 `src/runner/camoufox.rs` 从 skeleton 推进到最小 CDP runner，源码级 `scripts/camoufox_smoke.ps1` 可检查该实现；真实 Camoufox binary 已用 Firefox-compatible headless screenshot 打开 `https://example.com` 并产出 PNG，但不宣称 Chromium `/json/version` CDP attach 成功。

以下 dated bullets 是 2026-05-28 至 2026-06-12 的历史推进记录；其中的“最新 report / 最新复跑”只代表当时最新。当前 live truth 以本文件顶部、2026-06-22 本机 SessionBundle restore/M8/M4 report、DB/performance 复核和“当前下一步”为准。

- 2026-05-28 已补 roadmap profile-browser 证据：新增 `scripts/profile_browser_environment_probe.mjs`，用仓库内 fingerprint Chromium 通过 CDP 真实观测 browser API / canvas / timezone-locale 注入效果；report `data/validation-reports/profile-browser-environment-1779951691042.json` 与 `data/reports/profile-browser-environment/profile-browser-environment-1779951691042.json` 均为 `passed`。随后复跑 roadmap evidence `data/reports/roadmap-evidence/roadmap-evidence-smoke-1779951732878.json` 仍为 `passed_with_external_evidence_pending`；profile-browser comparison gate 进入 `partial_comparison_only`，原因是最新 validation report 是 profile-browser 专项报告，不含同份 desktop-webview 对照信号。
- 2026-05-28 继续按用户指定的 roadmap 4/5/6/7/9 推进：新增 `backend/app_environment_injection.go`，实例 debug ready 后会异步应用 profile 环境注入；同一 profile/debugPort/PID/launch timestamp 已加去重，停止实例时清理。`environment_audit.go` 已扩到 browser API、canvas、timezone/locale、WebGL/GPU、audio、fonts/text metrics、media devices、WebRTC/IP leak 8 个注入族。`transport/` 已从死骨架变为 Xray/SingBox 配置生成引用，但只向真实配置写入外部内核已知字段，runtime family/header-order/HTTP2 细节暂作为 explain metadata，不冒充真实出站观测。最新 roadmap evidence report：`data/reports/roadmap-evidence/roadmap-evidence-smoke-1779976243311.json`，状态 `passed_with_external_evidence_pending`。
- 2026-05-28 Camoufox Rust runner 已从 skeleton 改为最小 CDP runner：支持 `open_page` / `fetch` / `get_html` / `get_title` / `get_final_url` / `extract_text` / `validation_probe`，可 spawn 配置里的 Chromium-CDP-compatible browser binary，等待 `/json/version` WebSocket，执行 Target/Page/Runtime CDP 命令，输出 title/finalUrl/html/text/content/stdout/stderr preview，并在任务结束或超时后清理子进程；`validation_probe` 已采样 navigator、canvas、timezone、WebGL、audio、fonts、media devices、WebRTC、storage。源码级 smoke `data/reports/camoufox-smoke/camoufox-smoke-1779976246144.json` 为 `passed`；真实 Camoufox binary page-open smoke `data/reports/camoufox-binary-task/camoufox-binary-task-smoke-1779983524496.json` 为 `passed`，截图 `data/reports/camoufox-binary-task/camoufox-binary-task-smoke-1779983524496.png` 已持久化；runtime adapter gate `data/reports/runtime-adapter/runtime-adapter-evidence-gate-1779983066984.json` 仍为 `blocked_evidence_required`，因为 B1-B5 外部证据不齐，且该 Camoufox binary 暴露 Firefox/Juggler/BiDi 风格 runtime，Chromium CDP attach 不宣称通过。
- 2026-05-30 已把 `headed_external` 从 source contract 推进到真实 headed browser binary task-run，并继续推进到 profile-browser validation probe：先使用仓库内 `chrome/fingerprint-chromium-144.0.7559.132/chrome.exe` 执行 `get_title https://example.com`，report `data/reports/headed-external-smoke/headed-external-smoke-1780109927517.json` 为 `passed_real_binary_task`；随后执行 `validation_probe`，report `data/reports/headed-external-smoke/headed-external-smoke-1780121855313.json` 为 `passed_real_binary_validation_probe`，`realBinaryTask.status=passed`、`action=validation_probe`、`validation_signals=9`。最新 runtime adapter gate `data/reports/runtime-adapter/runtime-adapter-evidence-gate-1780121877907.json` 仍为 `blocked_evidence_required`，但已消费 headed profile-browser observed proof：`runtimeAdapterEvidence=partial_real_binary_validation_probe_recorded`、`fingerprintRuntimeDepth=partial_headed_profile_browser_observed`、categories `audio/canvas/detector/fingerprint/leak/timezone/webgl/webrtc`；不能据此宣称完整 headed runtime repeatability/coherence、`450` observed coverage、远程代理/TLS、provider 或 AdsPower refresh 已完成；跨机器 SessionBundle 后续已取消。
- 2026-05-31 修复 runtime/headed evidence 选择口径：`scripts/runtime_adapter_evidence_gate.ps1` 和 desktop release smoke contract（现为历史诊断）按 evidence strength 选择 headed_external report，避免新的 source-only / weak report 覆盖旧的 `passed_real_binary_validation_probe`；`scripts/headed_external_smoke.ps1` 已修复 cargo stdout 污染，防止 `realBinaryTask` 被命令输出破坏。最新 headed report `data/reports/headed-external-smoke/headed-external-smoke-1780198191425.json` 为 `passed_real_binary_validation_probe`，`cargo test --quiet headed_external` 8 个相关测试 passed；最新 runtime adapter gate `data/reports/runtime-adapter/runtime-adapter-evidence-gate-1780198524266.json` 仍为 `blocked_evidence_required`，但已明确 `headedExternalValidationProbe.selectedReportStatus=passed_real_binary_validation_probe`、`runtimeAdapterEvidence=partial_real_binary_validation_probe_recorded`、`fingerprintRuntimeDepth=partial_headed_profile_browser_observed`、`signalCount=9`，categories `audio/canvas/detector/fingerprint/leak/timezone/webgl/webrtc`。这只收口证据选择和本机 headed validation probe，不改变 Overall `40% / 60% / yellow`。
- 2026-06-02 M10 headed repeatability partial evidence 已记录：`scripts/headed_external_smoke.ps1 -Action validation_probe -RepeatValidationProbeCount 2 -RunRustTests` 生成 `data/reports/headed-external-smoke/headed-external-smoke-1780375482756.json`，状态 `passed_real_binary_repeatability`，repeatability `passed_repeatability_partial_coherence`，2/2 attempts passed，9 signals，categories `audio/canvas/detector/fingerprint/leak/timezone/webgl/webrtc`；新增 `scripts/m10_headed_repeatability_gate.ps1` 生成 `data/reports/m10-headed-repeatability/m10-headed-repeatability-gate-1780377051356.json`，状态 `passed_repeatability_partial_coherence`。最新 runtime adapter gate `data/reports/runtime-adapter/runtime-adapter-evidence-gate-1780375565693.json` 已消费该证据并记录 `runtimeAdapterEvidence=partial_real_binary_repeatability_recorded` / `fingerprintRuntimeDepth=partial_headed_profile_browser_repeatability_observed`，但仍为 `blocked_evidence_required`；这不是长任务稳定性、远程代理/TLS、provider、AdsPower refresh 或完整 `450` observed/replay coverage；跨机器 SessionBundle 后续已取消。
- 2026-05-31 本机可跑 evidence 已复刷：Camoufox source smoke `data/reports/camoufox-smoke/camoufox-smoke-1780198459643.json` 为 `passed`；Camoufox binary page-open `data/reports/camoufox-binary-task/camoufox-binary-task-smoke-1780198493050.json` 为 `passed`，截图 `.codex_tmp\camoufox-binary-task\1780198493050\camoufox-smoke.png`；profile-browser comparison gate `data/reports/profile-browser-comparison/profile-browser-comparison-1780198283892.json` 为 `blocked_missing_desktop_webview_report`；remote proxy/TLS `data/reports/remote-proxy-tls/remote-proxy-tls-probe-1780198283532.json` 为 `blocked_remote_proxy_required`；provider acceptance `data/reports/provider-acceptance/provider-acceptance-preflight-1780198283660.json` 为 `blocked_missing_credentials`；SessionBundle portability `data/reports/session-portability/session-bundle-portability-smoke-1780198283603.json` 为 `local_contract_passed`，后续跨机器 evidence 已取消；live truth guard `data/reports/governance/live-truth-guard-1780198316165.json` 为 `passed`。
- 2026-05-31 M4 acceptance harness 已落地：新增 `scripts/m4_acceptance_gate.ps1` 聚合 live truth、runtime adapter、provider acceptance、SessionBundle portability 和 profile-browser comparison 五类关键 gate；最新 report `data/reports/m4-acceptance/m4-acceptance-gate-1780234896011.json` 为 `expected_blocked`，summary `passed=1`、`expectedBlocked=4`、`failed=0`。Dashboard 验收证据列表已能展示 M4 gate、release performance、runtime adapter、profile/browser comparison、remote proxy/TLS、headed browser、Camoufox、provider、SessionBundle、taxonomy coverage/audit 和 external distribution；其中 release/external entries 后续只保留历史诊断价值。Go CDP automation 修复了 `Runtime.evaluate.result.value` 嵌套布尔/字符串解析，避免真实 CDP wait/EvaluateJS 被 mock 掩盖；验证：`go test ./backend/internal/scheduler ./backend/internal/behavior -count=1`、`cargo test evidence_report_history`、`pnpm typecheck` 和 M4 gate 均 passed/expected_blocked。外部 blocker 不变，不提高 Overall。
- 2026-06-01 M4.2 workflow task center 状态闭环已推进：scheduler task runtime state 现在随 SQLite 持久化 `status`、`last_run_at`、`last_error`、`retry_count`；`SchedulerListTasks` 返回的 UI 数据能在运行后看到 done/failed、上次运行时间、错误和重试次数；Automation 任务页触发“立即执行”后会刷新列表并展示步骤数、上次运行和失败原因。新增迁移版本 `13`，并保留 drift repair，兼容 schema_migrations 已前进但 scheduler runtime 列缺失的旧库。验证：`go test ./backend/internal/scheduler ./backend/internal/database -count=1` passed；`pnpm typecheck` passed。该切片只证明本地任务中心可创建/运行/检查状态，不代表 provider、远程代理/TLS、AdsPower 或完整 `450` replay/runtime 已完成；跨机器 SessionBundle 后续已取消。
- 2026-06-01 M4.3 automation primitive v1 已推进到 typed CDP task runner：scheduler action 现在支持 `select`、`dialog`、`download`、`upload`、`iframe` 和 `tab`，减少 operator 只能手写 raw `cdp` action 的情况；mock CDP tests 覆盖这些 action 的命令与参数形状。M4 acceptance gate v2 已纳入 `automation_primitives_contract` 本地源/测试覆盖检查；最新 report `data/reports/m4-acceptance/m4-acceptance-gate-1780284356878.json` 为 `expected_blocked`，summary `passed=2`、`expectedBlocked=4`、`failed=0`。验证：`go test ./backend/internal/scheduler -count=1` passed。该证据仍是本地 runner primitive 与 mock CDP contract，不代表目标站生产流程、真实 provider smoke 或完整 live behavior replay 已完成。
- 2026-06-01 M4.4 provider dry-run/operator closure 已推进：`scripts/provider_acceptance_preflight.ps1` 升级为 v3，本地输出 CAPTCHA/SMS/Email dry-run contract、dry-run next action、failure taxonomy 与 real provider smoke blocker；Settings 页新增 Provider dry-run 验收表，读取 `read_provider_production_readiness` 并展示 credentials/dry-run/taxonomy/real smoke/latest report/failure reason。M4 acceptance gate v3 会刷新 provider preflight，并新增 `provider_dry_run_contract` 本地报告检查；最新 report `data/reports/m4-acceptance/m4-acceptance-gate-1780294932319.json` 为 `expected_blocked`，summary `passed=3`、`expectedBlocked=4`、`failed=0`。该证据仍是本地 dry-run/operator 可见性，不代表真实 provider smoke、远程代理/TLS、AdsPower refresh 或完整 `450` live behavior replay 已完成；跨机器 SessionBundle 后续已取消。
- 2026-06-01 M4.5/M6 desktop/profile comparison 入口已推进：Dashboard 验收证据卡新增 `采集 WebView` operator action，可在真实桌面 WebView 中采样 fingerprint/canvas/audio/WebRTC/storage signals 并调用 `collect_validation_report` 写入 `data/reports/validation`；Tauri validation report normalization 已将 `fingerprint` 作为合法 browser signal category 保留，避免 WebView fingerprint 被降级为 `detector`；`scripts/profile_browser_comparison_gate.ps1` 升级为 v3，新增 `sameRunStatus`、`maxPairAgeMinutes`、`desktopProfileDeltaMinutes`，只有 desktop WebView 与 profile-browser 双边同窗且五类 category 都可比时才 `passed`。最新 comparison report `data/reports/profile-browser-comparison/profile-browser-comparison-1780298919581.json` 仍为 `blocked_missing_desktop_webview_report`，因为本轮未实际点击桌面 WebView 采集入口；最新 M4 gate `data/reports/m4-acceptance/m4-acceptance-gate-1780302244369.json` 仍为 `expected_blocked`，summary `passed=3`、`expectedBlocked=4`、`failed=0`。
- 2026-06-01 M4.6 SessionBundle operator loop 已推进：Settings 页新增 `SessionBundle 本机验收` 卡，直接调用 `export_session_bundle`、`preflight_session_bundle_import`、`restore_session_bundle`，支持本机 export、preflight、dry-run 和带确认的 confirmed local restore，并展示 restore plan、missing refs、conflicts、writePerformed 和 restored binding count。`scripts/m4_acceptance_gate.ps1` 升级为 v4，新增 `session_bundle_operator_contract` 本地 UI/API source gate；该切片验证 report `data/reports/m4-acceptance/m4-acceptance-gate-1780305132194.json` 为 `expected_blocked`，summary `passed=4`、`expectedBlocked=4`、`failed=0`。该证据只证明本机 operator loop 接通，不代表远程代理/TLS、provider、AdsPower refresh 或完整 `450` observed/replay coverage 已完成；跨机器 SessionBundle portability 后续已取消。
- 2026-06-01 M4.8 typed facade shrink 已推进两批：`src/types/desktop.ts` 新增 synchronizer/workbench core bridge DTO，`src/services/desktop.ts` 的 high-traffic `SynchronizerListGroups`、`SynchronizerArrangeProfiles`、`SynchronizerGetOperationLog`、`SynchronizerListTasks`、`SynchronizerSaveTasks` 不再暴露 `unknown[]`，`src/modules/synchronizer/api.ts` 移除对应 `as Promise<...>` cast；`src/modules/browser/api.ts` 新增 `BrowserNativeBindings` / `getWindowGoApp()` 类型合同，移除 browser API 内 `const bindings: any = await getBindings()` 和 `(window as any).go?.main?.App` 动态入口。M4 gate v6 扩展 `typed_facade_shrink_contract` 本地 source gate；最新 report 为 `expected_blocked`，summary `passed=6`、`expectedBlocked=4`、`failed=0`。该证据只收窄高频 bridge 类型边界，不代表 Wails/core bridge 已完全移除。
- 2026-06-01 M4.9 safety/logging contract 已推进：`backend/internal/logger` 新增默认敏感字段与文本脱敏器，logger 写入路径和 Text/JSON formatter 都会遮蔽 password/token/api_key/authorization/credential/secret/cookie 等字段、Bearer/Basic 值和 URL userinfo 密码；默认 interceptor 敏感字段列表同步扩展。M4 gate v7 新增 `safety_logging_contract` 本地 source/test gate；该切片 report `data/reports/m4-acceptance/m4-acceptance-gate-1780317928923.json` 为 `expected_blocked`，summary `passed=7`、`expectedBlocked=4`、`failed=0`。该证据只证明本地 logger/report 脱敏合同，不代表真实 provider 凭证 smoke、外部分发日志审计或历史报告已被回溯清洗。
- 2026-06-01 M4.10 total gate 已推进：`scripts/m4_acceptance_gate.ps1` 升级为 v8，顶层 `status`/`operatorStatus` 在本地 gate 全部通过且只剩预期外部阻塞时输出 `passed_with_expected_external_blockers`，并保留 `gateClassificationStatus=expected_blocked` 给底层分类和历史边界使用；report 新增 `m4TotalGate`，列出 passed/expectedBlocked/failed gate ids 和下一步。Dashboard evidence history 已识别该状态为 `expected_external_blockers`，避免把预期外部阻塞渲染成失败。2026-06-02 本轮复核 report `data/reports/m4-acceptance/m4-acceptance-gate-1780372827647.json` 为 `passed_with_expected_external_blockers`，summary `passed=7`、`expectedBlocked=4`、`failed=0`。
- 2026-06-01 M5 release health budget 已推进：`scripts/release_performance_smoke.ps1` 升级为 v2，新增 `budgetStatus`、三项 `budgetResults`、`healthSummary`、`mitigationHints` 和 drift reason 边界；新增 `scripts/m5_release_health_gate.ps1`，最新 gate 为 `passed_with_budget_overrun`，checks failed `0`。desktop evidence history 与 Dashboard 现在能展示 `m5_release_health`，release contract 也从当前唯一主线 exe 读取 artifact presence，不再查历史 NSIS 安装包路径。
- 2026-06-02 M13 evidence trend/risk v2 已推进：desktop evidence history 为每个本地 report 增加同 kind 上一份 report 的 `previousStatus`、`previousFailureReason`、`previousGeneratedAt`、`statusTrend`、`failureReasonTrend`、`riskLevel`、`riskScore`、`previousRiskLevel`、`previousRiskScore`、`riskTrend` 和 `trendSummary`；同时间戳 report 用 `reportPath` 稳定排序；Dashboard evidence rows 仅展示趋势提示，不改变 status/evidenceLevel/gate 口径，不把 blocked/warning 转成 passed。
- 2026-06-02 M6 profile/browser comparison readability 已推进：desktop evidence history 对 `profile_browser_comparison` report 输出 `sameRunStatus`、desktop/profile signal counts、comparable category count、missing category statuses、pair delta/max window 和具体 next action；Dashboard 现有 evidence row 可直接展示缺口。该切片不采集新 WebView/profile-browser 证据，不改变 `blocked_missing_desktop_webview_report` 或 M4 expected external blocker 口径。
- 2026-06-02 M13 failure reason taxonomy / richer diff 已推进：desktop evidence history 新增 `failureReasonCategory`、category trend、`reportDiffSummary` 和结构化 `reportDiffItems`，把 release budget overrun、provider credentials missing、remote proxy missing、desktop/profile comparison gap、runtime adapter evidence required 和 expected external blockers 分类为机器可读排障字段，并展示 latest-vs-previous status/failureReason/category/risk diff；其中 cross-machine session pending 和 external operator smoke required 后续已取消，只保留历史分类语义；Dashboard 仅展示这些排障字段，不改变原始 `failureReason`、`status`、`evidenceLevel` 或 gate 语义。
- 2026-05-28 已跑真实 Xray/SingBox 本地二进制配置验证：`data/reports/transport-binary-smoke/transport-binary-smoke-1779981454305.json` 为 `passed`；Xray `26.2.6` 的 `run -test -config` 返回 `Configuration OK`，SingBox `1.12.17` 的 `check -c` exit code 为 `0`。该证据只覆盖 direct SOCKS + direct outbound 配置接受性，不是远程代理出站或 TLS/HTTP2 指纹观测。
- 2026-05-28 taxonomy coverage materialization 已跑通：`data/reports/taxonomy-coverage/taxonomy-coverage-materialized-1779981626750.json` 为 `passed_materialized_contract`，fingerprint `450 / 450`、behavior `461 / 450`。该报告是 materialized contract 覆盖，不是 observed fingerprint 或 live behavior replay 覆盖。
- 2026-05-28 已执行全量剩余证据 gate 并同步 report：provider acceptance `data/reports/provider-acceptance/provider-acceptance-preflight-1779940254070.json` 为 `blocked_missing_credentials`；SessionBundle portability `data/reports/session-portability/session-bundle-portability-smoke-1779940254019.json` 为 `local_contract_passed`；runtime adapter gate `data/reports/runtime-adapter/runtime-adapter-evidence-gate-1779940253967.json` 为 `blocked_evidence_required`；external distribution `data/reports/external-distribution/external-distribution-smoke-1779940275059.json` 为 `blocked_external_smoke_required`（后续已取消为目标）；taxonomy audit `data/reports/taxonomy-audit/taxonomy-audit-1779940275062.json` passed；live truth guard `data/reports/governance/live-truth-guard-1779941218292.json` passed。
- P34 修复 Launch API/key 与旧二进制产物问题：Go config loader 会展开 `${ENV}`；但 2026-05-25 用户已明确废弃多 exe 兼容入口路线，只保留 `personal-pilot-tauri.exe` 为主线，其余 exe 需剔除或退出主线。
- 2026-05-24 已生成 API surface smoke report：`data/reports/api-surface/api-surface-smoke-1779621749594.json`，结果 `passed`，覆盖 health/profiles/cores/proxy/list/groups/behavior/recording/logs/manual/subscribe 等只读或低风险端点。
- 2026-05-24 曾重跑并重建 root entry：`PersonaPilot.exe` 已从 Tauri/Vite UI 重新构建；该路线现在已被用户明确排除，并已在 2026-05-26 清理出根目录。
- 2026-05-27 已对根目录 `personal-pilot-tauri.exe` 运行 release smoke：`data/reports/release-smoke/release-performance-smoke-1779895292285.json`，结果 `warning`；该历史性能样本已被最新 M5 release health smoke 覆盖，当前指标以本文件 Build Status 中的最新 report 为准，仍不能写成 release performance green。
- 2026-05-27 已重跑 external distribution smoke：`data/reports/external-distribution/external-distribution-smoke-1779895317484.json`，本机资产检查按单入口 `personal-pilot-tauri.exe` 执行，当时状态为 `blocked_external_smoke_required`；manual operator smoke、干净 Win11 安装/启动/卸载、页面导航、provider readiness、SessionBundle smoke 后续已取消为当前目标。
- 2026-05-26 本轮尝试派出 3 个 subagent 做入口验证、UI 收敛风险扫描和文档一致性扫描；本地 agent 分发器均返回 `503 Service Unavailable`，因此同等检查已由主线程接管完成。
- 2026-05-28 主线程复核当前根目录 `personal-pilot-tauri.exe`：文件存在，大小 `20,585,984` bytes，SHA256 `704B14A6B358276124CBD5AF43E8BE0820507F2AD8A579CDBD3A87869DE701DC`。
- 2026-05-27 主线 UI API 收口记录中曾把 `src/modules/monitor/EventMonitorPage.tsx` 写入已收口范围；2026-06-11 代码复核确认该页仍有直接 Wails imports 和 `(window as any).runtime`，已在 M4 Monitor facade 切片修正。2026-06-01 M4.8 已继续收窄 synchronizer high-traffic DTO 与 browser Wails binding 类型边界；2026-06-02 已继续补 browser runtime payload schema、Dashboard typed facade、Settings backup / Browser logs typed facade、Profile remote author typed facade、FingerprintPanel behavior preset facade 和 AutomationPage scheduler/rule facade；2026-06-11 已继续补 App shell 与 Monitor typed facade；2026-06-12 已补 Browser runtime facade、Workbench DTO facade、页面级 runtime facade 和选定 UI error boundary source gate；2026-06-22 已补 browser settings/core/proxy facade gate。剩余统一边界风险主要集中在 legacy UI any-catch、剩余 browser/profile/session 兼容路径和过渡 `tauriWailsBridge` 兼容层。
- 2026-05-27 第二套 console UI 清理后，主线页面入口以 `src/App.tsx` 和 `src/modules/**` 为准；保留的 `src/pages/`、`src/components/`、`src/features/`、`src/hooks/`、`src/utils/` 仅为 Win11 baseline layout placeholder，不承载用户界面。
- 2026-05-27 曾定位实例详情/编辑/复制页历史回归：旧第二套 UI 的详情路由参数名和页面读取字段不一致。该 UI 已删除；当前仍必须用 `BrowserProfileList` / `GetDashboardStats` 真数据链路验证实例不是 `0`。
- 2026-05-24 已新增并运行 items 5-10 总闸：`data/reports/overall-remaining/overall-remaining-gate-1779621870664.json`，结果 `blocked_or_partial`；items `6/7/10` passed，items `5/8/9` 仍因 profile-browser validation、runtime adapter/headed external、AdsPower refresh evidence 不足而 blocked。

## 未完成边界

### Mainline remaining

- provider-side proxy rotation write 已闭环 (multi-agent worktree closeout)。
- Synchronizer native broadcast write path 已闭环 (physical SetWindowPos + deterministic ordering)。
- Recorder / Templates native-first de-fallback closure 已闭环 (desktop session guard + empty-state fix)。
- 最终 Win11 packaging / operator acceptance polish 仍需在不重开 scope 的前提下完成（本轮已收敛 Mainline P0）。

### Local Self-Use Closure

- Validation Board、profile-browser comparison、observed fingerprint strict gate、report history 和 Dashboard evidence rows 已闭环；full observed coverage 最新为 `passed_full_observed_fingerprint_coverage`，`450 / 450`。
- CAPTCHA/SMS/Email 后端 handler/route、dry-run、failure taxonomy、Settings operator surface 和 report history 已落地；唯一缺口是真实服务商账号凭证 smoke。
- runtime materialization depth 已从 `12` 扩到 `26` projected fields；`scripts/full_observed_fingerprint_probe.mjs` 已用本机 Chrome/Edge + CDP 采集并生成 `450` 条 observed signal。
- behavior event taxonomy 已有 machine-readable seed，本地 deterministic replay runtime 最新为 `passed_full_local_replay_runtime`，`461 / 450`，`contractOnly=0`。
- `SessionBundle` profile-level export、import preflight、dry-run 和 confirmed local restore write path 已落地；Settings 已新增本机 operator loop，可从 UI 触发 export、preflight、dry-run 和 confirmed local restore，并显示 blocker/restore plan/writePerformed；P14 `scripts/session_bundle_portability_smoke.ps1` 已重置为本机 restore smoke；最新 portability report `data/reports/session-portability/session-bundle-portability-smoke-1782111637335.json` 为 `local_restore_verified`，M4 gate 本地 operator contract passed，跨机器 profile portability 已取消。
- 2026-06-22 M8 SessionBundle gate 已从历史 handoff package 改为本机 restore gate：`scripts/m8_session_handoff_gate.ps1` 会刷新本地 portability smoke、校验刷新成功，并检查历史 runbook、Settings operator surface、desktop SessionBundle command/tests、typed TS contract 和 M4 boundary guard；Dashboard evidence history 已显示为本机恢复口径。最新 gate `data/reports/m8-session-handoff/m8-session-handoff-gate-1782111637657.json` 为 `passed_local_restore_verified`；第二机交接和跨机器 portability 已取消。
- 2026-06-02 M4 browser payload schema normalize 已新增：`src/modules/browser/types.ts` 暴露 `BrowserRuntimeEventPayload`，`src/modules/browser/api.ts` 暴露 `normalizeBrowserRuntimeEventPayload(payload: unknown)` 并把 `normalizeLaunchServerInfo` / `normalizeRecordingDetail` 输入从 `any` 收窄到 `unknown`；Browser List/Detail runtime event handlers 已改用共享 normalizer。新增 `scripts/m4_browser_payload_schema_gate.ps1`，最新 report `data/reports/m4-browser-payload-schema/m4-browser-payload-schema-gate-1780404372030.json` 为 `passed_browser_payload_schema_contract`；Dashboard evidence history 已新增 `M4 Payload` row，M4 gate v9 已纳入 `browser_payload_schema_contract`，最新 M4 report `data/reports/m4-acceptance/m4-acceptance-gate-1780404394910.json` 为 `passed_with_expected_external_blockers`，summary `passed=8`、`expectedBlocked=4`、`failed=0`。该证据只证明 browser runtime event payload source-level schema boundary，不代表 `tauriWailsBridge` 已移除、低频 workbench/core bridge 已收敛、真实 headed runtime、provider、远程代理/TLS、AdsPower refresh 或完整 `450` coverage 已完成；跨机器 SessionBundle 后续已取消。
- 2026-06-02 M4 Dashboard typed facade shrink 已新增：`src/modules/dashboard/api.ts` 不再动态 import Wails `App` 或使用 `const bindings: any`，Dashboard stats/license/config/CD key 调用改走 `src/services/desktop.ts` 的 `readDashboardStats`、`readLicenseStatus`、`reloadDesktopConfig`、`generateDesktopCdKeys` typed wrappers；Dashboard evidence history 新增 `M4 Dashboard` row。新增 `scripts/m4_dashboard_facade_gate.ps1`，最新 report `data/reports/m4-dashboard-facade/m4-dashboard-facade-gate-1780405656641.json` 为 `passed_dashboard_facade_contract`；M4 gate v10 已纳入 `dashboard_facade_contract`，最新 M4 report `data/reports/m4-acceptance/m4-acceptance-gate-1780405703275.json` 为 `passed_with_expected_external_blockers`，summary `passed=9`、`expectedBlocked=4`、`failed=0`。该证据只证明 Dashboard API source-level typed facade usage，不代表 `tauriWailsBridge` 已移除、profile/settings/logs 动态 bindings 已收敛或外部证据已完成。
- 2026-06-02 M4 Settings/Logs typed facade shrink 已新增：`src/modules/settings/api.ts` 不再动态 import Wails `App` 或使用 `const bindings: any`，Settings backup initialize/export/import 改走 `src/services/desktop.ts` typed wrappers，并保留 destructive preflight、native file dialog 和 confirmation 行为；`src/modules/browser/pages/BrowserLogsPage.tsx` 的 app logs 读取/清空改走 `getAppLogs` / `clearAppLogs` typed wrappers；`tauriWailsBridge` 仍保留 Wails-compatible proxy。新增 `scripts/m4_settings_logs_facade_gate.ps1`，latest report `data/reports/m4-settings-logs-facade/m4-settings-logs-facade-gate-1780407201965.json` 为 `passed_settings_logs_facade_contract`；Dashboard evidence history 新增 `M4 Settings/Logs` row；M4 gate v11 已纳入 `settings_logs_facade_contract`，最新 M4 report `data/reports/m4-acceptance/m4-acceptance-gate-1780407239835.json` 为 `passed_with_expected_external_blockers`，summary `passed=10`、`expectedBlocked=4`、`failed=0`。该证据只证明 Settings backup API 和 Browser logs source-level typed facade usage，不代表 `tauriWailsBridge` 已移除、profile/workbench/core bridge API 已收敛或外部证据已完成。
- 2026-06-02 M4 Profile typed facade shrink 已新增：`src/modules/profile/api.ts` 不再动态 import Wails `App`、不再使用 `const bindings: any` 或 `(window as any).go?.main?.App`，远端作者资料读取先走 `src/services/desktop.ts` 的 `fetchRemoteAuthorProfileFromDesktop` typed wrapper，并保留非桌面预览的 browser `fetch` fallback；payload 归一化从 `Record<string, any>` 收窄到 `Record<string, unknown>`。新增 `scripts/m4_profile_facade_gate.ps1`，latest report `data/reports/m4-profile-facade/m4-profile-facade-gate-1780408998290.json` 为 `passed_profile_facade_contract`；Dashboard evidence history 新增 `M4 Profile` row；M4 gate v12 已纳入 `profile_facade_contract`，latest report `data/reports/m4-acceptance/m4-acceptance-gate-1780409033459.json` 为 `passed_with_expected_external_blockers`，summary `passed=11`、`expectedBlocked=4`、`failed=0`。该证据只证明 Profile remote author source-level typed facade usage，不代表 `tauriWailsBridge` 已移除、workbench/core bridge API 已收敛或外部证据已完成。
- 2026-06-02 M4 Behavior preset typed facade shrink 已新增：`src/modules/browser/components/FingerprintPanel.tsx` 不再直接 import Wails `BehaviorPresetList`，行为预设加载改走 `src/modules/browser/api.ts` 的 `fetchBehaviorPresets` module facade；新增 `scripts/m4_behavior_preset_facade_gate.ps1`，latest report `data/reports/m4-behavior-preset-facade/m4-behavior-preset-facade-gate-1780409948440.json` 为 `passed_behavior_preset_facade_contract`；Dashboard evidence history 新增 `M4 Behavior` row；M4 gate v13 已纳入 `behavior_preset_facade_contract`，latest report `data/reports/m4-acceptance/m4-acceptance-gate-1780410001954.json` 为 `passed_with_expected_external_blockers`，summary `passed=12`、`expectedBlocked=4`、`failed=0`。该证据只证明 FingerprintPanel behavior preset source-level facade usage，不代表 `tauriWailsBridge` 已移除、browser/workbench/core bridge API 已收敛或外部证据已完成。
- 2026-06-02 M4 Automation typed facade shrink 已新增：`src/modules/browser/pages/AutomationPage.tsx` 不再直接 import Wails scheduler/rule App bindings 或 `backend` model constructors，任务列表/创建/删除/立即运行与响应规则列表/创建/删除/开关/测试触发改走 `src/modules/browser/api.ts` 的 `fetchSchedulerTasks`、`createSchedulerTask`、`deleteSchedulerTask`、`runSchedulerTaskNow`、`fetchAutomationRules`、`createAutomationRule`、`deleteAutomationRule`、`toggleAutomationRule`、`testFireAutomationRule` module facade；新增 `scripts/m4_automation_facade_gate.ps1`，latest report `data/reports/m4-automation-facade/m4-automation-facade-gate-1780411382396.json` 为 `passed_automation_facade_contract`；Dashboard evidence history 新增 `M4 Automation` row；M4 gate v14 已纳入 `automation_facade_contract`，latest report `data/reports/m4-acceptance/m4-acceptance-gate-1780411429879.json` 为 `passed_with_expected_external_blockers`，summary `passed=13`、`expectedBlocked=4`、`failed=0`。该证据只证明 AutomationPage scheduler/rule source-level facade usage，不代表 `tauriWailsBridge` 已移除、完整 browser/app shell/monitor/workbench/core bridge API 已收敛或外部证据已完成。
- 2026-06-11 M4 App shell typed facade shrink 已新增：`src/App.tsx` 不再直接 import Wails `ForceQuit` / `QuitAppOnly` App bindings 或 `wailsjs/runtime/runtime` 的 `Environment` / `Quit` / `WindowHide` / `WindowMinimise`，关闭确认、通知订阅、环境读取、托盘隐藏、最小化和退出动作改走 `src/services/desktop.ts` 的 `desktopRuntimeListen`、`desktopEnvironment`、`desktopQuitAppOnly`、`desktopQuitFull`、`desktopQuit`、`desktopWindowHide`、`desktopWindowMinimize` typed facade；新增 `scripts/m4_app_shell_facade_gate.ps1`，Dashboard evidence history 新增 `M4 App Shell` row；M4 gate v15 已纳入 `app_shell_facade_contract`。该证据只证明 App shell source-level facade usage，不代表 `tauriWailsBridge` 已移除、monitor/browser/workbench/core bridge API 已全部收敛或外部证据已完成。
- 2026-06-11 M4 Monitor typed facade shrink 已新增：`src/modules/monitor/EventMonitorPage.tsx` 不再直接 import Wails `EventLogQuery` / `EventLogCount` / `EventLogPrune` / `EventLogExport` 或 `wailsjs/go/models`，实时事件订阅改走 `desktopRuntimeListen`，历史 query/count/export/prune 改走 `queryEventLog`、`countEventLog`、`exportEventLog`、`pruneEventLog` typed wrappers，并修正一次性订阅回调读取旧 `paused` 状态的问题；新增 `scripts/m4_monitor_facade_gate.ps1`，latest report `data/reports/m4-monitor-facade/m4-monitor-facade-gate-1781196311382.json` 为 `passed_monitor_facade_contract`；Dashboard evidence history 新增 `M4 Monitor` row；M4 gate v16 已纳入 `monitor_facade_contract`，latest report `data/reports/m4-acceptance/m4-acceptance-gate-1781196340402.json` 为 `passed_with_expected_external_blockers`，summary `passed=15`、`expectedBlocked=4`、`failed=0`。该证据只证明 EventMonitor source-level facade usage，不代表 `tauriWailsBridge` 已移除、browser/workbench/core bridge API 已收敛或外部证据已完成。
- 2026-06-12 M4 Browser runtime facade shrink 已新增：`src/modules/browser/api.ts` 的 browser instance runtime 订阅改走 `desktopRuntimeListen` 并暴露 `onBrowserInstanceRuntimeEvents`，`BrowserListPage` / `BrowserDetailPage` 不再直接 import/call Wails `EventsOn`；`src/modules/browser/types.ts` 新增 `BrowserInstanceRuntimeEventName` / `BrowserInstanceRuntimeEvent`。新增 `scripts/m4_browser_runtime_facade_gate.ps1`、`m4-browser-runtime-facade` evidence kind 和 Dashboard `M4 Browser Runtime` row；M4 gate v17 已纳入 `browser_runtime_facade_contract`，最新复跑为 `passed_with_expected_external_blockers`，summary `passed=16`、`expectedBlocked=4`、`failed=0`。该证据只证明 Browser List/Detail runtime subscription source-level facade usage，不代表 `tauriWailsBridge` 已移除、settings/core/proxy runtime bridge 已收敛或外部证据已完成。
- 2026-06-12 M4 Workbench DTO facade shrink 已新增并继续扩展：`src/types/desktop.ts` 增加 `DesktopCoreWorkbenchDetectionResult`、`DesktopCoreWorkbenchUiState`、`DesktopCoreWorkbenchDetectorSite`、`DesktopBrowserProfile`、`DesktopCoreWorkbenchFingerprintSnapshot`、`DesktopCoreWorkbenchFingerprintHealthProfile`、`DesktopCoreWorkbenchIdentityStrengthReport` 等共享 DTO；`src/services/desktop.ts` 的 Workbench detection results / detector sites / UI state / detector run / fingerprint health / fingerprint snapshot / identity report / browser instance status API 不再暴露 `unknown[]` 或裸 `unknown`；`src/modules/synchronizer/api.ts` 移除对应 `Array.isArray(results)` 防御性退化和 report/status cast。M4 gate v20 已把这些 Workbench/report DTO 纳入 `typed_facade_shrink_contract`，最新复跑为 `passed_with_expected_external_blockers`，summary `passed=17`、`expectedBlocked=4`、`failed=0`。该证据只证明 Workbench/report source-level DTO boundary，不代表 `tauriWailsBridge` 已移除、settings/core/proxy bridge 已收敛或外部证据已完成。
- 2026-06-12 M4 runtime facade shrink 已新增：`SettingsPage` 的 backup export/import progress、`CoreManagementPage` 的 download progress 和外链打开、`ProxyPickerModal` / `ProxyPoolPage` 的 proxy speed/IP health runtime result 事件、`LaunchApiDocsPage` / `UsageTutorialPage` 的外链打开均改走 `src/services/desktop.ts` 的 `desktopRuntimeListen` / `desktopOpenExternalUrl`；新增 `scripts/m4_runtime_facade_gate.ps1`、`m4-runtime-facade` evidence kind 和 Dashboard `M4 Runtime` row；M4 gate v19 已纳入 `runtime_facade_contract`，最新复跑为 `passed_with_expected_external_blockers`，summary `passed=17`、`expectedBlocked=4`、`failed=0`。该证据只证明选定页面级 runtime event / external URL source-level facade usage，不代表 `tauriWailsBridge` 已移除、所有 core/proxy/settings API 已统一或外部证据已完成。
- 2026-06-12 M4 UI error boundary shrink 已新增：`src/shared/errors.ts` 提供 `messageFromUnknownError(error: unknown)`；Dashboard、Dashboard API、Settings、Automation、NaturalLanguageTask、TagManagement、RecordingDetailModal 的选定错误处理从 `catch (...: any)` 改为 `unknown` guard；Dashboard WebView audio fallback 不再使用 `window as any`，Launch API docs markdown code block 不再使用 `children as any`。M4 gate v21 新增 `ui_error_boundary_contract`，最新复跑为 `passed_with_expected_external_blockers`，summary `passed=18`、`expectedBlocked=4`、`failed=0`。该证据只证明选定 UI surface source-level error boundary，不代表全仓 legacy `any` catch 已清零、`tauriWailsBridge` 已移除或外部证据已完成。
- 2026-06-12 M4 Browser instance UI error boundary shrink 已新增：`BrowserListPage`、`BrowserDetailPage`、`BrowserEditPage`、`BrowserSettingsModal`、`QuickLaunchModal` 的实例启动/停止/重启、录制、复制、编辑保存、内核保存、按 Code 启动等错误路径从 `catch (...: any)` 收窄到 `unknown` guard，并复用 `messageFromUnknownError` / `resolveActionFeedback` / `resolveActionErrorMessage` 保持 background attach warning 语义；同时修正 BrowserList 启动成功/失败的既有 mojibake 文案。M4 gate v22 扩展 `ui_error_boundary_contract` 覆盖这批 Browser instance surfaces，并新增 action-specific marker 与 mojibake 防回归检查；最新复跑为 `passed_with_expected_external_blockers`，summary `passed=18`、`expectedBlocked=4`、`failed=0`。该证据仍只证明选定 UI surface source-level error boundary，不代表全仓 legacy `any` catch 已清零或外部证据已完成。
- 2026-06-12 M4 Core management UI error boundary shrink 已新增：`CoreManagementPage` 的打开目录、扫描、保存/删除内核、设为默认、下载启动和全局设置保存错误路径从 `catch (...: any)` 收窄到 `unknown` guard，并复用 `messageFromUnknownError`。M4 gate v23 把 Core management surface 纳入 `ui_error_boundary_contract`；最新复跑仍为 `passed_with_expected_external_blockers`，summary `passed=18`、`expectedBlocked=4`、`failed=0`。该证据只证明 CoreManagementPage selected UI error boundary，不代表 ProxyPoolPage 或全仓 legacy `any` catch 已清零。
- 2026-06-12 M4 Proxy pool UI error boundary shrink 已新增：`ProxyPoolPage` 的订阅刷新、批量/单项删除、代理保存、URL 获取 fallback、订阅导入、解析、导入确认和代理名称修复错误路径从 `catch (...: any)` 收窄到显式 `unknown` guard，Clash proxy index signature 从 `any` 收窄到 `unknown`，并复用 `messageFromUnknownError` 保留原 fallback 文案。M4 gate v24 把 Proxy pool surface 纳入 `ui_error_boundary_contract`，并允许任意 catch 参数名但必须显式 `: unknown`；最新复跑仍为 `passed_with_expected_external_blockers`，summary `passed=18`、`expectedBlocked=4`、`failed=0`。该证据只证明 selected UI/source error boundary，不代表全仓 generic `Record<string, any>` 已清零或外部证据已完成。
- 2026-06-12 M4 generic any residue shrink 已新增：`Table` 泛型约束从 `Record<string, any>` 收窄到 `object` 并通过局部 `Record<string, unknown>` 读取动态列值；`ProxyIPHealthResult.rawData` 改为 `Record<string, unknown>`；`ProxyPoolPage/types.ts` 的 Clash proxy index signature 改为 `unknown`。M4 gate v25 新增 `generic_any_residue_contract`，最新复跑为 `passed_with_expected_external_blockers`，summary `passed=19`、`expectedBlocked=4`、`failed=0`。该证据只证明选定 generic any 残留已收窄，不代表仓库每一个显式 `any` 都已移除。
- 2026-06-12 M4 RecordingPanel UI error boundary shrink 已新增：`RecordingPanel` 录制/停止/养号/删除/导出/复制/导入/重命名/回放/暂停处理/接管/清理错误路径从未显式类型的 `catch (e)` 收窄到 `catch (error: unknown)`，本地错误文案复用 `messageFromUnknownError`，并修正回放失败 mojibake 文案。M4 gate v26 把 RecordingPanel 纳入 `ui_error_boundary_contract`，最新复跑为 `passed_with_expected_external_blockers`，summary `passed=19`、`expectedBlocked=4`、`failed=0`。该证据只证明 RecordingPanel selected UI error boundary，不代表全仓 legacy catch 已清零或外部证据已完成。
- 2026-06-12 M4 bridge compatibility type shrink 已新增：`src/services/desktop.ts` 的 `desktopRpc` 参数通道改用 `DesktopRpcArg` / `DesktopRpcArgs` 命名边界；`src/services/tauriWailsBridge.ts` 的 runtime event data、App proxy method 和 bridge result 改用 `BridgeEventData`、`BridgeRpcMethod`、`BridgeAppProxy` 等兼容层类型，避免散落裸 `unknown[]` / `Promise<unknown>`。M4 gate v27 新增 `bridge_compat_type_contract`，最新复跑为 `passed_with_expected_external_blockers`，summary `passed=20`、`expectedBlocked=4`、`failed=0`。该证据只证明桥接兼容层 source-level type boundary，不代表 `tauriWailsBridge` 已移除或所有 browser/settings/core/proxy API 已统一。
- 2026-06-20 M4 browser window.go fallback shrink 已新增：`src/modules/browser/api.ts` 的订阅 URL 导入、代理名称修复、Clash URL 导入和 Launch server info 读取不再各自直接访问 `window.go` / `go?.main?.App` fallback，改走 `src/services/desktop.ts` 的 `importBrowserProxySubscriptionFromDesktop`、`fixBrowserProxyNamesFromDesktop`、`fetchBrowserProxyClashFromDesktop`、`readLaunchServerInfoFromDesktop` typed wrappers；M4 gate v28 新增 `browser_window_go_fallback_contract`。2026-06-22 继续新增 M4 browser settings/core/proxy facade shrink：`src/modules/browser/api.ts` 的 Settings/Core/Proxy sections 改走 `src/services/desktop.ts` typed wrappers，`BrowserNativeBindings` 不再 advertise retired settings/core/proxy Wails methods，`tauriWailsBridge` 改为显式 `BRIDGE_RPC_METHOD_NAMES` allowlist；独立 report `data/reports/m4-browser-settings-core-proxy-facade/m4-browser-settings-core-proxy-facade-gate-1782114285844.json` 为 `passed_browser_settings_core_proxy_facade_contract`。
- 2026-06-22 M4 gate 已升级到本机自用口径；profile/browser comparison、SessionBundle 本机 restore、runtime adapter local self-use 已 passed。expected blocker 只剩 provider credentials / real smoke。该证据仍不代表 `tauriWailsBridge` 已移除，只代表其 surface 已显式 allowlist 并继续作为过渡兼容层。
- 2026-06-22 observed/replay/runtime/process 新证据：strict observed fingerprint coverage 为 `passed_full_observed_fingerprint_coverage`，`450 / 450`；live replay runtime 为 `passed_full_local_replay_runtime`，`461 / 450`，`contractOnly=0`；M10 stability 为 `passed_long_task_stability_coherence`；M15 browser process 为 `passed_real_browser_process_prewarm_cleanup`；M15 pool/process 为 `passed_real_pool_process_integration`；remote proxy/TLS 默认改为本机 direct TLS/transport observed；runtime adapter 为 `passed_local_self_use`。
- headed runtime、kernel strategy、observed/replay、TLS/transport、M15 pool/process 和 report history 已按本机自用范围闭环。Dashboard 已新增 Runtime Adapter 卡和 M4 Browser Facade / Observed Fingerprint / Replay Runtime / M10 Stability / M15 Process evidence rows。当前唯一未验是 CAPTCHA/SMS/Email 真实服务商账号凭证 smoke；`tauriWailsBridge` 仍存在，但已显式 allowlist 并继续作为过渡兼容层，不再把“完全移除 bridge”当作本轮阻塞。
- 代理链路现在分三段看：`BrowserProxyCheckIPHealth` / `BrowserProxyTestSpeed` 走 Go HTTP client，带认证的 HTTP/SOCKS/SOCKS5/SOCKS5H 标准代理可以被正确测试；浏览器启动链路会把带认证的标准代理转成本机 `sing-box` 无认证入口，再把 Chrome 指向本机 `socks5://127.0.0.1:port`；如果代理 URL 带 `?pp_via_ssh=panda`，应用会先自动启动本机 `ssh -L` 隧道，让链路变成 `Chrome -> 本机 sing-box -> 本机 SSH local forward -> panda VPS -> 上游代理`。
- 2026-06-28 复核：`sing-box check` 已接受本机生成的 HTTP / HTTPS / SOCKS5 认证出站配置；Webshare 从本机直连未成功，但经 `panda` VPS 可用。UDEAL 这条代理本机 TCP 端口可达但 HTTP/SOCKS 认证握手被上游断开；经 `panda` VPS 出口后 HTTP 和 SOCKS 均可用，出口为美国洛杉矶，`ip-api` 返回 `proxy=false`、`hosting=false`。当前代码已把手工 `ssh -L` 方案做成 URL 元参数：例如标准代理 URL 末尾追加 `?pp_via_ssh=panda`，普通代理不受影响；启动、测速和 IP 健康检测都会通过同一 bridge manager 自动走 VPS 链路。
- external browser integration 已有计划和 contract 边界，但不是完整 runtime depth；主仓库仍不托管 Chromium/Firefox fork。2026-05-27 已在 `docs/18-external-browser-integration-plan.md` 固化并深化 Camoufox `90` 分方案；2026-05-28 已把 Camoufox 作为 `browser_cores.kind=camoufox` 接入主线内核管理、路径校验、实例二进制解析和启动参数分发，并在主线 UI 三个内核入口可选；Rust runner 现在是最小 CDP runner 而非 skeleton；真实 Camoufox binary 已完成 headless screenshot page-open 证据；2026-05-31 `headed_external` 已完成真实 Chromium CDP 单任务 smoke、profile-browser `validation_probe` smoke，并修正 ranked evidence 选择。仍不能写成完整 Camoufox runtime-ready、完整 headed runtime realism-ready 或代理/TLS/行为 replay 闭环。
- `docs/24-external-distribution-readiness.md`、`docs/release-performance-mitigation-plan.md` 和 `docs/sessionbundle-cross-machine-portability-runbook.md` 只保留历史上下文；当前本机自用范围不再要求外部分发人工 smoke、profile cross-machine portability 或 release performance budget。

## 当前下一步

**本批（2026-07-11 续推）已落地**：uTLS Graph 出站、profile 同代理出口、TrustSurface CDP hydrate、DP1 人格库（≥12）、A5 噪声、S3 WebGPU 对齐、L5-3 惯性 wheel、CP2 并发预算。详见 `PLAN.md`。

**仍可本地推进：**

1. PersonaID SQLite 列持久化；DP3 geo 活体
2. docs/51 S4–S14；docs/54 CP3–6；docs/55 纵向指标
3. RPA 可视化 MVP；有额度时复跑 missing/deep **刷分**

**暂缓（外部条件）：** CAPTCHA/SMS/Email 真实凭证 live、Germany 节点、DNS-token、BitBrowser 额度、AdsPower 付费 API。

**执行入口**：`PLAN.md`。
