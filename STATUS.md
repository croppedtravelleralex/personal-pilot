# STATUS.md

This root `STATUS.md` is a compatibility entrypoint.
Canonical status now lives in `/docs/02-current-state.md`.

## Current Truth Markers

- `runtime alive` is not the same thing as `build recovered`
- gateway health with `upstream_configured=false` means shell online, not upstream productized
- `verify_proxy` volume does not count as browser mainline acceptance

## Follow These Docs

1. `/docs/02-current-state.md`
2. `/docs/final-goal-progress-breakdown.md`
3. `/docs/agent-alexstudio-gateway-runbook.md`

## Reporting Guardrail

When root and `/docs` wording drift, trust `/docs` and rewrite the root entrypoint instead of reviving legacy stage-closeout text.

